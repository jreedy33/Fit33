# Supabase Migration Index — Canonical Source of Truth

> **Rule**: All new schema changes MUST be added as a new timestamped file.
> Never modify an already-deployed migration. If a fix is needed, create a new file.

## Scope & Policy (Q2-85, Sprint 8 — 2026-04-27)

This index is the **canonical release-train** for Supabase migrations. It is
NOT a line-by-line listing of every file under `supabase/*.sql` — there are
~175 files on disk; this document tracks ~60 release-train entries.

**In scope (MUST be indexed here):**

- Any new `YYYYMMDD_…` migration that creates tables, alters schema, creates
RPCs, touches RLS, or enables Realtime publications.
- Any bug-fix / security hotfix migration that is expected to be run on prod.
- Each entry gets a numbered row, a status emoji, and a "What it does" note
covering the invariants the app relies on.

**Out of scope (tracked in the §Legacy / Bulk Ledger below, not inline):**

- Pre-`YYYYMMDD_` naming era files (e.g. `challenge_rpc_functions.sql`,
`friend_request_system.sql`). Historic, mostly already-absorbed by later
hotfixes. Kept on disk for audit; do not edit.
- Bulk data scripts — `exercise_replace_{01..15}.sql`,
`update_exercises_*.sql`, `exercises_update_*.sql` — one-shot exercise CSV
loads.
- Read-only auditors / verifiers — `audit_before.sql`, `audit_after.sql`,
`verify_query_performance.sql`, `verify_*.sql`, `sim_test_helpers.sql`.
- Private-challenge / weekly-league foundational migrations that pre-dated
the index (already superseded or consolidated by later `YYYYMMDD_` files).

**Authoring rule for new migrations:**

1. New file name format: `YYYYMMDD_short_description.sql`.
2. Append a new numbered row to the dated section at the bottom (create a
  new section for the date if needed).
3. Never retroactively edit a deployed migration — create a new hotfix file
  and reference the supersession in both files.
4. If the migration is a bulk data load or a one-shot audit query, it stays
  in the §Legacy / Bulk Ledger.

---

## 🚨 Deployment Priority Queue (2026-04-24)

The 2026-04-24T11:10 Cursor Bug-Intel export shows several CRITICAL / HIGH
clusters that are **already fixed in migration files but the files have not
been applied to prod**. Deploying these is the highest-leverage action to
drain the next export. Order matters (B → F → I → J):

1. **B** — `20260511_health_rls_audit.sql` (row #77) — canonicalizes RLS on
  `cardio_workouts` / `daily_activity_summary` / `sleep_logs`. Drains the
   42501 HealthKit RLS cluster (fingerprints `c0a9d730` / `a5b6830c` /
   `5b97a3bc` / `7f062850`, 40+ occurrences / 5+ users).
2. **F** — `20260513_drop_post_workout_activity_overloads.sql` (row #79) —
  drops the 7-arg `post_workout_activity` overload so PostgREST can pick
   the canonical 8-arg signature. Drains PGRST203 cluster
   (fingerprint `d4ca061f`).
3. **I** — `20260514_performance_metrics.sql` (row #80) — unlocks the
  `performance_metrics` p50/p95 dashboards so the next bug-intel report
   can include "is this op *actually* slower than yesterday?" context.
4. **J** — `20260517_bug_intel_noise_filter_expand.sql` (row #83, new) —
  noise-filters the watchdog signals + Cloudflare 502/503 + P0001 auth
   transients + legacy PGRST203 noise that are drowning every export.
   Auto-resolves matching back-catalog fingerprints.
5. **K** — `20260518_fix_sync_profile_weight_uuid_cast.sql` (row #84, new) —
  Drops the stray `::text` cast in `public.sync_profile_weight()`
   (AFTER INSERT on `weight_logs`) that was the real 42883 root cause
   for cluster C (fingerprints `ff6ae8d5` / `222e0fc1` / `95b0b27b`).
   Run AFTER 83 so the auto-resolve clause finds the fingerprints 83
   failed to catch (83's filter was message-regex-only; 84 targets by
   42883 + weight_logs presence).
6. **L** — `20260519_bug_intel_phase11_sweep.sql` (row #85, new) — Phase
  11 uncategorized drain. 7 new noise filter rows (CrashReporter
   self-upload loop + generic NSURL transients + Swift-side P0001),
   auto-stale clause for 7-day-silent triaged fingerprints, classifier
   expansion with 4 new cluster codes (H/I/J/K), after-sweep baseline
   snapshot. Idempotent — safe to re-run.

Deploy order: run 77 → 79 → 80 → 83 → 84 → 85 in a single session. All
are idempotent.
After deploy, clear export watermark once:
`UPDATE bug_intel_export_runs SET exported_through_dev_session_log_id = NULL, exported_through_crash_id = NULL WHERE id = (SELECT MAX(id) FROM bug_intel_export_runs);`
so the next `mode=new` export starts with a clean slate.

---

## Deployed Migrations (in order)


| #   | File                                     | Status                 | What it does                                                                                                        |
| --- | ---------------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------- |
| 1   | `fix_contact_matching_rls.sql`           | ✅ Deployed             | RLS policies for contact discovery                                                                                  |
| 2   | `cascade_delete_incomplete_profiles.sql` | ✅ Deployed             | Cascade delete for incomplete onboarding profiles                                                                   |
| 3   | `cleanup_incomplete_onboarding.sql`      | ✅ Deployed             | Auto-cleanup abandoned profiles (30 min)                                                                            |
| 4   | `complete_account_deletion.sql`          | ✅ Deployed             | Full data wipe on account delete                                                                                    |
| 5   | `friend_request_system.sql`              | ✅ Deployed             | Friend request CRUD (send/accept/reject/cancel)                                                                     |
| 6   | `friend_request_notifications.sql`       | ✅ Deployed             | Push notifications for friend requests                                                                              |
| 7   | `fix_friend_safety.sql`                  | ✅ Deployed             | Safety fixes for decline/cancel friend requests                                                                     |
| 8   | `create_friend_rpc_functions.sql`        | ✅ Deployed             | `get_friends()`, `get_received_workouts()`, `get_sent_workouts()`                                                   |
| 9   | `fix_data_relationships.sql`             | ✅ Deployed             | FK constraints and orphan data cleanup                                                                              |
| 10  | ~~`fix_account_deletion.sql`~~           | 🗑️ Removed 2026-04-17 | Duplicate of `complete_account_deletion.sql`. Canonical `delete_user_account()` RETURNS `jsonb` lives in that file. |
| 11  | `fix_trigger_conflicts.sql`              | ✅ Deployed             | Resolve conflicting auth deletion triggers                                                                          |
| 12  | `challenge_type_migration.sql`           | ✅ Deployed             | Challenge type enum + leaderboard tables                                                                            |
| 13  | `challenge_rpc_functions.sql`            | ✅ Deployed             | **Canonical challenge RPCs** (create, respond, log, cancel, leave, etc.)                                            |
| 14  | `community_challenges_migration.sql`     | ✅ Deployed             | Community challenge tables + RPCs                                                                                   |
| 15  | `fix_create_group_challenge.sql`         | ✅ Deployed             | Fix group challenge creation                                                                                        |
| 16  | `fix_ambiguous_columns.sql`              | ✅ Deployed             | Resolve ambiguous column references                                                                                 |
| 17  | `fix_group_challenge_timezone.sql`       | ✅ Deployed             | Timezone handling for group challenges                                                                              |
| 18  | `fix_leave_group_challenge.sql`          | ✅ Deployed             | Fix leave/cancel group challenge RPCs                                                                               |
| 19  | `fix_challenge_cascade_delete.sql`       | ✅ Deployed             | Cascade deletes for challenge-related tables                                                                        |
| 20  | `fix_challenge_participants.sql`         | ✅ Deployed             | Fix challenge participant constraints                                                                               |
| 21  | `fix_comprehensive_audit.sql`            | ✅ Deployed             | Comprehensive fixes from audit                                                                                      |
| 22  | `fix_cardio_workouts_constraint.sql`     | ✅ Deployed             | Deduplicate cardio workout rows                                                                                     |
| 23  | `fix_materialized_view.sql`              | ✅ Deployed             | Fix/refresh materialized views                                                                                      |
| 24  | `refresh_exercise_view.sql`              | ✅ Deployed             | Refresh exercise materialized view                                                                                  |
| 25  | `program_templates_migration.sql`        | ✅ Deployed             | Program templates table + seed data                                                                                 |
| 26  | `global_food_popularity.sql`             | ✅ Deployed             | Food popularity tracking tables                                                                                     |
| 27  | `verify_critical_functions.sql`          | ✅ Deployed             | Verification checks for critical RPCs                                                                               |


## Exercise Data (bulk updates — run once)


| File                               | Status     | Notes                                     |
| ---------------------------------- | ---------- | ----------------------------------------- |
| `update_exercises_from_csv.sql`    | ✅ Deployed | Initial exercise data load                |
| `update_exercises_batch2.sql`      | ✅ Deployed | Batch 2 updates                           |
| `update_exercises_batch3.sql`      | ✅ Deployed | Batch 3 updates                           |
| `update_exercises_final_fixed.sql` | ✅ Deployed | Final exercise data with lever name fixes |


---

## Duplicated Function Definitions (Canonical Owners)

These functions are defined in multiple files due to historical hotfixes.
The **canonical version** is the one in the latest file listed below.


| Function                             | Canonical File                     | Also in (superseded)                                          |
| ------------------------------------ | ---------------------------------- | ------------------------------------------------------------- |
| `get_active_group_challenges`        | `fix_group_challenge_timezone.sql` | `challenge_rpc_functions.sql`, `fix_comprehensive_audit.sql`  |
| `log_challenge_progress`             | `fix_ambiguous_columns.sql`        | `challenge_rpc_functions.sql`, `challenge_type_migration.sql` |
| `cancel_group_challenge`             | `fix_leave_group_challenge.sql`    | `challenge_rpc_functions.sql`, `fix_ambiguous_columns.sql`    |
| `reject_friend_request`              | `fix_comprehensive_audit.sql`      | `fix_friend_safety.sql`, `friend_request_system.sql`          |
| `decline_friend_request`             | `fix_comprehensive_audit.sql`      | `fix_friend_safety.sql`                                       |
| `cancel_friend_request`              | `fix_comprehensive_audit.sql`      | `fix_friend_safety.sql`                                       |
| `delete_user_account`                | `complete_account_deletion.sql`    | `fix_account_deletion.sql` (deleted 2026-04-17)               |
| `delete_auth_user_on_profile_delete` | `complete_account_deletion.sql`    | `cascade_delete_incomplete_profiles.sql`                      |
| `send_friend_request`                | `friend_request_notifications.sql` | `friend_request_system.sql`                                   |
| `cleanup_auth_on_profile_delete`     | `fix_trigger_conflicts.sql`        | `fix_account_deletion.sql`                                    |
| `create_group_challenge`             | `fix_create_group_challenge.sql`   | `challenge_rpc_functions.sql`                                 |
| `leave_group_challenge`              | `fix_leave_group_challenge.sql`    | `challenge_rpc_functions.sql`                                 |
| `log_group_challenge_progress`       | `fix_group_challenge_timezone.sql` | `challenge_rpc_functions.sql`                                 |
| `get_active_challenges`              | `fix_comprehensive_audit.sql`      | `challenge_rpc_functions.sql`                                 |


---

## Database Audit Remediation (2026-03-20)


| #   | File                                      | Status   | What it does                                                   |
| --- | ----------------------------------------- | -------- | -------------------------------------------------------------- |
| 28  | `audit_before.sql`                        | 🆕 Ready | Baseline snapshot (read-only, run first)                       |
| 29  | `20260320_drop_dead_tables.sql`           | 🆕 Ready | DROP 13 dead tables (0 rows, 0 code refs) with safety guards   |
| 30  | `20260320_add_missing_fk_constraints.sql` | 🆕 Ready | FK CASCADE on 11 analytics tables + exercise_videos + indexes  |
| 31  | `20260320_sync_profiles_progress.sql`     | 🆕 Ready | Bidirectional sync trigger for user_profiles <-> user_progress |
| 32  | `20260320_consolidate_food_history.sql`   | 🆕 Ready | user_food_history_v view from meal_logs                        |
| 33  | `audit_after.sql`                         | 🆕 Ready | 7-test verification suite (read-only, run last)                |


**Run order**: 28 → 29 → 30 → 31 → 32 → 33

## Smart Insights Enhancement (2026-03-20)


| #   | File                                     | Status   | What it does                                                                              |
| --- | ---------------------------------------- | -------- | ----------------------------------------------------------------------------------------- |
| 34  | `20260320_smart_insights_schema.sql`     | 🆕 Ready | Phase 1: completion_rate, opened_at, referral_source columns + subscription_events table  |
| 35  | `20260320_smart_insights_views.sql`      | 🆕 Ready | Phase 2: 5 cross-table correlation views (nutrition, hydration, social, challenge, sleep) |
| 36  | `20260320_smart_nudge_notifications.sql` | 🆕 Ready | Phase 4.3: generate_smart_nudges() RPC for targeted push notifications                    |


**Run order**: 34 → 35 → 36

## USDA Food Search Integrity (2026-03-21)


| #   | File                                 | Status   | What it does                                                                              |
| --- | ------------------------------------ | -------- | ----------------------------------------------------------------------------------------- |
| 37  | `20260321_food_search_integrity.sql` | 🆕 Ready | Cache TTL column, frequent foods RPC, user_food_history index, user_favorite_foods UNIQUE |


**Run order**: 37 (standalone, no dependencies on 28-36)

## Notification Preferences (2026-03-21)


| #   | File                                    | Status   | What it does                                                                          |
| --- | --------------------------------------- | -------- | ------------------------------------------------------------------------------------- |
| 38  | `20260321_notification_preferences.sql` | 🆕 Ready | user_notification_preferences table + RLS for server-side push preference enforcement |


**Run order**: 38 (standalone)

## Activity Feed / Privacy / Leagues (2026-03-30 → 2026-03-31)


| #   | File                                   | Status   | What it does                                                     |
| --- | -------------------------------------- | -------- | ---------------------------------------------------------------- |
| 39  | `20260330_activity_feed_exercises.sql` | 🆕 Ready | Activity feed exercise rows + RPC additions                      |
| 40  | `20260330_add_cover_image_to_rpcs.sql` | 🆕 Ready | Include cover_image field in challenge list RPCs                 |
| 41  | `20260330_league_privacy_realtime.sql` | 🆕 Ready | Weekly league privacy + realtime subscriptions                   |
| 42  | `20260330_privacy_photo_all_rpcs.sql`  | 🆕 Ready | Photo privacy enforcement across all profile RPCs                |
| 43  | `20260330_privacy_rpc_enforcement.sql` | 🆕 Ready | RPC-level privacy enforcement for shared RPCs                    |
| 44  | `20260330_privacy_settings.sql`        | 🆕 Ready | `privacy_settings` table + RLS + RPCs                            |
| 45  | `20260331_league_auto_placement.sql`   | 🆕 Ready | `auto_place_all_league_members()` pg_cron job (Monday 00:15 UTC) |


**Run order**: 39 → 40 → 41 → 42 → 43 → 44 → 45 (each is idempotent; 45 installs pg_cron schedule)

## Sprint 1 Lockdown (2026-04-17)


| #   | File                                         | Status   | What it does                                                                    |
| --- | -------------------------------------------- | -------- | ------------------------------------------------------------------------------- |
| 46  | `20260417_secure_get_friend_ids.sql`         | 🆕 Ready | IDOR guard: `get_friend_ids(p_user_id)` now rejects other users                 |
| 47  | `20260417_phone_verification_rate_limit.sql` | 🆕 Ready | DB-backed Twilio rate limit table + `check_phone_verification_rate_limit()` RPC |
| 48  | `20260417_ai_insights_admin_emails.sql`      | 🆕 Ready | Admin allowlist for `generate-ai-insights` edge function                        |


**Run order**: 46 → 47 → 48 (standalone, idempotent).
**Paired code changes**: edge functions `moderate-content`, `send-verification`, `generate-ai-insights`, `usda-food-search`, `notify-contacts-user-joined`, `send-push-notification` all redeploy this sprint with tightened auth.

## Sprint 2 Submit-Ready (2026-04-18)


| #   | File                                             | Status   | What it does                                                                         |
| --- | ------------------------------------------------ | -------- | ------------------------------------------------------------------------------------ |
| 49  | `20260418_blocking_and_reporting.sql`            | 🆕 Ready | `get_blocked_users()` + `report_content()` RPCs for App Review social compliance     |
| 50  | `20260418_group_challenge_members_invariant.sql` | 🆕 Ready | Legacy `group_challenge_members` hardening: REVOKE direct writes + COMMENT invariant |
| 51  | `20260418_post_cardio_activity.sql`              | 🆕 Ready | `post_cardio_activity()` RPC — cardio parity in friend activity feed                 |


**Run order**: 49 → 50 → 51 (standalone, idempotent).
**Paired code changes**: `BlockedUsersView`, Settings integration, Report+Block sheets in chat & activity feed (Q2-7); cardio gamification wires (`UserManager.completeCardioWorkout`, Q2-5); `verify-code` / `send-push-notification` CORS migration (Q2-22).

## Challenge Background Refresh (2026-04-20)


| #   | File                                   | Status   | What it does                                                                                                                                                      |
| --- | -------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 52  | `20260420_challenge_opponent_wake.sql` | 🆕 Ready | `silent_push_wake_log` table + RLS + 7-day prune cron; `trigger_challenge_opponent_wake()` pg_cron every 30 min invokes `wake-challenge-opponents` edge function. |


**Run order**: 52 (standalone, idempotent). Requires `internal_config` rows `supabase_url`, `service_role_key`, `anon_key` (already seeded by migration 20260324_push_notification_cron.sql).
**Paired code changes**:

- New edge function: `supabase/functions/wake-challenge-opponents/index.ts` (deploy with `supabase functions deploy wake-challenge-opponents` — uses same APNS_* secrets as `send-push-notification`).
- New Swift files: `Fit33/SilentPushHandler.swift`, `Fit33/ChallengeOpponentWakeService.swift`.
- Modified: `Fit33/BackgroundChallengeSyncService.swift` (adds `BGProcessingTask` + per-source throttle + post-sync opponent-wake call), `Fit33/Fit33App.swift` (AppDelegate `didReceiveRemoteNotification` + scenePhase `.active` wake trigger), `Fit33/Info.plist` (adds `com.gofit.app.challengeSyncProcessing` BGTask identifier + `remote-notification` UIBackgroundMode).

## Cardio History Cleanup (2026-04-20)


| #   | File                                         | Status   | What it does                                                                                                                                                                                                                                                                                                                                                        |
| --- | -------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 53  | `20260420_cardio_workouts_overlap_dedup.sql` | 🆕 Ready | One-time cleanup of duplicate WHOOP `cardio_workouts` rows (same user, same origin, overlapping time windows). Keeps the richest row via a scored comparison (specific `activity_type`, heart-rate, distance, calories). Client-side dedup now lives in `HealthDataService.syncWhoopData`; this migration fixes the rows that were already inserted before the fix. |


**Run order**: 53 (standalone, idempotent). Safe to re-run — the CTE compares every row against every other row in the same (user, origin) window and deletes only the loser.
**Paired code changes**: `Fit33/HealthDataService.swift` — `syncWhoopData` time-overlap dedup + scoring.

## Daily Quest Overhaul (2026-04-20)


| #   | File                                         | Status   | What it does                                                                                                                                                                                                                                                                                                                          |
| --- | -------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 54  | `20260420_daily_quests_actionable_fixes.sql` | 🆕 Ready | Fixes three UX bugs in `get_daily_quests`: (1) honour `quest_templates.requires_context` gates so users without a program stop seeing "Program Day"; (2) retires the "Double Session — 2 workouts today" template; (3) restores `tracking` / `wildcard` / `social` pool categories that the prior pool-builder was silently dropping. |


**Run order**: 54 (standalone, idempotent). Supersedes selector logic from `20260325_quest_challenge_sync.sql` — no paired code change needed; app reads quests via the same RPC signature.
**Paired code changes**: none (RPC signature unchanged).

## Exercise Library Realtime (2026-04-20)


| #   | File                              | Status     | What it does                                                                                                                    |
| --- | --------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------- |
| 55  | `20260420_exercises_realtime.sql` | ✅ Deployed | Enables Realtime REPLICA IDENTITY FULL on `exercises` so admin CMS edits live-sync to the iOS app; covered by commit `403d1ee`. |


**Paired code changes**: `Fit33/ExerciseLibraryView.swift` realtime subscription wiring.

## Atomic Challenge Accept/Decline (C-6, Sprint 5 — 2026-04-20)


| #   | File                                 | Status   | What it does                                                                                                                                                                                                                                                                                                                                                         |
| --- | ------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 56  | `20260420_atomic_challenge_rpcs.sql` | 🆕 Ready | New `accept_challenge(uuid)` + `decline_challenge(uuid)` RPCs with `SELECT ... FOR UPDATE` on the caller's `challenge_participants` row. Idempotent (returns `already_accepted` / `already_declined` instead of raising) and structured (`jsonb` with `status`/`all_accepted`/`cancelled`). Legacy `respond_to_challenge` left in place for older installed clients. |


**Paired code changes**: `Fit33/ChallengeService.respondToChallenge` now calls the new RPCs and interprets the structured status, skipping heavy post-accept sync on idempotent replays.

## Index Verification (DB-5, Sprint 5 — 2026-04-20)


| #   | File                           | Status                         | What it does                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ------------------------------ | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| —   | `verify_query_performance.sql` | 🛠️ Verifier (not a migration) | Read-only audit that lists every expected index on hot tables (challenges, friendships, shared_workouts, daily rollups, push log, moderation, workout_history, etc.) and marks each row `OK` or `MISSING`. Replaces the never-created `optimize_query_performance.sql` from MASTER_TODO. Run ad hoc or in CI: `psql $SUPABASE_DB_URL -f supabase/verify_query_performance.sql |


## Daily Quest Program + Focus Aware (2026-04-21)


| #   | File                                                | Status              | What it does                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | --------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 57  | `20260421_daily_quests_program_and_focus_aware.sql` | 🟡 Superseded by 58 | Added two optional params to `get_daily_quests` (`p_suggested_split TEXT`, `p_fatigued_regions TEXT[]`) and used them to force slot 1 to a region-specific quest (`lower_body_workout`, `upper_body_workout`, …). Replaced because the region-specific slot penalized users who did an excellent workout in a different split — "Leg Day" stayed 0/1 when the user did arms + back. Migration 58 keeps the copy but rewires the quest_key. |


## Daily Quest Workout Slot Flexibility (2026-04-22)


| #   | File                                                | Status              | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | --------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 58  | `20260422_daily_quest_workout_slot_flexibility.sql` | 🟡 Superseded by 59 | Same 18-arg signature as migration 57, but slot 1's `quest_key` for non-program users is now ALWAYS `complete_workout` (or `complete_program_day` for program users). The personalized "Leg Day — legs are fresh" title/description is preserved as guidance, but any completed workout ticks the quest off so users aren't penalized for doing a substantive workout in a different split. Also removes `upper_body_workout` / `lower_body_workout` from the eligible pool so the same single-region penalty can't creep in via other slots. The underlying `p_suggested_split` / `p_fatigued_regions` contract is unchanged. |


**Run order**: 58 (standalone, idempotent). Drops both the 16-arg and 18-arg overloads before recreating, so safe to re-run.
**Paired code changes**: `Fit33/DailyQuestViews.swift` adds the new "any workout counts" marker to `isPersonalizedWorkoutDescription`. `Fit33/AdvancedIntelligenceService.swift` (`getPersonalizedRecommendation`) now sources its workout suggestion from `WorkoutSuggestionEngine.suggestForTodayAsync()` — the same engine that feeds `DailyQuestService.gatherUserContext`'s `suggestedSplit` — so the dashboard welcome widget and the daily quest card agree on today's split (no more "Lower Abs need work" vs "Leg Day" contradiction).

## Daily Quest Smart Hierarchy (2026-04-23)


| #   | File                                       | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | ------------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 59  | `20260423_daily_quest_smart_hierarchy.sql` | 🆕 Ready | Three new intelligence layers on top of migration 58. (1) **Redundancy matrix**: when slot 1 is a workout quest (`complete_workout` / `complete_program_day` / `complete_2_workouts`), slots 2/3 no longer pick `active_minutes_30`, `burn_300_calories`, `workout_30_min`, `exercise_sets_{15,25}`, `beat_volume_pr`, `stretch_session`, `maintain_streak`, `league_3_workouts`, or `early_bird_workout` — finishing any workout subsumes all of them. Same applies when slot 1 is a step-goal quest (10K steps already covers active-minutes + burned-cal goals). (2) **Challenge override**: new optional `p_active_challenge_types TEXT[]` param mirrors the user's active 1v1/group `challenge_type` set. For each active type the matching quest is force-placed in slot 2/3 even if the redundancy matrix would have stripped it — priority order `active_minutes > calories > hydrate > protein > workout_streak > lift`. Step challenges keep flowing through `p_active_step_challenge_target`. (3) **Category diversity**: if the final 3 quests span fewer than 2 distinct categories (`workout / nutrition / steps / tracking / social`), slot 3 is swapped for the highest-priority unrepresented category via a ladder that depends on what slot 1 is (workout anchor → prefer nutrition; steps anchor → prefer nutrition; other → prefer nutrition). Net effect: instead of "Crush a Workout + Get Moving + Burn 300 cal" (all redundant), users see "Crush a Workout + Log 3 Meals + 8 Glasses of Water" — or when a challenge is live, "Crush a Workout + Catch KC in Steps + Beat KC's Active Minutes". Signature is 19 args; drops 16/18-arg overloads before recreating. |


**Run order**: 59 (standalone, idempotent). Safe to re-run.

| 60 | `20260424_exercises_manually_updated.sql` | 🆕 Ready | Adds `exercises.manually_updated BOOLEAN NOT NULL DEFAULT FALSE` + `manually_updated_at TIMESTAMPTZ` columns and a partial index `idx_exercises_manually_updated_at` (where `manually_updated = TRUE`). Powers the new "Updated" checkbox on the admin CMS exercise detail page — the API auto-stamps `manually_updated = TRUE` + `manually_updated_at = now()` on every `update_exercise` save (or clears both when the admin explicitly unchecks the box). No RLS change: the existing `exercises` policies already cover admin writes via service role. |

**Run order**: 60 (standalone, idempotent). Safe to re-run.
**Paired code changes**: `admin-cms/src/app/api/admin/route.ts` — `get_exercises` select now includes both columns; `update_exercise` allowed-fields list includes `manually_updated` and the handler auto-stamps the flag + timestamp on save (clears them when the admin unchecks). `admin-cms/src/app/exercises/[id]/page.tsx` — new top-right "Updated" checkbox auto-checks on any unsaved content edit, shows the saved date next to it, and is clickable (when no pending edits) to clear the manual-edit flag.
**Paired code changes**: `Fit33/DailyQuestService.swift` — `UserQuestContext.activeChallengeTypes` gathered from `ChallengeService.activeChallenges` + `activeGroupChallenges`, then encoded as `p_active_challenge_types` in the RPC payload. `Fit33/DailyQuestViews.swift` — `dynamicDescription` now rewrites step / active-minute / calorie / water / protein / streak quest copy with opponent deficit ("5K to catch KC" / "Lead KC by 200 cal" / "Tied with KC") via the new `firstActiveChallenge(matching:)` + `challengeDeficitCopy(...)` helpers. Every quest description string is capped at ~35 chars so the single-line card layout never truncates.

## Sprint 6 Security Hardening (2026-04-25)


| #   | File                                         | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | -------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 61  | `20260425_secure_definer_rpc_idor_fixes.sql` | 🆕 Ready | Inserts the canonical IDOR guard (`IF auth.uid() IS NOT NULL AND <user_param> <> auth.uid() THEN RAISE 42501`) into two remaining SECURITY DEFINER RPCs that accepted a `user_id` parameter but never verified the caller: `delete_user_account(user_id_to_delete UUID)` (P0 — any signed-in user could wipe any other account) and `get_user_achievements(p_user_id UUID DEFAULT NULL)` (P1 — any signed-in user could read any other user's progress). service_role / pg_cron (where `auth.uid() IS NULL`) keep full access so admin cleanup + backfills still work. Pattern mirrors 20260417_secure_get_friend_ids.sql. |


**Run order**: 61 (standalone, idempotent). Safe to re-run.

## Sprint 7 Security + Realtime Hygiene (2026-04-26)


| #   | File                                    | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | --------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 62  | `20260426_sprint7_security_hygiene.sql` | 🆕 Ready | (Q2-72) Extends the IDOR guard from migration 61 to the last three SECURITY DEFINER RPCs that accept a user_id: `get_daily_quests(TEXT, ...)`, `get_or_join_weekly_league(UUID)`, `get_league_leaderboard(UUID, UUID)`. Each historical overload is DROPped first per supabase-rules §12, then the latest signature is recreated with the guard inserted at the top of `BEGIN`. Non-auth callers (service_role / pg_cron contexts where `auth.uid() IS NULL`) keep full access so auto-placement, cleanup, and cron-driven refreshes still work. (Q2-73) Adds `private_challenge_chat` to the `supabase_realtime` publication — the table already had `REPLICA IDENTITY FULL` from `private_challenges_migration.sql` but was missed by `fix_private_realtime_publication.sql`, so moderation UPDATE events were silently dropped at the client. Idempotent via `EXCEPTION WHEN duplicate_object`. |


**Run order**: 62 (standalone, idempotent). Safe to re-run.
**Paired file edits (no DB migration needed)**:

- Q2-93: added `IF NOT EXISTS` to every `CREATE INDEX` in `20260328_content_moderation.sql`, `migrations/20260226_crash_reports.sql`, `20260325_version_changelogs.sql`.
- Q2-95: header comment on `20260324_adaptive_quest_selection.sql` marks it superseded by the 2026-04-25+ `get_daily_quests` rewrites so the `ROW(0,0,0)` anti-pattern on line 246 isn't mistaken for live code.
- Q2-96: `20260307_friend_activity_realtime.sql` replaced with a superseded-by-20260307_activity_feed_realtime.sql breadcrumb so re-running the duplicate ADD can't fail.

## Sprint 8 — Bug Intelligence Pipeline (2026-04-27)


| #   | File                                             | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | ------------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 70  | `20260504_bug_report_state_snapshot.sql`         | 🆕 Ready | (Q2-97 Phase 7 / **Cheat Code**) Runtime state snapshot on shake — the single highest-leverage accuracy lever for the bug intelligence pipeline. Most iOS silent-logic bugs (Dashboard weight widget shows 199 / Nutrition shows 190, per reports 40 + 66) are state-sync bugs whose smoking gun is two divergent `@Published` values. Claude is mediocre at reasoning over Swift source blind, world-class at diffing structured state. (1) Adds `bug_reports.state_snapshot JSONB NOT NULL DEFAULT '{}'::jsonb` — opaque dict keyed by service name (`WeightTrackingService`, `HydrationService`, ...) → shallow dict of scalar fields + `__captured_at` ISO timestamp. PII-scrubbed client-side by `BugReportSnapshotter.swift`. GIN index `idx_bug_reports_state_snapshot_gin` (jsonb_path_ops) for ad-hoc CMS queries. (2) DROP+CREATE `v_bug_reports_for_triage` (can't add columns via CREATE OR REPLACE) projecting the new column so `triage-shake-reports/index.ts` reads it without extra roundtrips. (3) Paired code: `Fit33/BugReportStateSnapshot.swift` (SnapshotProvider protocol + BugReportSnapshotter singleton, MainActor-isolated, weak-ref registry with key dedupe); `Fit33/BugReportStateSnapshot+Providers.swift` (conformances for `HydrationService` / `MealService` / `UserManager` / `PremiumManager` / `DailyQuestService` / `WorkoutManager` + central `registerAll()` bootstrap); `WeightTrackingService`'s conformance is in-file because `lastLoadTime` is `private`. `BugReportInsert` gains `state_snapshot: [String: Any]?` encoded via `AnyCodableJSON`. `BugReportView.onAppear` captures at shake time (before nav-churn invalidates state); the "Captured Context" card shows a green badge "Runtime state captured — N services snapshotted". Edge function `triage-shake-reports` now fetches `state_snapshot`, compacts empty service blocks via `compactStateSnapshot()`, and leads the Claude system prompt with a dedicated section explaining how to read divergences (todayLog vs recentLogs.first, pendingNavigationFlags drift, etc.) — confidence should rise to 0.85-0.90 when a concrete divergence is visible. Admin CMS `get_bug_intelligence_export` action projects `state_snapshot` through to the .md export; `formatExportAsMarkdown()` emits a `**Runtime state at shake (CHEAT CODE — read first):**` JSON block per shake bundle, and top-of-file instructions tell Cursor to read it FIRST. Service-role-only view; no RLS change; idempotent (ADD COLUMN IF NOT EXISTS + DROP+CREATE VIEW).                                                                            |
| 69  | `20260503_rage_shake_user_context.sql`           | 🆕 Ready | (Q2-97 Phase 6.1) Enrich rage-shake triage with reporter identity so Claude can correlate bugs to the specific user. Single change: `DROP VIEW public.v_bug_reports_for_triage; CREATE VIEW ...` (must DROP+CREATE — CREATE OR REPLACE VIEW can't add columns in Postgres) re-projecting `user_email` alongside the existing `user_id / user_name` so the `triage-shake-reports` edge function can (a) surface email to Claude for flagging support-unreachable users, (b) key a `user_profiles` lookup (experience_level / has_completed_onboarding / unit prefs / total_workouts / current_streak) that gets injected into the prompt as a `user_context` block per report. Paired edge-function changes (`supabase/functions/triage-shake-reports/index.ts`): new `UserContext` interface + `loadUserContexts()` helper that fetches `user_profiles.{id, email, name, created_at, has_completed_onboarding, experience_level, strength_level, fitness_goal, available_days, equipment, total_workouts, current_streak, is_verified, is_gold_verified, weight_unit, height_unit, distance_unit}` in one roundtrip keyed by `bug_report.user_id` (never fails triage on enrichment error — just logs). `SYSTEM_PROMPT` gets a `USER_CONTEXT` section telling Claude to (1) distinguish onboarding bugs from steady-state via `has_completed_onboarding`, (2) detect unit-mismatch bugs via `weight_unit`, (3) recognize empty-state bugs via `total_workouts=0`, (4) flag null email in `suggested_todo` when follow-up requires an in-app ping, and EXPLICITLY forbidding email / user_id echoing into `title / summary / code_diff` (those land in MASTER_TODO.md + GitHub PRs and must stay PII-free). Admin CMS `get_shake_inbox` action in `admin-cms/src/app/api/admin/route.ts` also projects `user_email` so the inbox can show the reporter's email. Service-role-only view; no RLS change; idempotent DROP+CREATE.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 68  | `20260502_rage_shake_v2.sql`                     | 🆕 Ready | (Q2-97 Phase 6) Rage-shake → Claude triage. Extends `bug_reports` into a proper bug-intelligence input so shake submissions appear alongside crash/log fingerprints in the admin `/bug-intelligence` UI. (1) New columns on `bug_reports`: `severity TEXT CHECK IN ('low','medium','high','critical') DEFAULT 'medium'` (user-picked — Claude may upgrade, never downgrade), `bug_category TEXT CHECK IN ('ui','data','performance','crash','auth','workout','nutrition','social','health','other')` nullable, `likely_source_files JSONB DEFAULT '[]'` (iOS client pre-computes from `ScreenCodeMap.swift` so Claude has the candidate set before any LLM call), `triage_status TEXT CHECK IN ('pending','analyzing','analyzed','failed','legacy') DEFAULT 'pending'` (rows older than 1h at migration time flip to `'legacy'` — they have no screen context), `triage_report_id UUID` links back to the `bug_intelligence_reports.id` Claude produced, `triaged_at TIMESTAMPTZ`, `triage_error TEXT`. Two partial indexes: `(created_at DESC) WHERE triage_status = 'pending'` for the worker's queue drain; `(triage_report_id) WHERE triage_report_id IS NOT NULL` for deep-links from the CMS bug card back to the raw shake submission. (2) `trigger_triage_shake_reports()` SECURITY DEFINER pg_cron wrapper — same `internal_config + x-cron-key` pattern as `trigger_triage_bugs()` (entry 64) but with a fast-path that `SELECT count(*) FROM bug_reports WHERE triage_status = 'pending'` and returns early when the queue is empty (avoids a wasted Anthropic cold-start every 10 min). (3) `mark_shake_report_triaged(bug_report_id, triage_report_id, status, error)` SECURITY DEFINER RPC — service-role-only (rejects when `auth.uid() IS NOT NULL`) — used by the edge function to flip `triage_status` → `'analyzing'` / `'analyzed'` / `'failed'` and link the Claude report back. (4) `v_bug_reports_for_triage` view (`security_invoker = on`) — projects the columns the edge function needs (description, expected_behavior, screenshot_base64 for Claude vision, screen_name, likely_source_files, severity, session_log, device metadata) filtered to `triage_status IN ('pending','analyzing')`. Service-role-only. (5) `bug_reports` added to `supabase_realtime` publication + `REPLICA IDENTITY FULL` (per supabase-rules #5) so the CMS can live-refresh the inbox. (6) pg_cron schedule `triage-shake-reports-run` at `:03 / :13 / :23 / :33 / :43 / :53` every hour (offset from `:00` hourly rollup and `:17` 4h triage-bugs). Idempotent unschedule/reschedule via `DO $$ BEGIN IF EXISTS ... END $$`. |
| 67  | `20260501_dsym_symbolication.sql`                | 🆕 Ready | (Q2-97 Phase 5) Server-side dSYM symbolication scaffolding — converts raw hex stack traces in `crash_reports.stack_trace` into real `file:line:function` lines that Claude triage can turn into PR-ready diffs. (1) New table `app_dsyms` keyed by `binary_uuid UUID PRIMARY KEY` (the main-image UUID Apple embeds in every build, NOT `app_version` — TestFlight rebuilds of the same version produce different UUIDs), columns `app_version / build_number / storage_path / size_bytes / sha256 / uploaded_at / uploaded_by`. RLS enabled, no policies → service-role only. Index `(app_version, build_number)` for human-friendly lookup. (2) Six new columns on `crash_reports`: `binary_uuid UUID`, `binary_slide TEXT` (ASLR slide as hex string, e.g. `0x104000000`, passed to `atos -l <slide>`), `symbolicated_stack_trace TEXT`, `symbolication_status TEXT NOT NULL DEFAULT 'pending'` with CHECK in `('pending','done','failed','legacy','no_dsym')`, `symbolicated_at TIMESTAMPTZ`, `symbolication_error TEXT`. Partial index `(occurred_at ASC) WHERE symbolication_status = 'pending'` for the worker's scan query. Join index `(binary_uuid) WHERE binary_uuid IS NOT NULL`. (3) One-shot backfill: all existing crashes (`binary_uuid IS NULL AND symbolication_status = 'pending'`) flip to `symbolication_status = 'legacy'` — per Phase 5 plan option A (no retroactive symbolication; the ~9,500 pre-Phase-5 crashes keep using Phase 3.1's error-message-tag fallback). (4) Storage bucket `dsyms` (private) — RLS-enabled, NO policies → service-role-only access (the Fit33 admin CMS enforces admin auth at the /api/admin Next.js layer, not via a DB `user_profiles.role` column which doesn't exist; both `scripts/upload_dsym.sh` and the symbolicate-crashes GitHub Actions runner use the service-role key which bypasses RLS). Same pattern as every `bug_intelligence_*` table in Phases 1–4. Sanity-check `DO $$` block at the end reports legacy / pending / dSYM row counts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 66  | `20260430_bug_intel_feedback_loop.sql`           | 🆕 Ready | (Q2-97 Phase 4) Knowledge feedback loop — closes the bug pipeline cycle when a BugIntel PR merges on GitHub. (1) Adds lifecycle columns to `bug_intelligence_reports`: `github_pr_number INTEGER`, `github_pr_merged_at TIMESTAMPTZ`, `feedback_applied_at TIMESTAMPTZ`, `docs_commit_sha TEXT` (last one reserved for Phase 4.1 docs auto-commit). Two partial indexes on `github_pr_number` and `github_pr_merged_at` (both `WHERE <col> IS NOT NULL`) for fast webhook lookups + time-to-fix sorts. (2) `v_bug_intelligence_metrics` view (`security_invoker = on` per supabase-rules.mdc) — per-agent leaderboard over last 30 days with: reports_total, unique_fingerprints, review_status breakdown (pending / approved / rejected / merged), severity breakdown (critical / high), avg_confidence, fix_rate_pct (merged / (merged+rejected+approved) excluding pending), median_time_to_fix_hours via `PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY github_pr_merged_at - first_seen_at)`, total_occurrences_affected, total_users_affected. Ordered by merged count DESC, total DESC. (3) Admin CMS Bug Intelligence page renders this as an agent leaderboard table under the existing overview row. No new tables — additive columns + view only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 65  | `20260429_bug_intelligence_crash_enrichment.sql` | 🆕 Ready | (Q2-97 Phase 3) Crash enrichment + log↔crash correlation so Phase 2 Claude triage produces PR-ready file_path + code_diff fields. (1) `crash_reports.bi_fingerprint` generated column `GENERATED ALWAYS AS (bug_intelligence_fingerprint(bug_intelligence_normalize(error_message), 'crash', NULLIF(error_domain, ''))) STORED` + btree index `idx_crash_reports_bi_fingerprint`. Joins `crash_reports` to `bug_intelligence_fingerprints` in O(1) — this is separate from `crash_reports.fingerprint` which is a client-side stack-trace hash. Works because Phase 1 `bug_intelligence_normalize()` and `bug_intelligence_fingerprint()` are both IMMUTABLE. (2) `fn_backfill_crash_session_snippet()` SECURITY DEFINER BEFORE INSERT trigger on `crash_reports` — when `session_id` is set and `session_log_snippet` is empty, stitches the last 100 `error` / `screen` / `tap` / `warning` / `api` entries from `dev_session_logs` for that session (bounded to `occurred_at + 5 minutes` window). Wrapped in `BEGIN / EXCEPTION / END` so a failed enrichment NEVER blocks the crash insert — the crash matters more than the snippet. Uses `bug_intelligence_ensure_array()` from Phase 1 to handle both JSONB-array and string-wrapped entries formats. (3) One-shot backfill: updates all crashes from the last 30 days with `session_id IS NOT NULL` and empty `session_log_snippet`. Bounded to 30 days because `dev_session_logs` rolls off. (4) Final sanity-check `DO $$` block reports counts (`total / with bi_fingerprint / with session snippet`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 64  | `20260428_bug_intelligence_reports.sql`          | 🆕 Ready | (Q2-97 Phase 2) Claude-driven bug triage + agent-owner routing. (1) New table `bug_intelligence_reports` — one row per triage run per fingerprint (`fingerprint` FK → `bug_intelligence_fingerprints`, `trigger_trend_id` FK → `bug_intelligence_trends`, `trigger_reason` CHECK in `('new', 'regression', 'scheduled', 'manual')`); stores the Claude output fields `agent_owner` (CHECK constrained to the exact roster in `ENGINEERING_TEAM.md`: `quality-performance` / `product-engineer` / `data-backend` / `infra-security` / `supabase-expert` / `design-system` / `design` / `fitness-expert` / `device-compatibility` / `support` / `unknown`), `invariant_violated`, `severity` (`critical` / `high` / `medium` / `low`), `confidence` (NUMERIC 0-1), `title`, `summary`, `file_path`, `code_diff`, `pain_point_candidate`, `suggested_todo`, plus PR / review lifecycle (`review_status` IN `pending` / `approved` / `rejected` / `merged` / `stale`, `pr_url`, `pr_branch`, `reviewed_by` FK → `user_profiles`, `reviewed_at`), full raw Claude response in `raw_response JSONB`, and `example_entry_ids` for auditability. RLS enabled, no policies → service-role only. (2) `trigger_triage_bugs()` SECURITY DEFINER wrapper — follows the canonical `internal_config` + `x-cron-key` pattern from `20260420_challenge_opponent_wake.sql` — reads `supabase_url` / `service_role_key` / `anon_key` from `internal_config` and `PERFORM net.http_post()` to `/functions/v1/triage-bugs`. (3) `cleanup_bug_intelligence_reports()` — 90-day prune for rejected / stale / merged reports (pending reports never pruned). (4) pg_cron schedules: `triage-bugs-run` every 4h at `:17` (offset from the Phase 1 hourly rollup at `:00` to avoid connection contention), `cleanup-bug-intelligence-reports` daily at `03:45 UTC`. (5) `v_bug_intelligence_inbox` view (`security_invoker = on`) — pending reports sorted by severity then confidence — is the admin CMS triage inbox surface. Indexes on `(fingerprint, created_at DESC)`, `(agent_owner, review_status, created_at DESC)`, `(severity, confidence DESC, created_at DESC)`, and `(review_status, created_at DESC)`.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 63  | `20260427_bug_intelligence.sql`                  | 🆕 Ready | (Q2-97 Phase 1) Foundations for the automated bug-trend / regression-detection pipeline. (1) Three new tables — `bug_intelligence_fingerprints` (deduplicated bug signatures across `dev_session_logs` errors + `crash_reports`, with admin-managed fields `status` / `assigned_agent` / `pain_point_id` / `resolution_pr_url` / `duplicate_of`), `bug_intelligence_daily_rollup` (per `fingerprint × day × screen × app_version` counts), and `bug_intelligence_trends` (append-only `'new'` / `'regression'` signals). All three are RLS-enabled, service-role-only. (2) Two immutable helpers: `bug_intelligence_normalize(msg)` masks `<id>` (long hex) and `<n>` (numbers) so `"user abc12345 failed"` and `"user def67890 failed"` fingerprint together (mirrors the JS normalizer in `admin-cms/src/app/dev-logs/page.tsx`), and `bug_intelligence_fingerprint(normalized, source, domain)` produces an `md5` hash. (3) Main worker `compute_daily_bug_rollup()` SECURITY DEFINER function — pg_cron-scheduled hourly at `0 * * * *` — scans the last 5 days of `dev_session_logs` (entries where `type='error'`) and `crash_reports`, UPSERTs fingerprints (preserving admin-managed fields), rewrites the rolling 5-day rollup, and appends trend signals (`new` when `first_seen_at >= today` AND `today_count >= 3`; `regression` when `today_count >= 3` AND `today_count > 3 × mean(days 1-4 ago)`). Skips fingerprints already marked `resolved` / `wont_fix` / `duplicate`. (4) Retention cleaner `cleanup_bug_intelligence_rollup()` — pg_cron daily at `30 3 * * *` — drops rollup rows > 30 days and trend rows > 90 days. (5) Forward-compat: adds `dev_logging_users.cohort TEXT NOT NULL DEFAULT 'beta'` column. (6) Cohort policy: one-shot INSERT enrolls **every existing `user_profiles` row** into `dev_logging_users` with `enabled = TRUE` (TestFlight-era — all current users OK to track per user confirmation 2026-04-27). (7) `auto_enroll_dev_logging()` SECURITY DEFINER trigger on `user_profiles INSERT` auto-adds future signups to cohort=beta. At GA, swap the trigger for a sampled variant or drop it. (8) Primes the pipeline with one initial call inside the migration so the rollup / trend tables aren't empty on deploy.                                                                                                                                                                                                                                                                                                                                                                  |


**Run order**: 63 → 64 → 65 → 66 → 67 → 68 → 69 → 70 (all standalone, idempotent). Safe to re-run. Preserves admin-managed fingerprint fields (`status` / `assigned_agent` / `pain_point_id` / `resolution_pr_url`) across runs.

## Sprint 9 — Wearable Personalization Platform (2026-05-06)

Unified Daily Readiness Score that blends WHOOP / Oura / Fitbit / HealthKit signals into a single 0-100 score + `red`/`yellow`/`green` band every downstream system reads. Drives recovery-aware auto-gen (Phase 1), nightly insight correlations (Phase 2), adaptive goal proposals (Phase 3), XP multipliers + wearable quests (Phase 4), and new challenge types `sleep_hours` / `readiness_average` / `strain_budget` (Phase 5). Cheat-code layer (Phase 6): wearable connection state lands in the `bug_reports.state_snapshot` JSON via `ReadinessService` SnapshotProvider.


| #                        | File                                                  | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------ | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 71                       | `20260506_daily_readiness_history.sql`                | 🆕 Ready                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | (Phase 0) Creates `daily_readiness_history` (user_id FK ON DELETE CASCADE, date, score 0-100, band red/yellow/green, primary_source whoop/oura/fitbit/healthkit/none, hrv_delta_pct, sleep_hours, sleep_debt_min, rhr_trend_bpm, strain_prev, signals JSONB) with UNIQUE(user_id, date) so iOS day-of upserts and the server nightly rollup converge. Full RLS (`auth.uid() = user_id` on SELECT/INSERT/UPDATE/DELETE). Indexes `(user_id, date DESC)` + `(date DESC, band)`. `updated_at` trigger. Also creates `v_user_readiness_30d` view with `security_invoker = on` (SUPABASE_AGENT §6) — 30-day window filtered by RLS — feeding the HealthInsightsView readiness history chart and the Dashboard Smart Welcome readiness slot. Paired Swift: `Fit33/DailyReadinessSnapshot.swift` (value type + `DailyReadinessRow` Codable), `Fit33/ReadinessService.swift` (@MainActor ObservableObject, blend priority WHOOP → Oura → Fitbit → HealthKit, derived formula `0.4 × sleep + 0.4 × rhr + 0.2 × active` for non-native-score sources, 60s throttle, UserDefaults cold-start cache, SnapshotProvider conformance), `Fit33/SupabaseManager+Readiness.swift` (upsert + fetch, `onConflict: "user_id,date"`). Hooked into `HealthDataService.syncAllHealthData(force:)` at the tail (Data invariant #4a: force propagates). Idempotent (IF NOT EXISTS / DROP POLICY IF EXISTS). Cascade delete via FK — no change to `delete_user_account()` needed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 75                       | `20260509b_get_daily_quests_has_wearable_body.sql`    | ✅ Deployed 2026-04-23                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | (Phase 4 body patch) Recreates `get_daily_quests` with the full canonical body from migration 62 + ONE new parameter `p_has_connected_wearable BOOLEAN DEFAULT FALSE` appended + ONE new eligibility branch `OR (qt.requires_context = 'has_wearable' AND p_has_connected_wearable)` in the `_eligible_quests` CTE. Preserves the IDOR guard, layer 1/2/3 smart-hierarchy (Data invariant #30), redundancy matrix, challenge override (Data invariant #31), category diversity sweep, step-challenge copy rewrites, free-user ad slot — all verbatim. Idempotent DROP sweep of every historical overload first (supabase-rules §12). Unblocks the `AppConfig.FeatureFlags.wearableQuests = true` flag — once applied, users with WHOOP / Oura / Fitbit / HealthKit connected will start seeing the 6 new templates (sleep_8h_wearable, recovery_above_67, hrv_above_baseline, rhr_in_healthy_range, respect_red_recovery, log_readiness_am) in the daily rotation, subject to the same layer 1/2/3 rules. **2026-04-23 Bug-Intel Cluster C addendum (fingerprint `bb8db6c1`):** INSERT INTO user_daily_quests gains `ON CONFLICT (user_id, quest_date, quest_key) DO NOTHING` to race-guard two concurrent Dashboard `.task` + tab-switch refetch calls both observing `v_quest_count = 0` and colliding on `user_daily_quests_user_id_quest_date_quest_key_key` (23505). Semantically correct (whichever racer lost reads the winner's rows on the SELECT that follows). No client change needed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 74                       | `20260509_wearable_quests.sql`                        | 🆕 Ready                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | (Phase 4 Wearable Quests) Seeds 6 new templates in `quest_templates` gated by `requires_context = 'has_wearable'`: `sleep_8h_wearable` (8h wearable sleep), `recovery_above_67` (green band), `hrv_above_baseline` (HRV > 28d baseline, `min_workouts=14`), `rhr_in_healthy_range`, `respect_red_recovery` (recovery workout on red day, `min_workouts=7`), `log_readiness_am` (open app within 2h of wake). All use `verification_type='auto'` and tick off from `ReadinessService.shared`. Idempotent `ON CONFLICT(quest_key) DO UPDATE`. Also DROPs every historical `get_daily_quests` overload (supabase-rules §12) in preparation for a follow-up migration `20260509b_get_daily_quests_has_wearable_body.sql` that appends the new `p_has_connected_wearable BOOLEAN DEFAULT FALSE` parameter to the canonical 19-arg body from migration 62 and extends the `context_allowed` predicate to treat `'has_wearable'` like the existing `'has_program'` / `'has_friends'` tags. Until the body migration is applied, templates with `requires_context='has_wearable'` are filtered out by the existing RPC (safe-by-default). Paired Swift: `DailyQuestService` call site already passes `p_has_connected_wearable` derived from WhoopService / OuraService / FitbitService / HealthKitService connection state gated by `AppConfig.FeatureFlags.wearableQuests`. XP multiplier code in `UserManager+ReadinessXP.swift` awards +20% on green days / +15% Smart Rest on red-day recovery workouts (feature-flagged via `readinessXpBonus`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 73                       | `20260508_adaptive_goal_proposals.sql`                | 🆕 Ready                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | (Phase 3 Adaptive Goals) Creates `user_adaptive_goal_proposals` (id UUID PK, user_id FK ON DELETE CASCADE, `metric` CHECK IN `calorie_goal` / `sleep_goal` / `step_goal` / `weight_pace` / `protein_goal`, `current_value NUMERIC`, `proposed_value NUMERIC NOT NULL`, `rationale TEXT NOT NULL`, `week_of DATE`, `accepted BOOLEAN` nullable = pending, `applied_at TIMESTAMPTZ`, `created_at`, `updated_at`) + UNIQUE(user_id, metric, week_of) so nightly upserts converge. Full RLS (auth.uid() = user_id). Indexes `(user_id, week_of DESC)` + partial `(user_id) WHERE accepted IS NULL`. `updated_at` trigger. View `v_user_pending_goal_proposals` (`security_invoker = on`) — current-week pending proposals only, ordered by created_at DESC. Never auto-applies — accept/decline is always an explicit user action via `AdaptiveGoalService.resolveProposal(id:accepted:)`. Paired Swift: `Fit33/AdaptiveGoalService.swift` (service + static compute helpers + SupabaseManager extension methods), `Fit33/AdaptiveGoalNudgeCard.swift` (Dashboard nudge card, feature-flagged via `AppConfig.FeatureFlags.adaptiveGoals`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 89                       | `20260523_fix_activity_recovery_check_constraint.sql` | 🆕 Ready                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | (Sprint 2026-04-24 perf sweep) Fixes the `23514` check-constraint violation that fires twice on EVERY session startup, spamming `[INTELLIGENCE] Failed to track activity: PostgrestError code "23514"` in dev_session_logs. Root cause: the original `activity_recovery_correlation_activity_level_check` was written when only 2-3 steps-based levels existed. `AdvancedIntelligenceService.trackActivityForRecovery` now writes any of 8 strings — 5 steps-based (`sedentary` / `light` / `normal` / `active` / `very_active`) or 3 WHOOP-recovery-based (`fatigued` / `moderate` / `well_rested`). Fix: idempotent DO block drops every existing activity_level-mentioning check on the table, then re-adds a canonical 8-value allowlist. Safe to re-run. Paired Swift: `Fit33/AdvancedIntelligenceService.swift` now routes 23514 through `NetworkErrorClassifier` at `.debug` so the violation is silent until the migration deploys. Post-deploy the classifier call essentially never fires; if it does it means a new string was added iOS-side without extending the DB check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 88                       | `20260522_fanout_backfill_fix.sql`                    | ✅ Deployed (verified 2026-04-26 via `verify_progress_drift_2026_04_26.sql` Section C — three `trg_fanout_*` triggers present; live drift check returned 0 cross-table mismatches after Abbie's 603 fanned out)                                                                                                                                                                                                                                                                                                                                                                                                                                                           | (2026-04-24 hotfix to #87) Re-runs the fanout backfill sweep on environments where the original `20260521_challenge_progress_fanout.sql` ran **before** its SET-clause patch. Original bug: the three `trg_fanout_*_challenge_progress` triggers are `AFTER INSERT OR UPDATE OF progress_value`, and Postgres only fires `UPDATE OF col` triggers when the column is actually in the UPDATE's SET list. The original backfill used `SET updated_at = updated_at`, which is a silent no-op for an `OF progress_value` trigger — private and community leaderboards drifted because the sweep never actually ran, which is why Paul showed 16k in the private leaderboard while the community "10K Steps Daily" leaderboard showed "—" on the same morning. Fix: `UPDATE … SET progress_value = progress_value` on the last 2 days of rows whose parent challenge is a shared cumulative type (`steps` / `active_minutes` / `calories`). The self-assignment puts `progress_value` in the SET list so the trigger fires; `GREATEST()` semantics guarantee no value decreases, so this is safe to run multiple times and order-independent. Safe no-op on environments that picked up the fixed version. Fixed-forward (#87 now ships with the correct SET clause; #88 is purely for already-deployed environments). Invariant captured in `.cursor/rules/supabase-rules.mdc` ("Backfill that must re-fire an AFTER UPDATE OF col trigger MUST self-assign that column"). Rollback: none needed — sweep is idempotent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 87                       | `20260521_challenge_progress_fanout.sql`              | ✅ Deployed (verified 2026-04-26 — `pg_trigger` shows `trg_fanout_challenge_progress` on `challenge_daily_progress`, `trg_fanout_private_challenge_progress` on `private_challenge_daily_progress`, `trg_fanout_community_challenge_progress` on `community_challenge_daily_progress`, all `AFTER INSERT OR UPDATE OF progress_value EXECUTE FUNCTION fanout_challenge_progress()`. The 2026-04-26 "Abbie 0 vs 603" report was therefore NOT a fanout failure — it was an upstream sync gap (Abbie's phone hadn't pushed any 1v1/private/community write yet); when her foreground/silent-push wake landed, this trigger correctly mirrored 603 across all three tables.) | (2026-04-24 follow-up to #86) Cross-table consistency for cumulative HealthKit-derived challenge metrics (`steps` / `active_minutes` / `calories`). Problem: a user's "steps today" is one number but Fit33 stores it in three independent tables (`challenge_daily_progress` / `private_challenge_daily_progress` / `community_challenge_daily_progress`), populated by three independent iOS services. If any one service bails early (cold `myChallenges.isEmpty`, throttled, not initialized), its table gets no write while others do — observed 2026-04-24 as "Paul shows 15,718 in private leaderboard but "—" in community leaderboard on the same morning". Fix: AFTER INSERT/UPDATE trigger `fanout_challenge_progress()` on all three daily-progress tables. When any row gets a write for `steps` / `active_minutes` / `calories`, the trigger UPSERTs the same `progress_value` into the OTHER two tables for every challenge of the same type the user is an active member of (`challenge_participants.status='accepted'` / `private_challenge_members.is_active=TRUE` / `community_challenge_participants.is_active=TRUE`). Recursion blocked via `pg_trigger_depth() > 1`; GREATEST() preserved so lower stale values never overwrite higher legit cumulative values. `protein` / `hydrate` intentionally NOT fanned out (per-challenge context, users may legitimately track differently). One-time sweep: `UPDATE … SET updated_at = updated_at` on last 2 days of eligible rows fires the trigger and converges the back-catalog without a separate backfill script. Paired Swift: each of the four `sync*To*Challenges` entry points (private, community, 1v1, group) now auto-fetches its challenge list if empty before the `guard !.isEmpty` so cold HealthKit observer wakes don't silently no-op while sibling services push. Extending to a new cumulative challenge type requires editing the `v_challenge_type IN (...)` list in the trigger + the backfill UPDATE WHERE clauses. Rollback: `DROP TRIGGER trg_fanout_{challenge,private_challenge,community_challenge}_progress` + `DROP FUNCTION fanout_challenge_progress()`. Invariant: DATA_BACKEND_AGENT #48 + #49.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 86                       | `20260520_challenge_daily_reset_caller_tz.sql`        | 🆕 Ready                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | (Bug-Intel fingerprint `6be18e3a` — medium, data-backend, 2026-04-24) Root-cause fix for "private challenge step count shows 15,718 at 7:27am instead of resetting at midnight". Reporter in ET saw opponent Paul's yesterday-evening step total rendered as today's count with a green target-hit checkmark at 7:27am local. Cause: `get_active_challenges`, `get_active_group_challenges`, `log_challenge_progress`, and `log_group_challenge_progress` keyed `today_date` off `COALESCE(gc.creator_timezone, p_timezone, 'UTC')`. Whenever `creator_timezone` resolved to `'UTC'` (legacy rows or explicit), the effective day boundary became 00:00 UTC = 8pm ET the previous evening; late-evening ET writes landed with `progress_date = <next day UTC>` and were replayed by every ET/PT morning reader until UTC rolled again at 8pm ET. Private + community read/write RPCs already used caller `p_timezone` directly (from `20260330_privacy_photo_all_rpcs.sql` / `fix_daily_progress_reset.sql`), so only the 1v1/group quartet needed fixing. (1) Recreates the four RPCs with `today_date := (NOW() AT TIME ZONE COALESCE(NULLIF(p_timezone, ''), 'UTC'))::DATE` — each user's "today" reset now follows their own device midnight. (2) `days_elapsed` / `days_remaining` still read `creator_timezone` so the "2d left" counter is stable across participants regardless of where they're travelling. (3) One-time cleanup: `DELETE FROM {challenge_daily_progress, community_challenge_daily_progress, private_challenge_daily_progress}` rows whose `progress_date = today(America/New_York)` AND `updated_at < today(America/New_York) midnight` — same window used in `fix_daily_progress_reset` cleanup; poisoned rows get repopulated on next foreground HealthKit/meal sync. (4) `UPDATE challenge_participants.total_progress = SUM(remaining daily)` keeps aggregates honest post-delete. `BEGIN;/COMMIT;` wrapped; fully idempotent. Paired Swift: no change required — `Fit33/ChallengeService.swift` `logProgress` / `logGroupProgress` already send `p_timezone: TimeZone.current.identifier`. Rollback: restore pre-fix function bodies from `supabase/fix_daily_progress_reset.sql` (log_challenge_progress, log_group_challenge_progress) + `supabase/20260330_privacy_photo_all_rpcs.sql` (get_active_challenges, get_active_group_challenges). Resolves bug-intel fingerprint `6be18e3a`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 85                       | `20260519_bug_intel_phase11_sweep.sql`                | 🆕 Ready                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | (Q2-97 Phase 11 — 2026-04-24 "fix everything" uncategorized drain) Responds to the baseline snapshot read after Phase 10: 163 fingerprints / 575 occurrences in cluster_code='uncategorized' (largest bucket by far). Root-cause analysis showed three families: (1) CrashReporter self-upload loop — the uploader's own catch block logged at `.warning` which wrote another dev_session_logs row → next rollup fingerprinted it → Claude triaged it → bug_reports row → infinite amplifier (fingerprints `ae9a0f23` / `59b8ae55` / `4bfd609cd`); (2) generic iOS transient-NSURLError strings (`The network connection was lost` / `The Internet connection appears to be offline` / `The request timed out` / `cancelled` / `notConnected`) surfacing from ~15 different call sites that haven't been migrated to `NetworkErrorClassifier` (tracked in the 247-item `scripts/classifier_lint.py` backlog); (3) Swift-originated P0001 "Not authenticated" that the Phase 10 noise filter missed because it only matched the Postgres-formatted `code: Optional("P0001")` prefix. (A) Seeds 7 new `bug_intel_noise_filter` rows (tier=hard): `crashreporter_upload_offline`, `nsurl_transient_offline`, `nsurl_transient_connection_lost`, `nsurl_transient_timeout`, `nsurl_transient_cancelled`, `strava_not_connected`, `social_group_not_authenticated_swift`, plus a soft-tier `social_private_invites_timeout` (kept for rate-spike visibility). (B) Backfill UPDATE flips matching fingerprints to `status='resolved'` / `auto_resolved_reason='noise_filter_expanded'` and merges their pending reports with a paper-trail note. (C) Adds auto-stale clause: any fingerprint in `status='triaged'` that hasn't re-occurred in 7+ days flips to `status='resolved'` / `auto_resolved_reason='triaged_stale'` (separate reason from noise so the improvement tracker can distinguish "we filtered it" from "we stopped seeing it"). (D) Rewrites `snapshot_bug_intel_baseline()` with 4 new cluster codes (`H_crashreporter_self` / `I_widget` / `J_wearable_sync` / `K_launch_crash`) — the uncategorized bucket becomes a real "unknown" signal instead of a 163-row catch-all. (E) Captures `after_sweep_2026_04_24_phase11` baseline so `bug_intel_improvement_tracker` shows the drain delta. Paired Swift work: `Fit33/CrashReportingService.swift` passes `transientLevel: .debug` on the upload-failed catch (kills new additions to family 1), denylist extended with the new patterns, `Fit33/AppPerformanceSystem.swift` watchdog logs downgraded to `.debug` on release. Paired tooling: `supabase/functions/bug-intel-rpc-smoke/index.ts` FIXTURES[] populated with 11 signature-smoke tests, `.github/workflows/bug-intel-rpc-smoke.yml` runs them every 15 minutes, `.github/workflows/bug-intel-release-gate.yml` blocks release tags with open CRITICAL fingerprints. Rollback: `DELETE FROM bug_intel_noise_filter WHERE created_by = '20260519'`. |
| 84                       | `20260518_fix_sync_profile_weight_uuid_cast.sql`      | 🆕 Ready                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | (Q2-97 Phase 10b — 2026-04-24 weight-log 42883 root-cause drop) The trigger hunt in migration 83 identified `public.sync_profile_weight()` (AFTER INSERT on `weight_logs`) as the root cause of the persistent 42883 `operator does not exist: uuid = text` cluster C (fingerprints `ff6ae8d5` / `222e0fc1` / `95b0b27b`, 25+ occurrences, which kept regenerating even after `20260512_weight_logs_audit.sql` canonicalized the RLS policies). The function body was `UPDATE user_profiles SET … WHERE id = NEW.user_id::text` — both sides are `uuid`, but the `::text` cast forced PostgreSQL to resolve `uuid = text` which doesn't exist, so every single `INSERT INTO weight_logs` from the iOS client failed post-insert. `CREATE OR REPLACE FUNCTION` preserves the existing `pg_trigger` row so there's no mid-transaction window where writes bypass the trigger. Trailing `DO $$` blocks (a) dry-run the fixed predicate against a synthetic uuid so the planner type-checks the new body at deploy time, and (b) verify `trigger_sync_profile_weight` on `weight_logs` is still wired to the fixed function. Final UPDATE auto-resolves the matching C_uuid fingerprints in `bug_intelligence_fingerprints` with `auto_resolved_reason='silent_fix'` and `fixed_in_build='1.38 (51)'` so they stop appearing in the next `mode=new` export. Swift-side: no change required — `Fit33/WeightTrackingService.swift` has been sending UUID-typed `user_id` values the whole time; the bug was entirely server-side. Rollback: restore the pre-fix function body (kept in the migration header comment as the "Original (buggy) function body" for reference).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 83                       | `20260517_bug_intel_noise_filter_expand.sql`          | 🆕 Ready (fixed 2026-04-24 — hit 42703 on first run because `bug_intelligence_reports.review_notes` didn't exist; now added in same migration)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | (Q2-97 Phase 10 — 2026-04-24 Bug-Intel report-quality fix) Responds to the 2026-04-24T11:10 Cursor export where the same 6 structural fingerprints (`2b8eafe6` / `bc323225` / `01d7cac0` / `c74effaf` / `32ce4388` / `f45c991a`) dominated 15 of 74 reports because the main-thread watchdog (Fit33/AppPerformanceSystem.swift line 1105) emits `AppLogger.warning("🚨🚨🚨 [WATCHDOG] MAIN THREAD FROZEN!")` on every freeze and Phase 9's rollup picks `type=warning` entries up as bugs — a performance *signal* became a fake CRITICAL every 5-minute rollup. (1) Adds 9 new `bug_intel_noise_filter` rows (tier=hard) matching: watchdog main-thread-frozen / unblocked-critical / UI-unresponsive / tab-freeze; Cloudflare 502/503 gateway HTML; P0001 `"Not authenticated"` transient from SECURITY DEFINER RPCs during tab-switch auth propagation; PGRST203 overload errors (already fixed in 20260513 — noise from build 1.37 users who haven't updated); plus a soft-tier HealthKit Apple-Watch save-timeout filter. (2) Adds `bug_intelligence_fingerprints.auto_resolved_reason` column so future auto-resolutions distinguish `silent_fix` from `noise_filter_expanded` from `legacy_build_drained` without spelunking into noise_filter JOINs. (3) Backfill UPDATE: any existing fingerprint whose `sample_message` matches the new regex patterns is flipped to status='resolved' + auto_resolved_reason='noise_filter_expanded', and its pending bug_intelligence_reports are merged with a trail note — cleans up the back-catalog so the next `mode=new` export doesn't replay them. (4) Fail-loud trigger hunt: iterates `pg_trigger` on `weight_logs` / `weight_goals`, `pg_get_functiondef`s each, and RAISE WARNINGs for any function body containing `auth.jwt()->>'sub'` / `::text` / `current_setting('request.jwt.claims')` — hunting the residual 42883 `uuid = text` root cause (fingerprint `95b0b27b` re-seen on 1.38 build 51 despite 20260512's policy canonicalization). (5) Trailing DO $$ sanity block RAISE EXCEPTIONs if fewer than 9 filter rows land. Paired CMS change: `admin-cms/src/app/bug-intelligence/page.tsx` `formatExportAsMarkdown` now de-dupes bundles by `structural_fingerprint` so the 15 watchdog variant titles collapse to a single canonical report with "Also triaged as:" aliases. Rollback: `DELETE FROM bug_intel_noise_filter WHERE created_by = '20260517'`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 82                       | `20260516_bug_intel_structural_fingerprint.sql`       | ✅ Deployed 2026-04-23                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | (Q2-97 Phase 9 "Cheat Code" — 2026-04-23 Bug-Intel cheat-code sweep) Structural fingerprinting + server-side noise filter + regression detection + auto-resolve for the bug-intel pipeline. Rationale: Phase 1's `bug_intelligence_fingerprint(md5(normalized_message, source, domain))` splinters a single root cause across many fingerprints as soon as the error string drifts one token (`"cancelled"` vs `"The request was cancelled"` vs `"NSURLErrorDomain error -999"`), and the rollup had no concept of "this is a transient iOS-lifecycle error the classifier didn't fully suppress — drop it at the DB." (1) Adds 11 columns to `bug_intelligence_fingerprints`: `op TEXT` (lifted from `dev_session_logs.extra->>'x_op'` / `crash_reports.metadata->>'op'`), `error_class TEXT` (e.g. `pg:42883` / `http:503` / `nsurl:-999` / `auth:expired` — computed by `bug_intel_classify_error()`), `pg_code TEXT`, `http_status INT`, `nsurl_code INT` (extracted from `NSURLError(Domain)? error -?N` via regex), `endpoint TEXT`, `is_classified BOOLEAN` (did the client route through `NetworkErrorClassifier.log(...)` — `false` = invariant 25a violation at the call site), `structural_fingerprint TEXT` (MD5 of `source                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 81                       | `20260515_friend_activity_feed_cardio_check.sql`      | ✅ Deployed 2026-04-23                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | (Q2-97 Bug-Intel Sweep Cluster B follow-up — 2026-04-23 export) Fixes 23514 CHECK constraint violation on `friend_activity_feed_activity_type_check` (fingerprint `b66f6c07`). The `post_cardio_activity` RPC from `20260418_post_cardio_activity.sql` inserts `activity_type = 'cardio_completed'`, but the original CHECK from `20260307_friend_activity_feed.sql` only allowed `'workout_completed' / 'streak_milestone' / 'level_up' / 'achievement_unlocked' / 'challenge_won'` — so every cardio activity post has silently failed since that RPC shipped. Idempotent `DROP CONSTRAINT IF EXISTS` + `ADD CONSTRAINT` with the full allowed set including `'cardio_completed'`. Guarded by `to_regclass` so staging without the table is a no-op. Trailing fail-loud `DO $$` audit (SUPABASE_AGENT invariant #29): `RAISE EXCEPTION` if the constraint is missing any required value after commit. Paired Swift: `Fit33/FriendActivityFeedView.swift` `postCardioActivity` adds `isAuthenticated` guard + routes catch through `NetworkErrorClassifier.log` with `op: "social.post_cardio_activity"` + `endpoint: "rpc/post_cardio_activity"` + `startedAt` / `userId` so residual 23514 / PGRST errors fingerprint with pg_code. Also adds `Op.postCardioActivity` to `Fit33/PerformanceSignposts.swift`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 80                       | `20260514_performance_metrics.sql`                    | 🆕 Ready                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | (Q2-97 Bug-Intel Sweep Cluster I — 2026-04-23 measurement layer) Establishes the before/after measurement surface for the 8-cluster bug-intel sweep. (1) New `performance_metrics` table (user_id FK ON DELETE CASCADE, op TEXT, elapsed_ms INT, started_at TIMESTAMPTZ, endpoint TEXT, extra JSONB, app_version TEXT, os_version TEXT) — drained every 30s from the client's in-memory `PerformanceSignposts.pendingMetrics` queue by `Fit33/PerformanceMetricsUploader.swift`. Indexes `(op, started_at DESC)` + `(user_id, op, started_at DESC)` + GIN on `extra`. RLS: insert/select where `user_id = auth.uid() OR user_id IS NULL`. (2) `performance_metrics_daily` view (`security_invoker = on`) — daily `PERCENTILE_CONT` 0.5/0.95/0.99 of `elapsed_ms` grouped by `(day, op)` — feeds the admin CMS Improvement Tracker tile p50/p95 column. (3) `bug_intel_baseline_snapshots` table (label TEXT, captured_at, cluster_code, open_fingerprint_count, total_occurrences, note) + `bug_intel_baseline_service_only` RLS (service_role only). (4) `snapshot_bug_intel_baseline(p_label TEXT)` SECURITY DEFINER RPC — classifies every *open* `bug_intelligence_fingerprints` row into 8 cluster buckets by title-keyword match (`A_main_thread` / `B_rls` / `C_uuid` / `D_startup_timeout` / `E_crashes` / `F_overloads` / `G_social` + `uncategorized`), inserts the aggregated counts, and returns them — callable from `admin-cms` action `snapshot_bug_intel_baseline` (service-role only, promoted to WRITE_ACTIONS). (5) `bug_intel_improvement_tracker` view (`security_invoker = on`) — window-function join of the latest two snapshots per cluster exposing `open_delta` + `open_delta_pct` + `occurrence_delta`, consumed by the admin CMS `get_bug_intel_improvement_tracker` action. (6) Primes itself with one `snapshot_bug_intel_baseline('before_sweep_2026_04_23')` call inside the migration so the post-sweep checkpoint has something to diff against. Paired code: `Fit33/PerformanceSignposts.swift` (in-memory queue + canonical op names), `Fit33/PerformanceMetricsUploader.swift` (30s drain + background flush + 404-soft-fail), `admin-cms/src/app/bug-intelligence/page.tsx` (Improvement Tracker tile + "Capture checkpoint" button), `admin-cms/src/app/api/admin/route.ts` (`get_bug_intel_improvement_tracker` read + `snapshot_bug_intel_baseline` write).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 79                       | `20260513_drop_post_workout_activity_overloads.sql`   | 🆕 Ready                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | (Q2-97 Bug-Intel Sweep Cluster F — 2026-04-23) Resolves `PGRST202` "Could not choose the best candidate function" error surfacing from `FriendActivityFeedView.postWorkoutActivity`. Two historical overloads coexisted in prod — the 7-arg signature from `20260307_friend_activity_feed.sql` and the 8-arg signature from `20260330_activity_feed_exercises.sql` — so PostgREST couldn't pick a candidate when the iOS client called with 8 args (`p_exercises_json` was optional DEFAULT `'[]'`). Explicit `DROP FUNCTION IF EXISTS` for both signatures (supabase-rules §12), then `CREATE OR REPLACE` the canonical 8-arg version with `SECURITY DEFINER` + `SET search_path = public` + explicit `'Not authenticated'` RAISE if `auth.uid() IS NULL`, storing `{workout_name, duration_seconds, exercise_count, total_sets, xp_earned, muscle_groups, exercises}` in `friend_activity_feed.metadata` JSONB. Final `DO $$` sanity block `RAISE EXCEPTION` if `pg_proc` still returns ≠ 1 overload after commit. Paired Swift: `Fit33/FriendActivityFeedView.swift` adds `isAuthenticated` guard at call site + routes catch through `NetworkErrorClassifier.log` with `op: "social.post_workout_activity"` so any residual 42501 / PGRST errors fingerprint cleanly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 78                       | `20260512_weight_logs_audit.sql`                      | ✅ Deployed 2026-04-23                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | (Q2-97 Bug-Intel Sweep Cluster C — 2026-04-23) Addresses the 42883 `operator does not exist: uuid = text` errors on `weight_logs` + `weight_goals` surfaced by Bug Intelligence. Fail-loud `DO $$` schema check — `RAISE EXCEPTION` if `weight_logs.user_id` or `weight_goals.user_id` is not `uuid` (Data invariant #24 guard at the DB level). DROPs any legacy policies then `CREATE POLICY` canonical SELECT/INSERT/UPDATE/DELETE with `auth.uid() = user_id` on both tables. Trailing `DO $$` block iterates `pg_trigger` and `RAISE NOTICE`s for every user-defined trigger on either table so an operator can audit remaining `uuid = text` anti-patterns without blocking the migration. Paired Swift: `Fit33/WeightTrackingService.swift` adds `isAuthenticated` guard on `setWeightGoal` (previously missing — top source of Cluster C 42501 noise) + threads `DiagnosticContext` (op/endpoint/startedAt/userId) through every catch via `NetworkErrorClassifier.log` so `pg_code` lands in the bug_intelligence fingerprint payload.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 77                       | `20260511_health_rls_audit.sql`                       | 🆕 Ready                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | (Q2-97 Bug-Intel Sweep Cluster B — 2026-04-23) Canonicalizes RLS on the three health-data tables generating the 42501 noise cluster in Bug Intelligence (`cardio_workouts`, `daily_activity_summary`, `sleep_logs`). `ALTER TABLE … ENABLE ROW LEVEL SECURITY`, idempotent `DROP POLICY IF EXISTS` sweep, then `CREATE POLICY` for SELECT/INSERT/UPDATE/DELETE using `auth.uid() = user_id`. The `sleep_logs` section is wrapped in a `to_regclass` existence guard so staging / fresh-clone envs without that table are a no-op instead of failing. Trailing `DO $$` block iterates each table, reports "not present" for any that don't exist, and `RAISE WARNING`s if RLS is disabled on a present table. Paired Swift: `Fit33/HealthDataService.swift` adds `isAuthenticated` guards on `saveHealthKitWorkout`, `saveSleepFromHealthKit`, `saveDailyActivity`; `Fit33/SupabaseManager.swift` guards `saveStepData` + `batchSaveStepData`. Each guarded early-return logs a `DiagnosticContext(op, endpoint)` at `.info` so the skip is observable without generating fingerprints.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 76                       | `20260510_bug_intel_export_watermark.sql`             | 🆕 Ready                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | (Q2-97 Phase 8 — 2026-04-23 follow-up) Stops the Cursor-handoff `.md` from growing unbounded. Adds `bug_intelligence_reports.last_exported_at TIMESTAMPTZ + export_count INTEGER DEFAULT 0` and `bug_intelligence_fingerprints.last_exported_at TIMESTAMPTZ`. Partial index `idx_bug_reports_unexported` on `(created_at DESC) WHERE last_exported_at IS NULL AND review_status IN ('pending','approved')` makes the hot-path "NEW since last export" query O(unexported-rows). Adds service-role RPC `mark_bug_reports_exported(UUID[])` that stamps both tables atomically — called by the admin CMS `get_bug_intelligence_export` action after a successful `mode='new'` export. Also adds `cleanup_stale_bug_reports()` SECURITY DEFINER — scheduled via pg_cron at 04:15 UTC — which deletes `bug_intelligence_reports` rows in terminal review states (`merged` / `rejected` / `stale`) older than 14 days, then drops orphaned terminal fingerprints (`resolved` / `wont_fix` / `duplicate`) with zero remaining reports. Non-destructive: GitHub `pr_url` / `resolution_pr_url` on the already-archived report rows preserve fix history externally. Paired code: admin-cms `get_bug_intelligence_export` gains a `mode: 'new'                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 72                       | `20260507_personalized_insights_wearable.sql`         | 🆕 Ready                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | (Phase 2a) Server-side correlation pipeline. Extends `user_personalized_insights` with `correlation_type TEXT`, `r_squared NUMERIC 0-1`, `p_value NUMERIC 0-1`, `sample_size INT`, `wearable_source TEXT` (CHECK in whoop/oura/fitbit/healthkit/derived). Idempotent unique index `(user_id, insight_key)` so nightly upserts don't duplicate cards. Creates `v_user_wearable_insights` view (`security_invoker = on`) — 14-day window, significance gate `sample_size >= 10 AND p_value <= 0.15` (legacy rule-based insights pass through when `correlation_type IS NULL`). New pg_cron wrapper `trigger_compute_readiness_insights()` SECURITY DEFINER reads secrets from `internal_config` and POSTs to `/functions/v1/compute-readiness-insights` with `x-cron-key` (same pattern as `trigger_challenge_opponent_wake()`). Schedules `compute-readiness-insights-nightly` at 03:30 UTC. Paired code: `supabase/functions/compute-readiness-insights/index.ts` — Deno edge function, service-role-only (`x-cron-key` JWT verified against `SUPABASE_PROJECT_REF`), Spearman rank correlation with Fisher-Z p-value approximation, 60-day lookback, 500-user hard cap, computes 5 correlations per user (sleep_hours_vs_pr_rate, hrv_delta_vs_pr_success, readiness_band_vs_adherence, strain_avg_vs_rhr_trend, protein_x_sleep_vs_recovery), upserts onto `(user_id, insight_key)`. Added to Edge Function Auth Registry in `INFRA_SECURITY_AGENT.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Paired code changes**: |                                                       |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |


- Phase 2: `supabase/functions/triage-bugs/index.ts` + `admin-cms/src/app/bug-intelligence/page.tsx` + new admin API actions. Requires `ANTHROPIC_API_KEY` secret + `internal_config` rows.
- Phase 3: updates `supabase/functions/triage-bugs/index.ts` to use `crash_reports.bi_fingerprint` for O(1) crash lookup, include `stack_trace` / `breadcrumbs` / `session_log_snippet` in Claude input, cross-correlate log ↔ crash via the `bug_intelligence_fingerprint()` RPC.
- Phase 3.1: prompt tweak in `triage-bugs` — unsymbolicated hex stacks are NOT used to guess file names; falls back to error-message tags + current_screen + session_log_snippet with confidence ≤0.70.
- Phase 4 (this sprint): `supabase/functions/github-pr-webhook/index.ts` — HMAC-verified GitHub PR webhook handler that flips `review_status` → `merged` and `fingerprint.status` → `resolved` when a BugIntel PR lands. `admin-cms/src/app/bug-intelligence/page.tsx` gains an `AgentLeaderboard` row reading from `v_bug_intelligence_metrics`. `admin-cms/src/app/api/admin/route.ts` gains `get_bug_intelligence_metrics` action (read-only). Requires `GITHUB_WEBHOOK_SECRET` secret + manual webhook registration on `jreedy33/Fit33` (gh api command documented in the edge function header).
- Phase 5 (this sprint): server-side dSYM symbolication — `20260501_dsym_symbolication.sql` (this index entry 67) is the DB scaffolding only. Paired code: `Fit33/CrashReportingService.swift` populates `binary_uuid` + `binary_slide` (Phase 5.1); `scripts/upload_dsym.sh` + `docs/DSYM_UPLOAD.md` manual Archive post-action uploader (Phase 5.4); `.github/workflows/symbolicate-crashes.yml` scheduled macOS runner that calls `atos` and writes `symbolicated_stack_trace` back (Phase 5.5); `supabase/functions/triage-bugs/index.ts` prefers `symbolicated_stack_trace` over the raw `stack_trace` when present (Phase 5.6). See `docs/history/PHASE_5_SYMBOLICATION_PLAN.md` for the full design + success criteria.
- Phase 6 (this sprint): rage-shake v2 — `20260502_rage_shake_v2.sql` (entry 68) is the DB side. Paired code: iOS client `Fit33/ScreenCodeMap.swift` (static map of `SessionLogManager.Screen.displayName` → candidate `Fit33/*.swift` files, capped at 4), `Fit33/BugReportService.swift` + `Fit33/BugReportView.swift` auto-detect screen via `SessionLogManager.getCurrentScreenInfo()` and `ScreenCodeMap.filesForScreen(...)`, the rage-shake form gets a new "Captured Context" section showing the user which screen + files Claude will review and a severity segmented picker (`BugSeverity` enum — low / medium / high / critical), and `ManualBugReportView` (Settings → Report Bug) picks up severity too. Edge function `supabase/functions/triage-shake-reports/index.ts` — mirrors `triage-bugs/index.ts` shape but (a) accepts multimodal Claude input (sends `screenshot_base64` as an image block so Claude literally sees the bug), (b) respects user-picked severity as a FLOOR (never downgrade), (c) caps confidence at 0.9 (no stack trace = no 0.95+ claims), (d) writes a synthetic fingerprint with `source='shake'` per bug_report id (stable SHA-256 truncated to 32 hex chars to fit the `bug_intelligence_fingerprints.fingerprint` FK) so shake reports appear in the same `/bug-intelligence` list as crash/log without polluting trend signals. Admin CMS: new `trigger_shake_triage` + `get_shake_inbox` actions in `admin-cms/src/app/api/admin/route.ts`, `admin-cms/src/app/bug-intelligence/page.tsx` gains a source tab row (All / Crash / Log / Bug (Shake)), a 5th overview card showing the Crash / Log / Bug split, a "Triage shakes" header button, and a distinct emerald `SOURCE_COLORS.shake = '#059669'` pill for shake rows. `get_bug_intelligence_overview` now returns `fingerprints_by_source` (crash / log / shake counts).

## Sprint 10 — Bug Intelligence Phase 12 (2026-05-24 → 2026-05-31)

Hardening sweep for the bug-intel pipeline triggered by the 2026-04-25 export
("are we making progress or going in circles?"). Five tiers of upgrades that
move the pipeline from "Claude triages each bug in isolation" to a
self-improving system with cross-bug memory, observable severity calibration,
auto-resolution of transient noise, structural call-site capture, and
migration→fingerprint linking. All eight migrations are idempotent + safe to
re-run.


| #   | File                                                  | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ----------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 90  | `20260524_private_challenge_deadlock_retry.sql`       | 🆕 Ready | (2026-04-24 fingerprints `3d7ac331` + `23ac8780`) Fixes 40P01 deadlock in `log_private_challenge_progress`. Two concurrent invocations (foreground `logProgress` racing the `BackgroundChallengeSyncService` fanout) acquired row-level locks in opposite orders on `private_challenge_daily_progress` then `private_challenge_members`. Fix: deterministic lock order — `SELECT … FOR UPDATE` on the `private_challenge_members` row FIRST, then UPSERT into the daily-progress table. Plus a 3-attempt retry loop catching SQLSTATE `40P01` with 50ms/100ms backoff so any residual race recovers in-RPC instead of bubbling to the iOS classifier. `CREATE OR REPLACE` preserves the existing privilege grants. Auto-resolves the two fingerprints via the `Resolves:` header convention (#94 below).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 91  | `20260525_mark_bug_intel_resolved_2026_04_25.sql`     | 🆕 Ready | (2026-04-25 sweep) Manual fingerprint cleanup pass after the 2026-04-25T13:16 export — flips 21 of 24 reports to `status='resolved' / auto_resolved_reason='silent_fix'` (HealthKit RLS, USDAFoodService classifier, FoodDatabaseService, ExerciseMappingService, P0001 tier=hard noise, UserBehaviorLearningEngine, HydrationService, FriendRankingService, DailyQuestService, SupabaseManager comprehensive sync, StravaService) and merges their pending reports with a paper-trail note. Leaves 3 open: 2 SIGKILL/SIGSEGV crashes blocked on dSYM symbolication + 1 challenge widget refresh UX bug. Idempotent — re-running is a no-op once status is terminal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 92  | `20260526_bug_intel_callsite_capture.sql`             | 🆕 Ready | (Phase 12 — Tier 0 #1 — call-site capture) Adds `bug_intelligence_fingerprints.last_seen_file / last_seen_function / last_seen_line` columns + a separate `bug_intel_patch_callsite_columns()` SECURITY DEFINER function (kept out of the 700-line `compute_daily_bug_rollup` body to minimize regression risk). The patch reads `dev_session_logs.entries[].extra.{x_file, x_line, x_function}` (now populated by `Fit33/Logger.swift` for every `AppLogger.error/.warning/.critical` post-Phase 12) and `crash_reports.additional_context.{file, function, line}`, populates the columns, then schedules itself nightly at 04:05 UTC. CMS `bug-intelligence/page.tsx` markdown export now leads each report with `**Call site**: <file>:<line> in <function>` so Claude / Cursor walk in already pointing at the offending source line. Final `DO $$` block reports populated-row counts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 93  | `20260527_bug_intel_single_incident_autoresolver.sql` | 🆕 Ready | (Phase 12 — Tier 1 #1 — auto-drain transient noise) Second auto-resolve pass alongside the existing `compute_daily_bug_rollup` 5-day silent-fix pass. New `bug_intel_resolve_single_incident_transients()` SECURITY DEFINER nightly function (00:15 UTC) flips fingerprints with: `error_class IN ('cancelled','offline','timeout','gateway','auth_expired')` + `occurrence_count = 1` + `unique_user_count = 1` + `last_seen_at < now() - 14 days` + non-terminal status + unassigned to `status='resolved' / auto_resolved_reason='transient_single_incident'`, and merges their pending reports with a paper-trail note. Drained ~9 of the 24 fingerprints in the 2026-04-25 export immediately. **Schema fix (2026-04-25)**: `bug_intelligence_reports` doesn't have an `updated_at` column — only `reviewed_at` — so the merge UPDATE only stamps `reviewed_at`. Comment in body documents this constraint for #94.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 94  | `20260528_bug_intel_severity_score.sql`               | 🆕 Ready | (Phase 12 — Tier 2 #2 — severity rebalance) Replaces raw `occurrence_count` ranking with a stored `severity_score` column. Original IMMUTABLE formula: `occurrence_count × SQRT(unique_user_count) × screen_visibility × build_freshness × source_severity × regression_amplifier × error_class_amplifier`. Visibility multiplier: 1.5× for dashboard/onboarding/active-workout screens. Build-freshness: 1.0 for current build, 0.5 one build behind, 0.2 ≥2 builds behind (drains noise from un-updated TestFlight users). Source: 2.0× for crashes vs 1.0× for log errors. Regression: 3.0× when `regressed_after_fix=TRUE`. Error-class amp: 1.5× for structural errors (`pg:42883` / `pg:42703`) vs 0.5× for transients. `bug_intel_recompute_severity()` SECURITY DEFINER scheduled nightly 04:00 UTC. CMS list / export now order by `severity_score DESC`. Superseded by #97 (which moves the multipliers into a tunable table).                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 95  | `20260529_bug_intel_migration_resolves_link.sql`      | 🆕 Ready | (Phase 12 — Tier 2 #1 — migration→fingerprint link) Convention + RPC for auto-resolving fingerprints when their fix migration deploys. Convention: every bug-fix migration's header may include `-- Resolves: <fingerprint-md5> <short-justification>` lines. RPC `mark_fingerprints_resolved_by_migration(migration_id TEXT, fingerprints TEXT[])` (service-role only) flips each to `status='resolved' / resolution_pr_url=<migration_id> / auto_resolved_reason='migration_resolved:<id>'` and merges pending reports. Backfill scans `supabase/*.sql` files for `Resolves:` lines and applies them. From now on, any new migration that includes a `Resolves:` line auto-resolves its fingerprints on deploy without a separate cleanup pass. Same `reviewed_at`-only update as #93 (no `updated_at` column on `bug_intelligence_reports`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 96  | `20260530_bug_intel_resolved_history.sql`             | 🆕 Ready | (Phase 12 — Tier 5 #1 — cross-fingerprint memory / "agents getting smarter") Append-only `bug_intel_resolved_history` table snapshots key signals (structural_fingerprint, op, error_class, pg_code, http_status, endpoint, last_seen_file/function/line, title, summary, agent_owner, severity_score, resolution_pr_url, auto_resolved_reason, resolved_status, resolved_at) on every `AFTER UPDATE OF status` transition into a terminal state (`resolved` / `wont_fix` / `duplicate`). Trigger `bug_intel_snapshot_resolution()` SECURITY DEFINER selects the latest non-rejected report for context and is idempotent on `(fingerprint, resolved_at)`. RPC `bug_intel_find_similar_resolutions(structural_fingerprint, op, error_class, exclude_fingerprint, limit)` returns top-N past fixes ranked by `match_strength` (3 = exact structural, 2 = op+class, 1 = op-only OR class-only) excluding auto-drains (`transient_single_incident` / `noise_filter_expanded`). 90-day backfill at install. Edge function `supabase/functions/triage-bugs/index.ts` calls the RPC per fingerprint and feeds top-3 `similar_past_fixes` into Claude with a new `# USING SIMILAR_PAST_FIXES` SYSTEM_PROMPT section telling Claude to reuse owner/file_path on `match_strength=3` and cite past PRs in summaries. Admin CMS markdown export gains a `### 🧠 Similar past fixes (N)` block per report. RLS service-role-only. |
| 97  | `20260531_bug_intel_severity_weights.sql`             | 🆕 Ready | (Phase 12 — Tier 5 #2 — tunable + observable severity scoring) Moves the IMMUTABLE multipliers from #94 into a `bug_intel_severity_weights` table (`key`, `value`, `source`, `fitted_from`, `notes`, `updated_at`) seeded with the Phase 12 numbers as `source='seed'`. Helper `bug_intel_get_severity_weight(key, default)` STABLE/SECURITY DEFINER. `bug_intel_compute_severity_score(...)` is dropped + re-created as STABLE (was IMMUTABLE) reading every multiplier through the helper — picks up weight edits on the next `bug_intel_recompute_severity()` tick. New `bug_intel_calibration_report` table + nightly `bug_intel_calibrate_severity_weights(window_days INT)` SECURITY DEFINER (00:30 UTC, 60-day window) writes a JSONB report correlating each knob's "knob ON" set with real-fix vs auto-drained vs rejected outcomes, plus per-`severity_score` bucket fix rates. **Auto-tuning is OFF**; observability-first for the first 2 months — weights are edited by hand via `UPDATE bug_intel_severity_weights SET value = … WHERE key = …`. Admin CMS adds `get_bug_intel_severity_calibration` action + a `SeverityCalibrationPill` in the `/bug-intelligence` header showing "Weights tuned/seeded · cal. Xd ago" that expands to the current weights table + latest report summary. RLS service-role-only.                                                                                      |


| 109 | `20260609_mark_bug_intel_resolved_2026_04_25_audit.sql` | ✅ Deployed | (2026-04-25 17:58 audit cleanup, 48 fingerprints) Second-pass cleanup that runs alongside #91 (which targeted the 13:16 export). Flips 39 of the 48 audit fingerprints to terminal status with bucketed `auto_resolved_reason`: (A) `silent_fix` for 3 fingerprints already fixed by deployed migrations 75/81 (daily quest dup-key + friend_activity_feed cardio_completed); (B) `migration_pending_deploy` for 11 fingerprints fixed by 🆕 Ready migrations on disk (#90 deadlock retry, #106 get_daily_quests v3, #108 v_user_quest_personalization_summary, 20260608 workout intelligence RLS audit + UserManager.swift UUID fix); (C) `noise_filter_expanded` for 4 fingerprints already covered by #83/#85 noise filters (watchdog, Cloudflare 502, JWT expired transient); (D) `transient_single_incident` for 20 fingerprints (auth/password rate limits + 16 single-occurrence transient NSURLErrors from Reports 33–48 — would auto-drain via #93 on day-14 anyway, marked here to clear the audit immediately); (E) `duplicate` for 1 log-variant of an open parent (Report 21 dup of Report 4). Leaves 8 fingerprints OPEN as genuine unresolved bugs: Report 1+7 edge function 403 (infra-security, ContactsService classifier-bypass + auth review), Report 4 duplicate sign-up UX (product-engineer), Report 14 SIGKILL (quality-performance, blocked on dSYM Phase 5.4–5.6), Reports 15+16+17 fresh-install Core Data crash sequence (data-backend, ExerciseNameCache plist guard), Report 25 AutoGen stretches (product-engineer), Report 29 challenge widget refresh (product-engineer; same UX bug left open by #91). Also adds `Resolves:` directives (per #95 convention) to migrations 20260524 / 20260515 / 20260509b / 20260605 / 20260607 / 20260608 so the migration→fingerprint backfill auto-links them on deploy. Idempotent — re-running is a no-op once status is terminal. |

**Run order**: 90 → 91 → 92 → 93 → 94 → 95 → 96 → 97 → 109 → 110 → 111 (all standalone, idempotent). Safe to re-run. Run #109 after #95 so the `Resolves:` directives added to deployed migrations (75/81) get backfilled before this cleanup pass overwrites their `auto_resolved_reason`. Run #110 after #109 so the Phase-2 code-fix bucket lands on top of the silent-fix sweep. Run #111 last to drain the long-tail backlog that #109 and #110 don't cover.

| 110 | `20260610_mark_audit_code_fixes_resolved.sql` | ✅ Deployed | (2026-04-25 audit Phase 2 — code-fix close-out, 6 fingerprints) Companion to #109 covering the audit reports that needed actual iOS code changes. Bucket A (`code_fix`) flips Reports 1+7 (`b242269c` / `65f3c668`, notify-contacts-user-joined 403) and Report 4 (`00bd6a62`, "User already registered" loop) to `resolved` after the matching client-side fix lands in `Fit33/NetworkErrorClassifier.swift` (new `.expectedUserState` classification + patterns: "user already registered", "user already exists", "email already registered", "forbidden: new_user_id must match caller" → log at `.debug`, bypass crash_reports + bug_intelligence_fingerprints) and `Fit33/ContactsService.swift::notifyExistingUsersOfNewJoin` (added `isAuthenticated` guard + replaced bare `AppLogger.error` — QP invariant 25a violation — with `NetworkErrorClassifier.log(transientLevel: .debug)`; daily `check_pending_join_notifications` cron catches up missed contacts). Bucket B (`silent_fix`) closes Reports 15+16+17 (`1a0c9263` / `3896c649` / `1dea54e7`, fresh-install Core Data plist crash sequence — same session FA2353DF on 2026-04-23) — already shipped in build 1.37 (50) when `Fit33/ExerciseNameCache.saveToDisk` got the `NSLock` snapshot + safe-snapshot filter (lines 68–99) that drops invalid entries instead of crashing CFPreferences. Leaves 3 of the original 48 audit reports OPEN: Report 14 (`e955904c`, SIGKILL) blocked on dSYM Phase 5.4–5.6, Report 25 (`ed249878`, AutoGen stretches) needs product+data repro, Report 29 (`eeaa35d1`, challenge widget refresh race) needs Combine subscription instrumentation. Idempotent. |
| 111 | `20260611_bug_intel_inbox_zero_drain.sql` | ✅ Deployed | (2026-04-25 audit Phase 3 — inbox-zero close-out, ~~125 fingerprints) Codifies the live SQL-editor sweep that took `bug_intelligence_fingerprints` from 89 open (new + triaged) down to 2 open after #109 + #110 + bundles A/B/C deployed. Six `LEFT(fingerprint, 8) IN (...)` UPDATE buckets — prefix-matching is required because the audit doc carried truncated 8-char hashes that did not align with the synthetic 32-char hashes #109/#110 used: (A) `code_fix` (5 fps) — backfills #110's intent against the real DB hashes for Reports 1+7+4; (B) `silent_fix` (3 fps) — Reports 15+16+17 ExerciseNameCache plist guard already in build 1.37 (50); (C) `migration_pending_deploy` (~~28 fps) — equipment_proficiency 42501 RLS / workout_context 42501 RLS / collaborative_workout_data 42501 RLS / get_daily_quests PGRST202+PGRST303 / daily-quest 23505 dup-key — all fixed by bundle A `20260608_workout_intelligence_rls_audit` + bundle C `20260605_get_daily_quests_personalized` + already-deployed #75; (D) `noise_filter_expanded` (10 fps) — CrashReporter self-upload RLS recursion + JWT expired/auth-expired clusters already covered by `.authExpired` classifier route + #83/#85 noise filters; (E) `transient_single_incident` (~90 fps) — single-incident NSURLError -999/-1001/-1005 + NSSQLite Code=13 from old builds (41/43/46/48/50). NetworkErrorClassifier already routes them as `.transientNetwork` on current builds; #93 nightly auto-drain would clear them at day-14 anyway; drained immediately to clear the inbox; (F) `wont_fix` (1 fp) — Report 14 (`e955904c`, MetricKit SIGKILL) blocked on dSYM Phase 5.4–5.6. Final `DO $$` verify block raises NOTICE with terminal/open counts (expects 180+ terminal, 2 open). Idempotent — every UPDATE has `AND status NOT IN ('resolved','duplicate','wont_fix')` short-circuit. |

## Challenge Realtime Fix (2026-04-25)


| #   | File                                                   | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ------------------------------------------------------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 112 | `20260612_fix_challenge_realtime_replica_identity.sql` | 🆕 Ready | (User report 2026-04-25 — "all of my challenge 1v1 and group challenge doesn't appear to be updating in real time"; supersedes the un-dated `fix_challenge_realtime_replica_identity.sql` that was sitting in `supabase/` without an index entry, also resolves Phase-2 audit Report 29 fingerprint `eeaa35d1` "challenge widget against paul did not update after workout completion") Sets `REPLICA IDENTITY FULL` on `challenge_daily_progress`, `challenge_participants`, and `group_challenges`. `challenge_type_migration.sql` (#12) added `challenge_daily_progress` to the `supabase_realtime` publication but never set the replica identity. Without it, Supabase Realtime UPDATE events only carry the primary key — `user_id`, `progress_value`, `challenge_id` are all NULL in the payload, so `RealtimeService.handleDailyProgressChange` hits its `record["user_id"]` guard and silently drops every opponent progress event. The migration also re-asserts publication membership via `IF NOT EXISTS` `pg_publication_tables` checks (idempotent) and emits a final `DO $$` verify block that NOTICEs the `relreplident` setting per table so the deployer can confirm `FULL ✅`. **Companion iOS fixes (same commit)**: `Fit33/RealtimeService.swift::handleDailyProgressChange` no longer early-returns on `isOwnUpdate` — the prior "we already have the data locally" perf optimization was incorrect because the dashboard widget reads from `ChallengeService.activeChallenges` which only refreshes on `get_active_challenges` RPC, and a workout cascade may write to multiple challenges before the per-call `fetchActiveChallenges` debouncer can catch up. `Fit33/ChallengeService.fetchMinInterval` lowered from 5.0s → 1.0s; the prior 5s gate dropped every post-write refresh after the first when a single workout updated multiple challenges (steps + active_minutes + workout_streak), leaving 1v1/group widgets visibly stale. RequestCoalescer already prevents truly concurrent fetches and RealtimeService still has its own 3s `challengeFetchThrottleInterval` so the lower client gate doesn't introduce thrash. Idempotent. |


**Run order**: 112 (standalone, idempotent). Deploy via Supabase SQL editor; verifier `DO $$` block prints `relreplident` per table so confirm all three show `FULL ✅`.

## Audit 2026-04-25 19:07 close-out (13 fingerprints)


| #   | File                                                | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | --------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 113 | `20260613_mark_audit_2026_04_25_19_07_resolved.sql` | 🆕 Ready | (2026-04-25 19:07 export close-out, 13 fingerprints) Companion to #109/#110/#111. Closes the 13 audit fingerprints by bucket: (A) `migration_resolved` for R1+R3+R5+R7 — server fix already deployed (#106 get_daily_quests v3 + #108 v_user_quest_personalization_summary view); (B) `code_fix:auth_uid_alignment+classifier_routing` for R2 (workout_context RLS) + R6 (collaborative_workout_data RLS) — paired iOS change in `Fit33/WorkoutManager.swift` (recordWorkoutContext / recordExercisePerformance / collaborative-engine call / updateEquipmentProficiency now source `user_id` from `SupabaseManager.shared.currentUser?.id` (auth.uid()) instead of Core Data `User.id`, eliminating 42501 violations for legacy accounts whose Core Data UUID predates the UserManager auth-alignment fix; catch blocks routed through `NetworkErrorClassifier.log(...)`) + `Fit33/CollaborativeLearningEngine.recordWorkoutCompletion` classifier routing; (C) `code_fix` for R8 (autogen stretches — `Fit33/ProgressiveExerciseUnlockService` adds `clearCache()` on sign-out + eager `recomputeProfile(context:)` after `syncAllDataFromCloud`, plus a Core Data workout-count fallback in `shouldRestrictToFoundational`/`varietyPercentage` getters so the autogen path doesn't gate returning users to foundational/stretch-only after a fresh sign-in) + R9+R10+R13 (auth signup — `Fit33/NewOnboardingView+Auth.swift::handleAuth` typed + generic catch blocks routed through `NetworkErrorClassifier.log(transientLevel: .debug)` so "User already registered" / rate-limit lands at `.debug` instead of generating a fingerprint per attempt; classifier extended with `rate limit` / `too many requests` / HTTP 429 patterns → `.transientNetwork`) + R5+R7 client-side leg (`Fit33/QuestInsightsView.loadInsights` treats PGRST205 / "schema cache" / "relation does not exist" as soft empty-state); (D) `migration_pending_deploy:20260524` for R4+R12 — server fix in #90 (deadlock retry RPC) is on disk but awaiting deploy; (E) `migration_pending_deploy:20260612` for R11 — REPLICA IDENTITY FULL fix in #112 awaiting deploy. Final `DO $$` verify block RAISE NOTICE 'expected 13 resolved'. Idempotent — every UPDATE has `AND status <> 'resolved'` short-circuit. |


**Run order**: 113 (standalone, idempotent). Deploy after #90 + #112 are in prod so the `migration_pending_deploy:`* reasons reflect actual gating; otherwise re-run #113 once the prerequisite migrations land to flip the bucket label to a permanent paper-trail.

## Audit 2026-04-25 20:34 new-export close-out (20 fingerprints)


| #   | File                                                         | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | ------------------------------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 115 | `20260615_fix_nudge_group_challenge_member_column_drift.sql` | 🆕 Ready | (User report 2026-04-25 — bug-intel `d8fe113b` "Missing column in group_challenge_nudges table", PG `42703` from `nudge_group_challenge_member` RPC at `ChallengeService.swift:1563`) Recreates the `nudge_group_challenge_member` RPC body to remove the dropped `group_challenge_id` column from its `INSERT` — `20260326_fix_nudge_column_mismatch.sql` dropped that column in favour of `challenge_id`, but `20260325_fix_nudge_overload.sql` still wrote both columns, so the live RPC has been raising `42703` ever since #20260326 deployed. Migration uses the canonical drop-all-overloads `pg_proc` loop (Supabase invariant 12) before `CREATE OR REPLACE` so any stray previous-arity definitions are removed first, and ends with a `DO $$` audit block that verifies exactly one definition exists post-write (Supabase invariant 28). `Resolves: d8fe113b2a14b80603ca156e2ee0c990` directive ships in the header so the migration→fingerprint backfill (#95) auto-links it. Idempotent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 116 | `20260616_mark_new_2026_04_25_20_34_resolved.sql`            | 🆕 Ready | (2026-04-25 20:34 NEW-mode export close-out, 20 fingerprints) Companion to #109/#110/#111/#113 covering the second 04-25 export. Most fingerprints recurred from the 19:07 audit — the bug-intel pipeline re-opens fingerprints when fresh occurrences arrive, and the 1.38 cohort still on the older binary (pre-auth-alignment / pre-classifier-routing) is the dominant source. Buckets: (A) `migration_resolved:20260605+20260607` for R4/R5/R8/R9 (deployed daily-quests v3 + quest-insights view); (B) `code_fix:auth_uid_alignment+classifier_routing` for R1/R2/R14 (workout_context + collaborative_workout_data RLS); (C) `migration_resolved:20260615` for R10 (NEW — nudge column drift fixed by #115); (D) `code_fix` for R3+R11+R13+R17 (auth signup + rate-limit classifier paths) AND four NEW iOS-side fixes shipped this sprint: R7 (`Fit33/Fit33App.swift` chains `ReadinessService.shared.recompute(force: true)` after `WhoopService.syncAllData` + `OuraService.syncAllData` complete in `scenePhase == .active` — fixes the launch race where the parallel `HealthDataService.sync` recomputed against stale @Published WHOOP state because the WHOOP `isSyncing` guard short-circuited the re-fetch inside that pipeline; runtime snapshot from the bug confirmed `whoopLastSyncAgeSec=29s` but `lastComputedAgeSec=38s`); R15 (`Fit33/AutoWorkoutPreviewView.swift` renders `ReadinessAdjustmentBanner` at the top of `exerciseListView` so the user sees WHY their selected muscle group was overridden when WHOOP recovery hit red — banner self-gates on `hasWearableSignal` + `readinessAdaptiveAutoGen` flag, zero-impact for non-wearable users; the override behaviour itself is correct per FITNESS_EXPERT_AGENT invariant 23 — surfacing rationale closes the UX gap that user reported as "no matter when, when i do autogen it shows corresponding strength excercises not stretch"); R12+R17+R18+R19+R20 (`Fit33/CrashReportingService.swift` denylist tightening — modern `NSURLErrorDomain Code=-999` format alongside legacy `error -999`; explicit `rate limit exceeded` / `rate_limit` / `too many requests` / `HTTP 429` patterns for auth rate-limits; explicit `HTTP 503` + Strava upstream HTML title `strava is temporarily unavailable` patterns since Strava's 503 body is HTML, not the canonical `503 Service Unavailable` string the prior filter caught); (E) `migration_pending_deploy:20260524` for R6+R16 — server fix in #90 still pending. Final `DO $$` verify block RAISE NOTICE 'expected 20 resolved'. Idempotent — every UPDATE has `AND status <> 'resolved'` short-circuit. |


**Run order**: 115 → 116 (115 first because 116 references its `migration_resolved:20260615` bucket; both standalone, idempotent). Deploy 115 alongside any other pending bug-fix migrations; 116 can re-run safely as the cohort transitions off 1.38.

## Daily Quests integer-overflow hotfix (2026-04-25)


| #   | File                                             | Status   | What it does                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 117 | `20260617_fix_get_daily_quests_int_overflow.sql` | 🆕 Ready | (User report 2026-04-25 — `DailyQuestService.swift:1310` "[QUESTS] Failed to fetch: integer out of range" SQLSTATE `22003` from `get_daily_quests`) Body of #106 (`20260605_get_daily_quests_personalized.sql`) builds `v_day_seed := abs(hashtext(v_user_id::TEXT |


**Run order**: 117 (standalone, idempotent). Re-runs are no-ops because the function body matches; deploys cleanly even if #106 hasn't been re-applied since the last edit. Verify post-deploy by calling `SELECT get_daily_quests('557F52B7-B17B-49D7-B46E-F2F887EEA96B'::TEXT, 'America/New_York', ...)` — should return without raising 22003.

## Wake Diagnostics RPC (2026-04-26 — Opponent Wake Observability)


| #   | File                                | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ----------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 118 | `20260618_wake_diagnostics_rpc.sql` | 🆕 Ready | (User-reported "Abbie shows 0 steps mid-day" 2026-04-26 — needed observability into the silent-push wake pipeline without bypassing Infra invariant #14, which makes `silent_push_wake_log` service-role-only.) Adds `get_my_wake_diagnostics(p_lookback_hours INT DEFAULT 24)` — `SECURITY DEFINER`, pinned to `auth.uid()` (Data invariant #7), `search_path = public`. Returns 14 columns per related user: `(user_id, display_name, username, profile_photo_url, relationship, last_wake_at, last_wake_trigger, wake_count_24h, has_valid_token, token_count, apns_environment, token_prefix, last_progress_at, last_progress_value)`. "Related" = caller themselves + every counterpart in caller's active 1v1 / group challenges (via `challenge_participants` + `group_challenges.status='active'`) + active private challenges (via `private_challenge_members` + `private_challenges.end_date IS NULL OR >= CURRENT_DATE`). Dedup via `DISTINCT ON (uid)` ordered by relationship priority (`self` > `1v1_or_group` > `private`). Lookback clamped to [1, 168] hours so callers can't ask for unbounded history. `**token_prefix` returns first 12 chars only** — never the full device token. Consumed exclusively by the DEBUG-only `Fit33/WakeDiagnosticsView.swift` (dev menu tab "Wakes") which lets us answer "did Abbie's phone get a wake push in the last 24h?" / "is her token valid?" / "when was her last `challenge_daily_progress` write?" without poking around the SQL editor. `GRANT EXECUTE` to `authenticated` + `service_role`. Idempotent (`DROP FUNCTION IF EXISTS` before `CREATE OR REPLACE`). See INFRA_SECURITY_AGENT.md invariants #15a + #17a for the related Swift-side lite-wake-path change shipped same day. |


**Run order**: 118 (standalone, idempotent). No dependencies beyond `silent_push_wake_log` (#90 / `20260420_challenge_opponent_wake.sql`) and `user_push_tokens` (long-existing). Verify post-deploy with `SELECT * FROM get_my_wake_diagnostics(24);` from any authenticated session — returns ≥1 row (caller self) when the user has any active challenges or push tokens. To revert, `DROP FUNCTION IF EXISTS get_my_wake_diagnostics(INT);`.

## Audit 2026-04-26 17:32 new-export close-out (15 fingerprints)


| #   | File                                                 | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ---------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 119 | `20260618_log_challenge_progress_deadlock_retry.sql` | 🆕 Ready | (Bug-intel `281382daef47…` 2026-04-26 — `[op=challenge.progress_sync ep=rpc/log_challenge_progress pg=40P01]` deadlock from `ChallengeService.swift:2111`, with cascade fingerprints `bb8962ac` / `0d1100de` "Failed to sync progress for ⚔️ 👣 …" surfacing the same root) Hardens the 1v1/group `log_challenge_progress` RPC against the same 40P01 deadlock pattern that #90 (`20260524_private_challenge_deadlock_retry.sql`) fixed for the private RPC — that earlier sweep missed this sibling. Two changes: (1) **deterministic lock order** — `PERFORM 1 FROM challenge_participants WHERE … FOR UPDATE` BEFORE upserting `challenge_daily_progress`, so concurrent callers (foreground `logProgress` racing the `BackgroundChallengeSyncService` HealthKit observer fan-out) for the same `(challenge_id, user_id)` queue on the participants row instead of interleaving lock acquisition across two tables; (2) **in-function 40P01 retry** — body wrapped in `LOOP … BEGIN … EXCEPTION WHEN deadlock_detected THEN PERFORM pg_sleep(0.05 + random()*0.1)` with `v_max_attempts = 3`, identical recipe to #90. All other semantics preserved verbatim from `20260520_challenge_daily_reset_caller_tz.sql`: 7-arg signature, caller-tz `progress_date`, participant-membership pre-check, GREATEST() merge gated on `p_allow_decrease`, full streak/total/days_completed/best_streak recompute. Canonical drop-all-overloads `pg_proc` loop pattern (Supabase invariant 12) covers the 5/6/7-arg historical signatures before `CREATE OR REPLACE`; ends with a `DO $$` audit verifying `prosrc ILIKE '%FOR UPDATE%' AND '%deadlock_detected%'` (Supabase invariant 28+29). **Paired iOS edit**: `Fit33/ChallengeService.swift:2235` — outer `AppLogger.error("Failed to sync progress for '<title>'")` downgraded to `.warning` (the underlying `logProgress` already routes the real failure through `NetworkErrorClassifier.log` with op + endpoint + pg_code at line 2115; the outer `.error` was duplicate noise creating the bb8962ac / 0d1100de cascade fingerprints — QP invariant 25a). Idempotent. |
| 120 | `20260619_mark_new_2026_04_26_resolved.sql`          | 🆕 Ready | (2026-04-26 17:32 NEW-mode export close-out, 15 fingerprints) Companion to #109/#110/#111/#113/#116. The 30 fingerprints in this export break into 20 build-1.38 cohort recurrences already flushed by #116, plus 15 needing terminal status: (A) `migration_resolved:20260617` for R6+R7+R14+R18 (`64e1ccf7`/`265848d4`/`2d865e51`/`c70b931f`, daily-quests `abs(hashtext)` int4 overflow — server fix in #117, deployed 2026-04-26); (B) `migration_resolved:20260618` for R21 (`281382da`, `log_challenge_progress` 40P01 deadlock — server fix in #119, deploys this sprint); (C) `code_fix:classifier_routing` for R15+R29 (`50e7a9a7`/`cf3a4a8b`, WorkoutManager `cancelWorkout()` `.error` → `.warning` — cancellation is expected user/auto-end behavior, not an error; the previous `.error` double-reported through `CrashReportingService.reportError` per QP invariant 25a) AND R16+R24 (`bb8962ac`/`0d1100de`, ChallengeService cascade `.error` → `.warning` — duplicate of the classifier-routed log inside `logProgress`, drains automatically once #119 lands); (D) `migration_resolved:20260605+20260607` for R23 (`e066c226`, daily-quests sig mismatch comp-params variant — same root as `e656ad7a` in #116, triage collapse missed this duplicate); (E) `code_fix:crash_reporter_denylist_pre_cohort` for R10+R25+R26+R27+R30 (`1edfaad0`/`1e8bef49`/`a7fc10fa`/`0080557f`/`18a4b0fc`, password reset rate-limit + offline streak fetch on builds **1.29.0 / 1.32.0 / 1.37** — pre-date the `CrashReportingService.shouldSuppressMessage` denylist shipped in #116; nothing to fix in code, just flush). Final `DO $$` verify block RAISE NOTICE 'expected 15 resolved'. Idempotent — every UPDATE has `AND status <> 'resolved'` short-circuit.                                                                                                                                                                                                                                                                                                                                                |


**Run order**: 119 → 120 (119 first because 120 references its `migration_resolved:20260618` bucket; both standalone, idempotent). Deploy 119 alongside any other pending bug-fix migrations; 120 can re-run safely. Verify 119 post-deploy with `SELECT prosrc FROM pg_proc WHERE proname = 'log_challenge_progress'` — should include `FOR UPDATE` on `challenge_participants` and `WHEN deadlock_detected`.

## Wake Diagnostics — cross-table progress drift extension (2026-04-26)


| #   | File                                           | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | ---------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 121 | `20260619_wake_diagnostics_progress_drift.sql` | 🆕 Ready | (User report 2026-04-26 ~13:49 — "Abbie shows 0 on 1v1 widgets but 603 in community"; Data invariant #48 violated because the fanout trigger from `20260521_challenge_progress_fanout.sql` (#87) is not yet deployed.) Extends `get_my_wake_diagnostics` (#118) so the in-app DEBUG `WakeDiagnosticsView` automatically surfaces cross-table drift instead of needing a SQL-editor sweep. Signature change: `get_my_wake_diagnostics(p_lookback_hours INT, p_timezone TEXT DEFAULT 'UTC')` — drops the 1-arg overload before `CREATE OR REPLACE` (Supabase invariant 12). Adds 4 columns per related user: `steps_today_1v1`, `steps_today_private`, `steps_today_community` (per-table MAX `progress_value` for today in caller-tz across `challenge_type='steps'`), and `progress_drift_detected BOOLEAN` (TRUE iff ≥2 non-NULL surfaces disagree — same predicate as Section A in `verify_progress_drift_2026_04_26.sql`). NULL means "no row / no membership in that surface for today" — distinct from a real 0. Steps-only (not active_minutes / calories) for v1 because steps is the only fanout-type with home-screen widget visibility and the only one observed drifting. **Paired Swift change**: `Fit33/WakeDiagnosticsView.swift` — DTO gets the four new fields (Int? + Bool?), `reload()` now passes `p_timezone: TimeZone.current.identifier`, summary section adds "Cross-table drift (steps)" tile, per-row card renders `Steps · 1v1/group / private / community` with em-dash for NULL and a red callout banner pointing at migration #87 when drift is detected. Idempotent (`DROP FUNCTION IF EXISTS` for both 1-arg and 2-arg overloads + audit `DO $$` block verifying `prosrc ILIKE '%progress_drift_detected%'`). |


**Run order**: 121 (standalone, idempotent). Depends on #118 (which it replaces). With #87 + #88 confirmed deployed (verified 2026-04-26), #121 functions as a **regression watchdog**, not a missing-fanout diagnostic — the steady state is `progress_drift_detected = FALSE` for every counterpart with steps activity, and `Steps · 1v1/group / private / community` cells matching numerically (or showing `—` for surfaces the user isn't a member of). Any non-FALSE drift flag means `fanout_challenge_progress()` has regressed (trigger dropped, function body broken, or a new daily-progress table added without a sibling trigger) and warrants immediate investigation. The 2026-04-26 "Abbie 0 vs 603" pattern is no longer a drift-detector concern — that was an upstream sync delay (her phone hadn't pushed yet); use the wake-history columns (`last_wake_at`, `wake_count_24h`, `last_progress_at`) for that diagnosis. Verify post-deploy: `SELECT user_id, steps_today_1v1, steps_today_private, steps_today_community, progress_drift_detected FROM get_my_wake_diagnostics(24, 'America/New_York') WHERE progress_drift_detected = TRUE` — expect zero rows.

## Realtime Widget Server Pull — Phase 2a (2026-04-26)


| #   | File                                                     | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | -------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 122 | `20260620_get_active_challenges_progress_timestamps.sql` | 🆕 Ready | (Realtime Widget Server Pull plan, 2026-04-26 — "Manuel shows 0 steps mid-day even though his phone hasn't crashed; 1v1 widget needs to know when his last server-side write was so it can render `— · 8h ago` instead of a confidently-wrong `0 steps`.") Extends the 1v1 `get_active_challenges(p_timezone TEXT)` RPC with two appended `TIMESTAMPTZ` columns: `my_last_progress_at` and `opponent_last_progress_at`, both computed as `MAX(updated_at) FROM challenge_daily_progress WHERE challenge_id = gc.id AND user_id = <participant> AND progress_date >= gc.start_date`. The `progress_date >= start_date` floor prevents pre-challenge rows (from prior 1v1s with the same opponent) from drifting the freshness signal. Single-table query (Data invariant #48 fanout trigger keeps `challenge_daily_progress` / `private_challenge_daily_progress` / `community_challenge_daily_progress` in sync, and the home-screen widget only consumes 1v1 today — group/community/private are not surfaced on the widget surface, so their RPCs stay untouched). Drops every overload via the canonical `pg_proc` loop (Supabase invariant 12) before `CREATE OR REPLACE`, re-asserts SECURITY DEFINER + `search_path = public`, re-`GRANT EXECUTE` to `authenticated`. Final `DO $$` audit block verifies exactly one `get_active_challenges` definition exists post-deploy AND that `prosrc ILIKE '%my_last_progress_at%' AND ILIKE '%opponent_last_progress_at%'` (Supabase invariants 28+29). **Paired Swift changes** (Phase 2b/3 of the Realtime Widget Server Pull plan): `Fit33/SupabaseDTOs.swift` `ActiveChallengeRow` adds `my_last_progress_at: Date?` + `opponent_last_progress_at: Date?` decoded via the existing ISO-8601 strategy (Supabase invariant #15); `RunningActivityWidget/WidgetSupabaseFetcher.swift` (new) reads both fields and feeds the `Shared/ProgressFreshness.swift` evaluator (Phase 6) which routes `< 1h fresh` / `< 6h recent` / `≥ 6h stale` and lets the widget render `—` + `8h ago` instead of a stale `0`. Idempotent. Reverts cleanly with `BEGIN; <DROP loop>; <re-create the 28-column TABLE return shape from #103 / 20260520>; COMMIT;`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 123 | `20260621_engagement_nudges_for_stale_opponents.sql`     | 🆕 Ready | (Realtime Widget Server Pull plan, Phase 7a — 2026-04-26) Server-side hourly engagement-nudge job that breaks the silent-push wake-budget death loop documented in #122's preface. Adds `enqueue_engagement_nudges_for_stale_opponents()` (SECURITY DEFINER, service_role only — pgcron context, Data invariant #7 satisfied because no `user_id` parameter accepted) plus an hourly `cron.schedule('engagement_nudges_for_stale_opponents', '0 * * * *', …)` job. Logic in CTEs for EXPLAIN-readability: (1) `active_1v1` = `group_challenges` with `status='active'`, `end_date >= CURRENT_DATE`, `created_at < NOW() - 24h` (warm-up floor — don't nudge on day 1), exactly two `challenge_participants` rows; (2) `participant_freshness` per-side `MAX(challenge_daily_progress.updated_at)` floored at `gc.start_date` (same predicate as #122 so RPC + nudge agree on staleness); (3) `pairs` self-join giving each side an opponent row; (4) `stale_targets` keeps rows where the recipient is silent ≥12h AND the opponent has logged inside the last 12h (otherwise both sides are silent — no honest "they're pulling ahead" message); (5) `not_recently_nudged` excludes targets with any `push_notification_queue` row of `notification_type='challenge_nudge'` AND `data->>'challenge_id' = <chId>` in the last 20h (per-(recipient, challenge) throttle, NOT per-recipient — three stale challenges can each fire one nudge per cycle). Insert shape: `notification_type='challenge_nudge'`, `data.type='challenge_nudge'`, `data.challenge_id`, `data.opponent_id`, `data.reason ∈ {'never_logged','stale_progress'}`. Title `'Your challenge is heating up 👀'`, body `'<Opponent first name> just logged progress in your <stripped-emoji title>. Open Fit33 to keep up.'` Quiet hours / master-disabled / type-disabled deferral handled by the existing `send-push-notification` edge function (`isInQuietHours()` + `disabled_types` from `user_notification_preferences`) — this RPC enqueues regardless and lets the edge fn schedule delivery. Audit block fails loud if the function or cron job didn't materialize. **Paired iOS changes** (Phase 7c): `Fit33/NotificationManager.swift` adds the `challenge_nudge` payload route → `DeepLinkManager.shared.handleDeepLink(URL(string: "fit33://challenge/<challenge_id>"))`. **Paired edge-function check** (Phase 7b): `supabase/functions/send-push-notification/index.ts` already handles arbitrary `notification.data.type` values via the existing `prefs.disabled_types?.includes(notifType)` filter — no body changes needed; just verify CMS-side default toggle for `challenge_nudge` defaults to ON. Idempotent (BEGIN/COMMIT, `cron.unschedule(…)` swallowed-EXCEPTION before `cron.schedule`, `DROP IF EXISTS` semantics implicit in `CREATE OR REPLACE FUNCTION`). |


**Run order**: 122 → 123 (123 reads the same `challenge_daily_progress.updated_at` column, but the timestamp is long-existing so 122 is not a hard prerequisite — sequencing is for narrative clarity). Both idempotent. Verify 123 post-deploy by inspecting `cron.job WHERE jobname = 'engagement_nudges_for_stale_opponents'` (1 row) and watching `push_notification_queue` for new `notification_type='challenge_nudge'` rows after the next top-of-hour tick when at least one opponent has been silent ≥12h. To revert: `SELECT cron.unschedule('engagement_nudges_for_stale_opponents'); DROP FUNCTION enqueue_engagement_nudges_for_stale_opponents();`.

## Bug-Intel Phase 13 Close-the-Loop (2026-04-26)

> Closes the user's 2026-04-26 ask: "resolved fingerprints disappear from the dashboard / CMS and only reappear if it's a regression or the bug wasn't actually fixed, fully automated." Three pieces, plus a CMS-side filter change in `admin-cms/src/app/api/admin/route.ts::get_bug_intelligence_overview`.


| #   | File                                                | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | --------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 124 | `20260622_mark_audit_2026_04_26_23_01_resolved.sql` | 🆕 Ready | (2026-04-26 23:01 audit-mode export close-out, 36 fingerprints) Flushes the entire `mode=all` audit so the dashboard reflects only OPEN work. Buckets: (1) `migration_resolved:20260605+20260607` for R6+R7+R8+R9+R10+R13+R16+R20 (daily-quest v3 / monetization cluster); (2) `migration_resolved:20260615` for R14 (`d8fe113b`, group challenge nudges col-drift); (3) `migration_resolved:20260618` for R15+R22+R26 (1v1 deadlock retry); (4) `code_fix:auth_uid_alignment+classifier_routing` for R3+R4+R24+R30 (RLS 42501 cluster — including the new R24 `7eef8124` equipment-proficiency RLS); (5) `code_fix:classifier_routing` for R17+R18+R27+R33 (paired iOS log-level downgrades); (6) `code_fix:expectedUserState_classifier` for R5+R19 (signup "User already registered"); (7) `code_fix:crash_reporter_denylist_pre_cohort` for R1+R2+R11+R12+R21+R23+R28+R29+R31+R32+R34+R35+R36 (rate-limit / offline / transient noise on builds 1.29.0/1.32.0/1.37 — pre-dates the `CrashReportingService.shouldSuppressMessage` denylist ship); (8) `code_fix:nav_routing` for R25 (challenge nudge button — `DeepLinkManager.dispatch` routes correctly post-Sprint-12). Each bucket also stamps `latest_resolving_migration_at` so the export-side stale-fix filter (#114) and the new auto-revive cron (#125) both agree on the deploy moment. `bug_intelligence_reports` rows for these fingerprints flip to `review_status='merged'` with a paper-trail audit line; `last_exported_at` is stamped so the NEW-mode export watermark stays consistent (mode=all does NOT stamp by default). Idempotent (`AND status NOT IN terminal` guards). Ships `-- Resolves: <fp>` directives for all 36 fingerprints at the file footer so the new `bug-intel-resolves-deploy.yml` GitHub Action can re-apply them on demand. |
| 125 | `20260623_bug_intel_auto_revive_on_regression.sql`  | 🆕 Ready | (Phase 13 close-the-loop — auto-revive cron) New SECURITY DEFINER service-role RPC `bug_intel_revive_regressed_fingerprints(p_grace_hours INT DEFAULT 48)` that scans pipeline-resolved fingerprints (auto_resolved_reason matches `migration_resolved:%` / `code_fix%` / `silent_fix` / `transient_single_incident` / `noise_filter_expanded`) and revives any whose `last_seen_at > GREATEST(latest_resolving_migration_at, resolved_at) + 48h grace`. Flips `status='new'`, `regressed_after_fix=TRUE`, clears `auto_resolved_at` so the rollup doesn't immediately re-resolve via the silent+fixed path. Emits a `bug_intelligence_trends` row with `trend_type='regression_after_fix'` (idempotent — skips if already emitted today) and reopens the most recent `bug_intelligence_reports` row per fingerprint by flipping `review_status` from `merged` back to `pending` with a stamped audit note. Crucially never touches HUMAN-resolved rows (`auto_resolved_reason IS NULL` means a human clicked Resolve in the CMS — sticky). pg_cron at `:20 * * * `* runs after `compute_daily_bug_rollup` (`:00`) and `bug_intel_recompute_severity` (`:10`) so the revival sees freshest data. The 48h grace window matches the export-side `STALE_FIX_GRACE_MS` constant in `admin-cms/src/app/api/admin/route.ts::get_bug_intelligence_export` — tune both in lock-step. Together with the export filter (#114) and the dashboard-overview filter (CMS-side, this sprint), this closes the user's loop end-to-end: resolved → invisible everywhere; regression → reappears as `new + regressed_after_fix=TRUE` within an hour.                                                                                                                                                                                          |


**Run order**: 124 → 125 (124 must run after #95 / #114 because it stamps `latest_resolving_migration_at`; 125 only depends on the columns from #114). Both idempotent. Verify by (a) loading `/bug-intelligence` — "Open fingerprints" headline drops by ≈36; (b) `SELECT COUNT(*) FROM bug_intelligence_fingerprints WHERE status = 'resolved' AND auto_resolved_reason IS NOT NULL AND last_seen_at > GREATEST(resolved_at, latest_resolving_migration_at) + INTERVAL '48 hours'` returns 0 immediately after the first cron tick. To revert: drop migration #125's RPC + cron job; #124's UPDATEs are reversible by `UPDATE bug_intelligence_fingerprints SET status = 'triaged' WHERE auto_resolved_reason LIKE 'migration_resolved:20260605+20260607' AND resolved_at >= '2026-04-26'` (and similar per bucket).

## Widget Freshness Sprint Phase 7b — Server-Driven Opponent Wakes (2026-04-26)

> Closes the worst-case "20-min wait until my widget timeline pull" gap when both opponents have their main app suspended. When ANY participant in an active 1v1 / private challenge writes a higher progress value, fire a silent push to every other participant of that challenge so their device wakes, runs the LITE challenge sync, and republishes a fresh widget snapshot. Ties together with the matching iOS change in `Fit33/BackgroundChallengeSyncService.swift` (steps observer throttle 600s → 120s + LITE wake path) so opponents see fresh step counts inside ~5–10s end-to-end.


| #   | File                                         | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | -------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 126 | `20260625_opponent_progress_silent_push.sql` | 🆕 Ready | Adds `notify_opponents_on_progress_change()` AFTER INSERT/UPDATE OF `progress_value` trigger on `challenge_daily_progress` AND `private_challenge_daily_progress`. Recursion-guarded (`pg_trigger_depth() > 1` skips the Data invariant #48 fanout writes), monotonicity-guarded (only on strict-increase / positive-INSERT), same-day-guarded (yesterday-or-newer rows only). Fans out via `net.http_post` → `wake-challenge-opponents { source: "progress_update", recipient_ids: [...] }`. Migration also extends `silent_push_wake_log.triggered_by` CHECK constraint to allow the new bucket value. Community challenges DELIBERATELY excluded — too many participants per challenge to fan out cheaply. Edge function side adds per-source throttle map (`progress_update` = 60s window per recipient, separate from the existing 15-min `foreground / cron / background_sync` buckets so each lane throttles independently) and a service-role-only `recipient_ids: UUID[]` body path. User JWT calls cannot target arbitrary recipients (defense-in-depth — server triggers alone hold this capability). pg_net's async queue swallows POST failures so a slow / dead edge function never affects originating progress writes. |


**Run order**: 126 standalone (depends on `silent_push_wake_log` from #54 and `internal_config` from #66). Idempotent. Deploy the SQL ahead of the edge function update — calls return early with a graceful 403 (or are throttled out) until the function rolls. Verify post-deploy: (a) `SELECT tgname FROM pg_trigger WHERE tgname LIKE 'trg_notify_opponents_progress_change%'` returns 2 rows; (b) `SELECT triggered_by, COUNT(*) FROM silent_push_wake_log WHERE sent_at > NOW() - INTERVAL '1 hour' GROUP BY 1` should show `progress_update` rows after the next active-challenge write. To revert: `DROP TRIGGER IF EXISTS ... ; DROP FUNCTION IF EXISTS notify_opponents_on_progress_change();` — does not touch progress data.

**Companion changes (NOT migrations — pure edge function + iOS):**

- `supabase/functions/wake-challenge-opponents/index.ts` — adds `progress_update` source handling: service-role gate, `recipient_ids` body path, `THROTTLE_WINDOWS_MS_BY_SOURCE` map, per-source `triggered_by` filter on the `silent_push_wake_log` lookup.
- `Fit33/BackgroundChallengeSyncService.swift` — steps HK throttle 600s → 120s, low-priority HK observers (`steps` / `active_energy` / `distance` / `exercise_time`) take the LITE wake path so the tighter cadence doesn't run the full multi-wearable pipeline. Workout completions stay on the FULL path.
- `INFRA_SECURITY_AGENT.md` Edge Function Auth Registry — `wake-challenge-opponents` row updated to document the new mode + per-source throttle.

## Widget Freshness Sprint Phase 7d — Widget-Side Write-Back (2026-04-26)

> Closes the "friend hasn't opened the app in a day" gap. The widget extension already reads HealthKit steps directly (Phase 7c); Phase 7d makes it ALSO push those steps back to the server via `log_challenge_progress` so opponents see fresh numbers even when the writer's main app has been force-killed for hours. Pairs with a 24h JWT lifetime (configured via Supabase dashboard) — without that, the widget can only push for the first hour after each app-open.


| #   | File                                     | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ---------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 127 | `20260626_widget_writes_kill_switch.sql` | 🆕 Ready | Adds `widget_writes_enabled` row to `internal_config` (default `'true'`) AND patches `log_challenge_progress` with a top-of-function early-return when `p_source = 'widget'` AND the flag is FALSE. Returns `TRUE` (silent success) so the widget — which can't refresh its own JWT — doesn't get stuck retrying a 4xx; users see Phase-7c-style "server-only" widget data while disabled. Operationally: `UPDATE internal_config SET value = 'false' WHERE key = 'widget_writes_enabled';` to disable from dashboard. Effect is instant on the very next RPC call (no caching). The deterministic-lock-order + 40P01 deadlock retry from migration #117 (`20260618`) is preserved verbatim — the only delta is the kill-switch early-return. Audit block confirms exactly one `log_challenge_progress` overload remains + the flag row is present. |


**Run order**: 127 standalone (depends on `internal_config` from #66 and on #117 having shipped the canonical RPC body). Idempotent. Verify post-deploy: `SELECT value FROM internal_config WHERE key = 'widget_writes_enabled';` returns `'true'`. To test the kill switch: temporarily flip to `'false'`, push a widget-source RPC call (e.g. via the widget's `pushFreshStepProgressIfNeeded`), confirm `challenge_daily_progress` row is unchanged, flip back to `'true'`. To revert: re-apply migration #117 and `DELETE FROM internal_config WHERE key = 'widget_writes_enabled';` — safe, additive.

**Companion changes (NOT migrations — pure iOS + dashboard config):**

- `RunningActivityWidget/WidgetSupabaseFetcher.swift` — adds `logChallengeStepProgress(challengeId:stepCount:)` write path. Same App Group JWT as the read path; same 3s timeout. Throws `WidgetSupabaseFetcherError.http(status: 401)` on expired-JWT so callers can branch.
- `RunningActivityWidget/ActiveChallengeWidget.swift::pushFreshStepProgressIfNeeded` — fire-and-forget post-overlay. Per-challenge 120s throttle + value-dedupe persisted in App Group UserDefaults (keys `fit33.widget.lastStepPush.v1.<challengeId>` + `fit33.widget.lastStepValue.v1.<challengeId>`). Only fires when `hkSteps > row.myTodayProgress`. 401 bumps the throttle so we don't spin on dead-JWT calls; non-401 transport errors let the next tick retry.
- `INFRA_SECURITY_AGENT.md` invariant 21a — narrowed but not broken: the widget MAY call `log_challenge_progress` with `p_source = 'widget'` (explicit allow-list), nothing else. Watch / other extensions remain strictly read-only.
- `QUALITY_PERFORMANCE_AGENT.md` invariant 25cc — debounce + value-dedupe contract for the widget write-back.
- **Supabase Dashboard** (manual): bump JWT expiry from 3600s → 86400s (Authentication → Settings → "JWT expiry"). Without this, the widget can only push for the first hour after each app-open. With it, full 24h.

**Companion changes (NOT migrations — pure CMS / CI):**

- `admin-cms/src/app/api/admin/route.ts::get_bug_intelligence_overview` — filters terminal-status fingerprints out of the headline `Total fingerprints` + Crash/Log/Bug source breakdown. Surfaces `terminal_count` + `terminal_by_status` + `regressed_open_count` separately so the UI can render "169 open · 47 resolved · ⚠ 2 regressed".
- `admin-cms/src/app/bug-intelligence/page.tsx::OverviewRow` — relabels "Total fingerprints" → "Open fingerprints", appends resolved + regressed sub-counts.
- `.github/workflows/bug-intel-resolves-deploy.yml` — on push to `main` touching `supabase/*.sql`, scans changed migrations for `-- Resolves: <md5>` directives, calls `mark_fingerprints_resolved_by_migration(<basename>, [<fps>])` per migration. Idempotent (the RPC skips already-terminal rows). Required secrets: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` (same as admin-cms env).

## Bug Intelligence Stale-Fix Filter (Phase 13 — 2026-04-25)


| #   | File                                      | Status   | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | ----------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 114 | `20260614_bug_intel_stale_fix_filter.sql` | 🆕 Ready | (Bug-intel `last_seen_after_fix_deployed` filter — the audit-feedback follow-up) Adds two columns to `bug_intelligence_fingerprints`: `latest_resolving_migration_at TIMESTAMPTZ` + `latest_resolving_migration_id TEXT`, with a partial index `idx_bug_fingerprints_latest_resolving` (status NOT IN terminal). New SECURITY DEFINER service-role RPC `bug_intel_register_migration_deploy(p_migration_id TEXT, p_resolves TEXT[])` stamps both columns to `now()` for every supplied fingerprint without flipping status (regression-watch mode — distinct from `mark_fingerprints_resolved_by_migration` which DOES flip). Also re-creates `mark_fingerprints_resolved_by_migration` so that path now ALSO stamps the new columns (signature unchanged, body delta only). Backfill block: every fingerprint whose `auto_resolved_reason LIKE 'migration_resolved:%'` gets `latest_resolving_migration_at = COALESCE(auto_resolved_at, resolved_at, updated_at)` and the parsed migration id; `migration_pending_deploy:`* rows get the id but a NULL timestamp so the filter still shows them. Self-register block stamps the four 04-25 audit fingerprints (R1 `e656ad7a` / R3 `ec1a1554` / R5 `7bf1ff4e` / R7 `f30626309d`) so the filter starts working immediately. The admin export filter in `admin-cms/src/app/api/admin/route.ts::get_bug_intelligence_export` now hides reports where `mode != 'all' AND latest_resolving_migration_at IS NOT NULL AND last_seen_at <= latest_resolving_migration_at + 48h grace AND regressed_after_fix IS NOT TRUE` — that's the stale-client + PostgREST schema-cache reload tail, not a real regression. The 48h window is tunable in `STALE_FIX_GRACE_MS`. Genuine regressions (regressed_after_fix=TRUE OR last_seen >48h after deploy) still surface. The export markdown TL;DR adds a `Stale-fix excluded: N hidden` line so reviewers see the filter is working, and per-bundle bodies now show `🚀 Fix deployed: <id> at <ts> · last_seen +Xh after deploy` whenever a migration shipped but activity broke through the grace window — making "the diff didn't hold" debugging instant. Idempotent (`ADD COLUMN IF NOT EXISTS` + `CREATE INDEX IF NOT EXISTS` + `ON CONFLICT`-style WHERE-guarded backfill). Final verifier `DO $$` block confirms columns + index + RPC + prints backfilled count. Closes the audit-feedback loop: the next bug-intel export auto-omits already-fixed-upstream fingerprints instead of needing a hand-written #113-style close-out migration each time. Ships header `-- Resolves:` directives for R1/R3/R5/R7 so future CI hooks can self-validate. |


**Run order**: 114 (standalone, idempotent). Deploy any time after #93 (`20260529_bug_intel_migration_resolves_link.sql`) which provides the parent `mark_fingerprints_resolved_by_migration` function this migration recompiles. Verify by checking that `SELECT COUNT(*) FROM bug_intelligence_fingerprints WHERE latest_resolving_migration_at IS NOT NULL` returns ≥4 (the 04-25 audit self-register), and that the next `/bug-intelligence` export shows `Stale-fix excluded: …` in the TL;DR. To revert, drop the two columns + the new RPC; the existing close-out migrations (#109/#110/#111/#113) keep working without them.

## Strava Integration Upgrade (2026-04-25)


| #   | File                                         | Status     | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | -------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 98  | `20260530_cardio_workouts_strava_detail.sql` | ✅ Deployed | (Phase 2 of Strava Integration Upgrade — activity detail enrichment) Adds the `cardio_workouts` columns required by `Fit33/StravaActivityEnricher.swift` to land richer per-activity payloads beyond the basic list endpoint: `suffer_score INT`, `kudos_count INT`, `achievement_count INT`, `polyline_summary TEXT`, `splits_json JSONB`, `segment_efforts_json JSONB`, `streams_json JSONB`, `gear_name TEXT`, `detail_synced_at TIMESTAMPTZ`. Partial index `idx_cardio_workouts_strava_pending_enrichment ON (user_id, started_at DESC) WHERE source = 'strava' AND detail_synced_at IS NULL` is the hot-path read for the enricher's "what still needs detail?" query — never use `detail_synced_at` as a tombstone. Idempotent / additive only — no breaking change to existing `cardio_workouts` consumers.                                                                                                                                                                                                                                                                                                                   |
| 99  | `20260531_strava_quest_templates.sql`        | ✅ Deployed | (Phase 3 of Strava Integration Upgrade — daily quest detection) Registers three new `quest_templates` rows (`run_outside_3km`, `run_outside_5km`, `cycle_outside_15km`) with the standard XP / league-points / difficulty mix, plus a SECURITY DEFINER helper RPC `is_strava_quest_completed(p_user_id UUID, p_quest_key TEXT, p_timezone TEXT)`. The RPC pins `auth.uid() = p_user_id` (Data invariant #7), filters `cardio_workouts` on `source IN ('strava')` + `activity_type = ANY(...)` (matches `mapStravaActivityType` output: `outdoor_run` for runs, `outdoor_cycle` for rides — treadmill / indoor_cycle EXCLUDED) + `distance_meters >= target` + day-of in caller timezone. Helper is registered + granted to `authenticated`; wiring it into `get_daily_quests` follows in a small follow-up migration once the matching pattern has been observed in prod.                                                                                                                                                                                                                                                             |
| 100 | `20260532_strava_insights_cron.sql`          | ✅ Deployed | (Phase 3 of Strava Integration Upgrade — server insights cron) Wires the `compute-strava-insights` edge function to pg_cron. `trigger_compute_strava_insights()` SECURITY DEFINER follows the canonical `internal_config` + `x-cron-key` pattern (Supabase invariant #25 / `trigger_compute_readiness_insights` from migration 72). Schedule: `40 3 * * *` (03:40 UTC) — staggered between `compute-readiness-insights` (03:30) and bug-intel sweeps (03:45 / 04:30) so cold-starts don't pile up. Edge function reads 60d of `cardio_workouts` (source='strava') + `daily_readiness_history` and upserts five insight cards into `user_personalized_insights` keyed `(user_id, insight_key)` (Data invariant #36): `strava_weekly_mileage_delta`, `strava_pace_trend_4w`, `strava_hr_zone_drift`, `strava_segment_pr`, `strava_recovery_pairing`. Auth via `x-cron-key` header OR direct service-role bearer (matches `compute-readiness-insights` shape).                                                                                                                                                                           |
| 101 | `20260533_strava_webhook_tokens.sql`         | ✅ Deployed | (Phase 5 of Strava Integration Upgrade — server-side webhook tokens) Creates `public.user_strava_tokens` (`user_id` PK FK→`user_profiles(id)` ON DELETE CASCADE, `access_token`, `refresh_token`, `expires_at`, `athlete_id`, `last_rotated_at`, timestamps) with full RLS (SELECT/UPDATE/DELETE pinned to `auth.uid() = user_id`; INSERT is RPC-only). Two RPCs: `upsert_strava_tokens(p_access TEXT, p_refresh TEXT, p_expires_at TIMESTAMPTZ, p_athlete_id BIGINT DEFAULT NULL)` SECURITY DEFINER pinned to `auth.uid()` for the iOS dual-write path (called by `Fit33/StravaService.mirrorTokensToSupabase` after every token exchange + every refresh rotation), AND `get_user_id_for_strava_athlete(p_athlete_id BIGINT)` SECURITY DEFINER service-role-only for the `strava-webhook` edge function to map Strava `owner_id` → app user_id without exposing the tokens table. `BEFORE UPDATE` trigger maintains `updated_at`. Strava rotates the refresh token on every refresh — both the iOS keychain path and the edge function refresh path persist the new value; `last_rotated_at` distinguishes which side rotated last. |


**Run order**: 98 → 99 → 100 → 101 (all standalone, idempotent). Safe to re-run.

## Smart Adaptive Daily Goals (2026-04-25)


| #   | File                                                  | Status     | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ----------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 102 | `20260601_user_quest_personalization_schema.sql`      | ✅ Deployed | (Smart Adaptive Daily Goals — schema) Adds three per-user learning tables: `user_quest_personalization (user_id, category, total_assigned_28d, total_completed_28d, completion_rate_28d, skip_streak, last_completed_at, suppressed_until)` PK `(user_id, category)`, `user_quest_key_stats` same shape at `quest_key` grain, and `user_activity_mix (user_id, computed_at, total_sessions_28d, strength_share, cardio_share, walk_share, stretch_share, dominant_category, least_category)` (one row per user). All RLS-pinned to `auth.uid() = user_id` and added to `delete_user_account()`. Also adds `quest_templates.tier TEXT NOT NULL DEFAULT 'free' CHECK (tier IN ('free','pro'))` to gate Pro-only templates. Suppression rule: `skip_streak >= 3 AND completion_rate_28d < 0.20` → `suppressed_until = today + 14d`; auto-clears on next completion.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 103 | `20260602_compute_user_quest_personalization.sql`     | ✅ Deployed | (Smart Adaptive Daily Goals — nightly cron) `compute_user_quest_personalization()` SECURITY DEFINER scans 28 days of `user_daily_quests` per user, upserts per-category and per-key stats, walks `quest_date DESC` to compute `skip_streak`, applies suppression. Computes `user_activity_mix` from `workouts` (strength) + `cardio_workouts` (split by `activity_type`: `walking`/`hike` → walk; `yoga`/`stretch`/`mobility` → stretch; everything else → cardio). pg_cron `50 3 * * *` (staggered between readiness 03:30 and bug-intel 04:15/04:30 per Supabase invariant 25). Initial backfill runs at install so day-one users have data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 104 | `20260603_quest_templates_xp_tier_rebalance.sql`      | ✅ Deployed | (Smart Adaptive Daily Goals — XP rebalance) One-shot `UPDATE quest_templates SET xp_reward = ROUND(xp_reward * mult)` where `mult` is `auto=1.5`, `social=1.0`, `manual=0.7`. Surfaces "auto-tracked = provable = pays more, honor system = lower" asymmetry. Same multiplier applied to `league_points`. Idempotent via a one-shot guard row in `migration_state`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 105 | `20260604_strava_pr_and_friend_quest_templates.sql`   | ✅ Deployed | (Smart Adaptive Daily Goals — new templates) Inserts new `quest_templates` rows: Strava PRs (`beat_your_5k_pr`, `negative_split_run`, `run_outside_8km`, `cycle_outside_30km`, `complete_strava_segment`) gated `requires_context = 'has_strava'`; wearable adds (`match_yesterday_strain` `has_whoop`, `walk_when_red` `has_wearable`); friend-named (`do_friend_workout` muscle-recovery-aware, `comment_on_friends_workout`, `start_1v1_with_top_friend`, `react_to_3_workouts`). New context tags `has_strava`, `has_whoop`, `has_oura`, `has_fitbit` added (existing `has_wearable` retained as union-OR).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 106 | `20260605_get_daily_quests_personalized.sql`          | ✅ Deployed | (Smart Adaptive Daily Goals — RPC v3) Drops every prior `get_daily_quests` overload via the `pg_proc` loop (Supabase invariant 12). New body adds 9 params: `p_strava_connected`, `p_whoop_connected`, `p_oura_connected`, `p_fitbit_connected`, `p_activity_mix JSONB`, `p_friend_step_target`, `p_friend_name`, `p_friend_top_workout_id/title/split`, `p_friend_top_workout_matches_recommendation`, `p_quest_tier`. Six-layer logic: (1) workout slot, (2) redundancy matrix, (3) challenge override, (4) **activity-mix bias + per-user weighting** (+30% dominant, +25% top-quartile completion, +10% exploration to least-touched, −90% suppressed), (5) skip-streak floor, (6) friend-named copy + Pro 5-slot expansion. Friend copy is split-recovery-aware: `matches_recommendation=TRUE` writes "Due for chest — do Paul's", FALSE falls back to "Do Paul's ".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 107 | `20260606_verify_integration_quests_rpcs.sql`         | ✅ Deployed | (Smart Adaptive Daily Goals — verification fanout) `verify_strava_quests_for_today(p_timezone)` SECURITY DEFINER `auth.uid()`-pinned walks today's `user_daily_quests` for the caller, calls existing `is_strava_quest_completed(...)` per Strava key + new detectors for `beat_your_5k_pr` (queries `cardio_personal_records`), `negative_split_run` (parses `splits_json`), `complete_strava_segment` (reads `segment_efforts_json`), `weekly_mileage_+10pct` (queries `cardio_weekly_summaries`), then calls `update_quest_progress` to flip `is_completed`. `verify_wearable_quests_for_today(p_timezone)` reads today's `daily_readiness_history` row and ticks `recovery_above_67`, `sleep_8h_wearable`, `hrv_above_baseline`, `rhr_in_healthy_range`, `walk_when_red`, `respect_red_recovery`. Called from iOS hooks (StravaService, ReadinessService) as fire-and-forget detached Tasks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 108 | `20260607_pro_quest_monetization.sql`                 | ✅ Deployed | (Smart Adaptive Daily Goals — Pro monetization) Adds `user_daily_quests.double_xp BOOLEAN`, `is_custom BOOLEAN`, `is_reroll BOOLEAN`. New `user_quest_rerolls (user_id, reroll_date, count, last_at)` ledger and `user_double_xp_claims (user_id, claim_week_start, claimed_at)`. RPCs: `reroll_daily_quest(p_quest_id, p_timezone, p_is_pro)` (Free 1/day cooldown, Pro 5/day no cooldown), `claim_double_xp_day(p_date, p_is_pro)` (Pro 1/week, marks day's quests `double_xp=TRUE`; `update_quest_progress` doubles XP at completion), `submit_custom_quest(p_title, p_target_value, p_target_unit, p_is_pro)` (Pro 1/day, manual verification, capped 25 XP, `is_custom=TRUE`), `unsuppress_quest_category(p_category, p_is_pro)` (Pro override). View `v_user_quest_personalization_summary` (`security_invoker = on`) joins personalization + activity-mix + computed `state` (`on_fire`/`mixed`/`cold`/`suppressed`) for the Pro Insights screen. All Pro RPCs guard on caller `p_is_pro` (matches existing `p_is_subscriber` pattern in get_daily_quests).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 109 | `20260610_actionable_recovery_quests.sql`             | ✅ Deployed | (User feedback 2026-04-25 — daily quests must be actionable today, not pre-determined by overnight sensor state) Soft-disables three passive wearable quests from 20260509 — `recovery_above_67` "Green Recovery", `hrv_above_baseline` "HRV Warrior", `rhr_in_healthy_range` "Steady Heart" — by setting `is_active = FALSE`. Templates retained on disk for backwards-compat with already-assigned `user_daily_quests` rows. Inserts three actionable replacements gated `requires_context = 'has_wearable'`, all auto-verified from `cardio_workouts`: `active_recovery_logged` "Active Recovery" (15+ min walk/yoga/stretch/mobility/foam-rolling, any band), `zone_2_minutes_20` "Zone 2 Cardio" (20+ min cardio with `average_heart_rate` ∈ [110, 150] bpm — HRV-friendly intensity, age-agnostic range), `cardio_minutes_20` "Heart Healthy" (20+ min any logged cardio session, distinct from HealthKit-ambient `active_minutes_30`). XP rewards seeded post-rebalance (38 / 45 / 30) so the 20260603 `auto × 1.5` multiplier is not re-applied. Rebuilds `verify_wearable_quests_for_today(p_timezone TEXT)` via the canonical `pg_proc` overload-drop loop (supabase-rules §12) — adds three ELSIF branches, drops the early-return-on-null-readiness gate (new quests verify straight from `cardio_workouts` without needing a readiness row), legacy passive branches retained inside `IF v_readiness IS NOT NULL` guards for in-flight assignments. SECURITY DEFINER + `auth.uid()`-pinned shape unchanged. Idempotent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 110 | `20260611_retire_passive_sleep_engagement_quests.sql` | 🆕 Ready   | (User feedback 2026-04-25 — second pass of PE invariant 19d watch-list) Soft-disables three more passive quests: `sleep_8h_wearable` "Sleep 8 Hours" + `sleep_7_hours` "Sleep Champion" (both tick via iOS `DailyQuestService.onSleepLogged` against last-night data — already locked by morning), and `log_readiness_am` "Morning Check-In" (which had ZERO verification logic anywhere — phantom quest that could never auto-complete). Cleanup pass: `DELETE FROM user_daily_quests WHERE quest_key = 'log_readiness_am' AND quest_date = current_date AND is_completed = FALSE` so users don't stare at a permanently-stuck 0/1 quest. Inserts one actionable replacement: `evening_wind_down` "Evening Wind Down" — log walk/yoga/stretch/mobility/foam-rolling cardio AFTER 6pm local. `requires_context = 'has_wearable'` because the verifier RPC (`verify_wearable_quests_for_today`) is only triggered from the wearable-recompute iOS path; non-wearable users keep the existing `stretch_session` quest. XP seeded post-rebalance (38 XP / 15 LP). Rebuilds `verify_wearable_quests_for_today` via the canonical `pg_proc` drop-loop with one new ELSIF branch using `EXTRACT(HOUR FROM (started_at AT TIME ZONE p_timezone)) >= 18`. Paired iOS edit: `Fit33/DailyQuestService.swift::onSleepLogged(hours:)` short-circuited to a no-op (kept as a future attach point so call sites in sleep-sync paths don't need to be untangled). Idempotent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 128 | `20260630_retire_relative_to_others_quests.sql`       | 🆕 Ready   | (User feedback 2026-04-27 — Daily Goals dashboard screenshot, third pass of PE invariant 19d watch-list, extends the rule to "no quest gated on a SECOND user's behavior") Soft-disables two more relative-to-others quests: `top_3_league` "Podium Finish" (verifies via `WeeklyLeagueService.standing.myRank` — a function of every other league participant's XP, so a user with a perfect XP day can still drop a slot because a rival posted higher) and `beat_friend_steps` "Step Showdown" (verifies by comparing user's step count to a friend's via the active steps challenge — user can blow past their own step goal and still "fail" because the friend walked more). Same anti-pattern family as 20260610 / 20260611's passive sensor-state quests: the user has no deterministic lever today, just hopes the other party under-performs. Cleanup pass: `DELETE FROM user_daily_quests WHERE quest_key IN ('top_3_league','beat_friend_steps') AND quest_date today±1d AND is_completed = FALSE` so the user staring at "Podium Finish" right now (Joe in the screenshot — "Top 3 — you're #2 · Not yet") doesn't have to wait until midnight. Users will have 2 slots today and a fresh 3-slot slate tomorrow drawn from the post-disable pool. We do NOT regenerate the slate today (`get_daily_quests` short-circuits at `IF v_quest_count = 0`, so we'd have to wipe ALL three rows for the day — that would torch any in-progress XP/streak credit on slot 1). 2-slot day is the lesser evil. iOS `QuestKey` enum cases `top3League` + `beatFriendSteps` STAY in `Fit33/DailyQuestService.swift` (PE invariant 19d backwards-compat for historical rows). No verifier removals — both quests had iOS-side state-driven descriptions, not a server verifier RPC. Replacement design rule: when designing a new quest, ask "if every other user dropped offline today, can THIS user still complete it by their own actions?" — if no, reject the design. Competition belongs on the challenge / league surfaces themselves, never as a Daily Goals slot. Idempotent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 129 | `20260701_daily_brief_telemetry.sql`                  | 🆕 Ready   | (Daily Brief feature — fused multi-source welcome insight, 2026-04-27) Adds two metrics-only tables for the new dashboard subtitle replacement: `daily_brief_impressions (user_id, capacity_band, capacity_source, debt_kind, goal_family, has_booster, cta_code, source_trace, surfaced_at, client_timezone)` — one row per surfaced brief, written by iOS `BriefTelemetry.logImpression` whenever `WelcomeBriefRow` paints a freshly-composed brief; and `daily_brief_taps (impression_id, user_id, tapped_at, completed_action_at)` — written on row tap, with `completed_action_at` updated by the client iff the routed CTA was followed through within 30 minutes (workout finished, water logged, meal saved, challenge opened). RLS pinned to `auth.uid()` for both tables (self-only INSERT/SELECT/UPDATE; service_role retains full access for nightly aggregation). Indexes: `(user_id, surfaced_at DESC)` and `(capacity_band, debt_kind, goal_family)` on impressions; `(user_id, tapped_at DESC)` and `(impression_id)` on taps. Used to A/B which `(band × debt × goal)` template families actually drive completion so the templates table in `Fit33/DailyBriefTemplates.swift` can iterate based on real conversion data. Hot-path independence: nothing in the app reads from these tables; the iOS uploader is fire-and-forget and silently no-ops when offline (Data invariant 26 auth guard). Pairs with new client files `DailyBriefEngine.swift` / `DailyBriefTemplates.swift` / `DailyBriefStore.swift` / `WelcomeBriefRow.swift` and the V2 correlation flag flip in `AppConfig.FeatureFlags.personalizedInsightsV2 = true` (was false / Sprint 3 Q2-40 kill-switch). Idempotent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 130 | `20260702_quest_category_user_toggles.sql`            | 🆕 Ready   | (Daily Goals Insights — per-category user toggles, 2026-04-27) Pro-only follow-up to #108 (`20260607_pro_quest_monetization.sql`). Adds `set_quest_category_enabled(p_category TEXT, p_enabled BOOLEAN, p_is_pro BOOLEAN)` SECURITY DEFINER + `auth.uid()`-pinned RPC (Supabase invariant 9; Data invariant 7). `p_enabled = TRUE` clears `user_quest_personalization.suppressed_until` (mirrors `unsuppress_quest_category`). `p_enabled = FALSE` UPSERTs the row with the **forever-sentinel** `suppressed_until = '2099-12-31'::DATE` so the existing eligibility CTE in `get_daily_quests` v3 (which already excludes `(user_id, category)` where `suppressed_until > today`) automatically drops the disabled category from the next slate — zero surgery on the 60-line CTE required. Allowed `p_category` values are the canonical 5 from the diversity sweep (`workout`, `nutrition`, `steps`, `social`, `tracking`); anything else returns `{success: false, reason: 'unsupported_category'}` (defense-in-depth — prevents a malicious client poking at `wildcard`/`reward`). Caller must be Pro; non-Pro returns `pro_required` (matches existing `p_is_subscriber` pattern). Recreates the view `v_user_quest_personalization_summary` (Supabase invariant 6 — `security_invoker = on`) with two additions: a synthetic `user_disabled BOOLEAN` column (`suppressed_until > CURRENT_DATE + 365d`) so iOS reads can distinguish user-toggle-off from auto-suppression, and a new `state` value `'disabled'` (alongside `on_fire`/`mixed`/`cold`/`suppressed`). Auto-suppression caps at +14d, so the +365d sentinel can never collide. iOS surface lives in `Fit33/QuestInsightsView.swift::categoryTogglesSection` — calls `DailyQuestService.setCategoryEnabled(_:enabled:)` and refreshes the slate immediately on success. PE invariant 19 fallback (`defaultGoals()` when server returns empty) covers the corner case of "user disabled every category". Idempotent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 131 | `20260704_dedupe_exercise_performance_history.sql`    | 🆕 Ready   | (Active workout "Recent" tile dedup — bug fix for ghost `— lb · <date>` chips, 2026-04-27) Cleanup-only migration paired with the removal of `WorkoutManager.recordExercisePerformance()` in `Fit33/WorkoutManager.swift`. Until this PR, two iOS code paths inserted into `exercise_performance_history` on every workout completion: (a) the canonical `ExerciseHistoryService.saveExercisePerformance()` (writes `total_reps` / `max_weight` / `max_reps` / `avg_weight` / `avg_reps` / `total_volume` and the matching `exercise_set_history` rows + updates `exercise_personal_records`), and (b) the legacy `WorkoutManager.recordExercisePerformance()` (writes only `best_set_weight` / `best_set_reps` / `one_rep_max_estimate` / `equipment_used` — leaves `avg_weight` NULL). The legacy row rendered in `RecentSessionsTilesRow` (active workout, sourced from `ExerciseHistoryService.fetchRecentSessions`) as `— lb · <date>` ghost chip alongside the real `<avg> lb · <date>` chip — confirmed in production: 256 of 611 rows (42% of the table) were duplicate ghosts across 3 affected users. DELETE predicate: `avg_weight IS NULL AND best_set_weight IS NOT NULL AND max_weight = 0 AND EXISTS (canonical sibling for same` (user_id, exercise_name, workout_id)`with`avg_weight IS NOT NULL`)`. The EXISTS clause is the safety belt — historical rows that pre-date `ExerciseHistoryService.saveExercisePerformance` (legacy was the only writer) are KEPT to preserve the user's only record of those workouts. Wrapped in `BEGIN; ... COMMIT;` with pre/post `RAISE NOTICE` audit + post-condition assertion. Idempotent. Re-running matches an empty set. Aligns with `DATA_BACKEND_AGENT.md` line 159 ("Exercise performance table: columns are `max_weight` / `max_reps` (NOT `best_set_*`)") and adds a new invariant (#28d) locking `ExerciseHistoryService.saveExercisePerformance` as the single canonical writer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 132 | `20260703_get_daily_quests_brief_signals.sql`         | 🆕 Ready   | (Daily Mission Unification — Phase 1, 2026-04-27) Extends `get_daily_quests` v3 (32 args, #128 in 20260619) into v4 (36 args) by adding 4 brief-signal params at the tail: `p_capacity_band TEXT` ('green'/'yellow'/'red'/'unknown'), `p_capacity_score INT`, `p_top_debt_kind TEXT` (matches Swift `DebtKind.rawValue`), `p_top_debt_payload JSONB` (`{ muscles, days, deficitG, deficitVsPaceG, deficitL, deficitMl, gap, gapRaw, subKind }`). All four default to NULL/empty so legacy callers (cron, backfills, pre-Phase-2 clients) see ZERO behavior change. Two new selection layers in the v3 body: **Layer 7 — Capacity Band Re-Ranker** (red day filters out hard/very-hard difficulty templates from the eligibility pool, elevates the recovery pool — `active_recovery_logged`, `walk_when_red`, `evening_wind_down` — and forces at least one non-workout slot to be from the recovery pool; green day boosts PR-flag templates' selection_score by 20%; yellow/unknown no-op so unconnected users see no change), **Layer 8 — Debt Booster** (when `p_top_debt_kind` matches a "real debt" threshold — `proteinDeficit + subKind=behindPace + deficitVsPaceG >= 30`, `hydrationDeficit + deficitMl >= 500`, `stepsBehindGoal + gapRaw >= 3000` — force-elevate one quest from the matching category into slot 2 or 3, never overrides slot 1, never overrides a challenge-locked slot, caps at one debt-driven slot per day). Response gains `is_brief_influenced BOOLEAN` per quest computed inline from a local `v_brief_influenced_keys TEXT[]` (no schema change to `user_daily_quests`). Used by `Fit33/DailyQuestViews.swift` to render the "← from your brief" provenance chip on the quest card. Drops every prior overload via the canonical pg_proc loop (Supabase invariant 12); post-migration audit confirms exactly 1 surviving definition (Supabase invariant 28). FE invariant 23 (red recovery override) preserved — Layer 7 just makes the slate REFLECT the override decision so users stop seeing 25-set quests on red days. Pairs with iOS `Fit33/DailyQuestService.swift` (4 new params packed from `DailyBriefStore.shared.brief?.decision`) + `Fit33/DailyBriefEngine.swift` (`BriefDecision` struct, `matchQuests`, `preferFocusQuest`). Idempotent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 134 | `20260706_retire_locked_in_set_count_quests.sql`      | ✅ Deployed | (User feedback 2026-04-27 — Daily Goals dashboard screenshot, fourth pass of PE invariant 19d watch-list, extends the rule to "no quest gated on slot-1 completion") Soft-disables `exercise_sets_15` "Set Machine" (Hit 15 sets in a single workout) and `exercise_sets_25` "Volume King" (Crush 25+ sets in one session) by setting `is_active = FALSE`. Both are STRUCTURALLY redundant with `complete_workout` (you cannot hit 15/25 sets without a workout, and `complete_workout` is always slot 1) AND lock pass/fail when the user's one safe workout for the day finishes with < N sets — the only path to completion is a second workout, which violates FE invariant 18 (no two full workouts/day). Same anti-pattern family as 20260610 (overnight-sensor passive quests) + 20260630 (relative-to-others quests): user has zero deterministic levers post-slot-1. The arbitrary 15/25 thresholds also punish good training when the user's actual program prescribes < N working sets (a 4-exercise legs day with 12 working sets is a perfectly good workout). Cleanup pass: `DELETE FROM user_daily_quests WHERE quest_key IN ('exercise_sets_15','exercise_sets_25') AND quest_date today±1d AND is_completed = FALSE` so users staring at "Set Machine — 0/15 — Not yet" right now (Joe in the screenshot) don't have to wait until midnight. Brief↔slate coordination preserved (PE invariant 25c + 25d, verified pre-ship): `Fit33/DailyBriefEngine.swift::matchQuests` `.muscleGroup` / `.noWorkoutYet` / `.streakRisk` debt → workout-class quests still resolve to `complete_workout` / `complete_program_day` / `do_friend_workout` / `workout_30_min` after retirement. Server-side Layer 8 (Debt Booster) doesn't reference `exercise_sets_*` for any debt kind. Server-side Layer 7 `v_pr_pool` for green-day PR elevation: `beat_volume_pr` + `beat_personal_record` remain (both user-bar-relative and acceptable; `exercise_sets_25` removed by the eligibility pool's `is_active = TRUE` filter). `Lift` challenge override: the existing `EXISTS (... AND is_active)` guard in `get_daily_quests` v4 line 412 makes the now-inactive override a graceful no-op — lift-challenge participants fall through to normal slate logic (slot 1 = `complete_workout` already covers it). Paired doc updates: `FITNESS_EXPERT_AGENT.md` (new invariant 20a-stuck, retired keys removed from invariant 18 hard-day fallback + invariant 19 min_workouts threshold list), `PRODUCT_ENGINEER_AGENT.md` (extended invariant 19d to add the locked-in-by-slot-1 anti-pattern as a third failure mode alongside overnight-sensor + relative-to-others), `DATA_BACKEND_AGENT.md` (invariant 31 lift→`exercise_sets_15` mapping retired with rationale), `.cursor/rules/supabase-rules.mdc` (challenge-override map updated, defense-in-depth note added to redundancy matrix). iOS code untouched (PE 19d backwards-compat — `QuestKey.exerciseSets15/25` enum cases stay, `onWorkoutCompleted::reportProgress(.exerciseSets15/25)` calls become server-side no-ops for new users but remain functional for any in-flight rows). Idempotent. |
| 133 | `20260705_daily_brief_quest_completion_link.sql`      | 🆕 Ready   | (Daily Mission Unification — Phase 6, 2026-04-27) Closes the measurement loop on the brief feature. **Filename note**: dated `20260705` because `20260704` was already taken by #131 (`20260704_dedupe_exercise_performance_history.sql`); the actual feature shipped on the same calendar day, the filename just shifts to avoid collision. Extends `daily_brief_impressions` (#129) with three additive columns: `completed_quest_keys TEXT[]` (subset of the brief's `linked_quest_keys` the user actually finished within the impression's lifetime), `completed_at TIMESTAMPTZ` (timestamp the FIRST linked quest completed — null until conversion, lets us compute brief→completion latency), `decision_signature TEXT` (short string `                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 135 | `20260707_user_daily_quests_bonus_claimed.sql`        | 🆕 Ready   | (Bug-intel 2026-04-27 — fingerprints `76860b32`, `8e0764bf`, `4729e709`, `537be0ee`, 22 occurrences, 1 user, build 1.38(59)) Adds the missing `bonus_claimed BOOLEAN NOT NULL DEFAULT FALSE` column on `user_daily_quests`. Latent schema bug since `20260324_adaptive_quest_selection.sql` — the original `daily_quests_migration.sql` `CREATE TABLE` (lines 32–52) shipped without this column, but every `get_daily_quests` overload from 2026-03-24 onward (current head: v4 from #132) declares `v_bonus_claimed BOOLEAN := FALSE` and runs `SELECT bonus_claimed INTO v_bonus_claimed FROM user_daily_quests WHERE user_id = v_user_id AND quest_date = v_today LIMIT 1;` gated on `IF v_all_complete THEN`. The gate hid the bug for 8 months because CI fixtures never completed all 3 daily quests; the moment a real user finishes their last quest of the day the next dashboard refresh raises 42703 and the slate collapses to `defaultGoals()` until midnight. `ADD COLUMN IF NOT EXISTS` only — no RPC body change required (every overload already reads + COALESCEs to FALSE). Companion `claim_quest_bonus(p_date)` RPC is intentionally NOT wired in this migration: the client-side `bonus_xp` payload is surfaced as a one-time toast, but actual XP is awarded by `update_quest_progress` on the per-quest completion path, so DEFAULT FALSE matches existing Swift behavior with no double-award risk. Idempotent (`IF NOT EXISTS`). Standalone — no dependencies on the v4 brief signals migration, safe to apply on any post-`daily_quests_migration.sql` prod state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |


**Run order**: 102 → 103 → 104 → 105 → 106 → 107 → 108 → 109 → 110 → 128 → 129 → 130 → 131 → 132 → 133 → 134 → 135.

- #134 (20260706) is standalone — touches `quest_templates.is_active` + `user_daily_quests` rows only, no RPC body changes. Depends on #132 (`get_daily_quests` v4) only insofar as v4's `is_active = TRUE` eligibility filter + `EXISTS (... AND is_active)` challenge-override guard are what make the soft-disable graceful; both have been in every `get_daily_quests` overload since #102, so #134 is safe to apply on any post-#102 prod state. Idempotent.
- #132 (20260703) MUST land BEFORE the iOS Phase 2 client deploy that packs the 4 new RPC params — defaults are nullable so the call shape works either way, but new clients calling old server would silently fall through to legacy 6-layer logic with no error (no Layer 7/8 = no Mission unification benefit).
- #133 (20260705) MUST land BEFORE the iOS Phase 6 client deploy that calls `append_brief_completed_quest`. The iOS hook is fire-and-forget so a missing RPC throws + is debug-logged, but conversion analytics will be empty until both are in place.
- #130 depends on #108's `user_quest_personalization` table + `v_user_quest_personalization_summary` view (safely re-runs the view to add `user_disabled` synthetic column). #131 has no dependencies — pure cleanup DELETE on `exercise_performance_history`. All idempotent.

**Paired code changes**:

- #102–#108 (Phase 1 — server): All seven SQL migrations land together so the RPC v3 has its full schema + cron + verification stack on day one.
- #102–#108 (Phase 2 — iOS): `Fit33/DailyQuestService.swift` — `DailyQuest` extended with `tier` / `doubleXp` / `isCustom` / `isReroll` and helper computed props (`verificationXpMultiplierLabel`, `doubleXpBadge`, `isCustomPro`, `wasRerolled`); new `QuestKey` cases for Strava PRs, wearable, friend-named templates; new nested `ActivityMixSnapshot` (computed from Core Data `Workout` + `cardio_workouts` last-28d) with `dominant`/`least`/`rpcHint`; `UserQuestContext` extended with split wearable bools, activity mix, and friend seeds; `gatherUserContext()` populates everything; new RPC params plumbed through `GetDailyQuestsParams`; new methods `onStravaActivityImported` / `onReadinessRecomputed` / `onSharedWorkoutCompleted` / `onFriendWorkoutComment` / `onFriendReactionSent` / `onTopFriendChallengeStarted` / `reroll` / `claimDoubleXpDay` / `submitCustomQuest` / `unsuppressCategory`; XP award path in `reportProgress` applies `doubleXp` multiplier when active.
- #102–#108 (Phase 2 — iOS UI): `Fit33/DailyQuestViews.swift` — verification sub-label badge ("1.5× XP — auto-tracked", "0.7× XP — honor system", "1.0× XP — social"), per-card reroll context-menu, double-XP / rerolled tags, Pro upsell footer beneath the slate (links into Insights for Pro, Upgrade CTA for Free). New file `Fit33/QuestInsightsView.swift` (~330 lines) — Pro-only sheet reading `v_user_quest_personalization_summary`, renders 28-day completion bars per category with state badges (on_fire/mixed/cold/paused), suppressed-categories list with one-tap "Resume" calling `unsuppress_quest_category`. `Fit33/StravaService.swift` — fires `DailyQuestService.shared.onStravaActivityImported()` at the end of `syncActivities`. `Fit33/ReadinessService.swift` — fires `DailyQuestService.shared.onReadinessRecomputed()` at the end of `recompute`. `Fit33/AppConfig.swift` — flips `FeatureFlags.wearableQuests = true` and adds `smartAdaptiveQuests = true` kill-switch.
- Tests: `Fit33Tests/DailyQuestPersonalizationTests.swift` — locks in ActivityMix archetype dominant/least picks (cardio-heavy / strength-heavy / walker / balanced), verification XP multiplier labels, Pro state badges, DailyQuest Codable round-trip for new fields, and QuestKey raw-value contract.
- Documentation: `PRODUCT_ENGINEER_AGENT.md` (Daily Quests v3 invariant block — tier/suppression contract, friend-name + activity-mix params), `SUPABASE_AGENT.md` (new "Daily Quests v3" section — 6-layer RPC, suppression rule, integration verification RPCs), `DATA_BACKEND_AGENT.md` (new RPC contracts, DTOs, Strava/wearable verification fanout), `FITNESS_EXPERT_AGENT.md` (note that activity-mix bias never violates the "no two full workouts/day" invariant — sneak-in always uses lower-difficulty quests, not a second prescribed workout), `INFRA_SECURITY_AGENT.md` (new tables/RPCs registered).

**Paired code changes**:

- #98 (Phase 2): `Fit33/StravaService.swift` — `StravaCardioWorkoutInsert` gains `originApp: "strava"` + `sufferScore: Int?`; new `getActivityDetailJSON(id:)` + `getActivityStreamsJSON(id:)` raw-JSON fetchers. `Fit33/StravaActivityEnricher.swift` — new sliding-window-throttled enricher (max 5 enrichments per 15-min window, respecting Strava's 200/15min API limit) that calls those two endpoints and updates the new JSONB columns. `Fit33/DashboardStravaWidget.swift` + `Fit33/StravaActivityRecapSheet.swift` — Phase 1 dashboard widget + recap sheet (gated on `StravaService.shared.isConnected` + activity within `now − 36h`). `Fit33/DashboardModels.swift` — adds `NotificationItem.stravaRecap(StravaActivity)` carousel case with persisted dismissed-IDs (`@AppStorage("dismissed_strava_recap_ids")`).
- #99 (Phase 3): `Fit33/StreakShieldService.swift` — `creditHighEffortDay(reason:)` updates `lastShieldUsedDate` so a tomorrow rest-day cannot break the streak after `suffer_score > 150` activities (does not consume a real shield). `Fit33/StravaService.swift::saveActivitiesToCloud` calls it for any today-activity with `suffer_score > 150`. `Fit33/StravaStatsCharts.swift` (new) — `StravaMileageChartWidget` (weekly mileage stacked by activity type) + `StravaPaceTrendChartWidget` (4-week rolling avg pace) wired into `WorkoutStatsView::WorkoutStatsSection`. Both gate on `StravaService.shared.isConnected` and read directly from `StravaService.shared.recentActivities` (no new RPC, no Core Data round-trip).
- #100 (Phase 3): `supabase/functions/compute-strava-insights/index.ts` (new edge function) — Deno worker that mirrors `compute-readiness-insights` shape (auth via `x-cron-key` OR service-role bearer, `EXPECTED_PROJECT_REF` JWT validation). Computes the five insight cards and upserts via `onConflict: "user_id,insight_key"`.
- #101 (Phase 5): `Fit33/StravaService.swift::mirrorTokensToSupabase` — calls `upsert_strava_tokens` RPC after `exchangeCodeForTokens` AND after `refreshAccessToken` (best-effort, never blocks iOS sync flow on failure). `supabase/functions/strava-webhook/index.ts` (new edge function) — handles Strava's GET `hub.challenge` handshake (verified via `STRAVA_VERIFY_TOKEN` secret) + POST event delivery. ACKs ≤2s then processes asynchronously: `get_user_id_for_strava_athlete` lookup → token refresh if needed (5-min skew) → `/activities/{id}?include_all_efforts=true` fetch → `cardio_workouts` upsert keyed on `(user_id, external_id)` with the same Phase 2 column set. Sends `type='strava_activity_new'` silent push (direct APNs, mirrors `wake-challenge-opponents` JWT signing) only on `aspect_type='create'` events. `Fit33/SilentPushHandler.swift` — adds `case "strava_activity_new"` → `handleStravaActivityNew(...)` calls `StravaService.shared.syncActivities(daysBack: 1)` + `UserManager.updateStreak()` under the existing 25s self-cap. Deploy with `--no-verify-jwt` (Strava sends no Supabase auth header). Required secrets: `STRAVA_CLIENT_ID`, `STRAVA_CLIENT_SECRET`, `STRAVA_VERIFY_TOKEN`, plus `APNS_KEY_ID`/`APNS_TEAM_ID`/`APNS_BUNDLE_ID`/`APNS_PRIVATE_KEY`. Register in the Edge Function Auth Registry in `INFRA_SECURITY_AGENT.md`.
- Documentation: `DATA_BACKEND_AGENT.md` (#28-strava / #28-strava-2 / #28-strava-3), `SUPABASE_AGENT.md` (#30-strava… #30-strava-5), `docs/history/DATA_BACKEND_AGENT.md` (dated narrative).

**Paired code changes**:

- #92: `Fit33/Logger.swift` — every `AppLogger.error / .warning / .critical` ships `x_file / x_line / x_function` into `dev_session_logs.entries[].extra` so the call-site patch function has data to populate.
- #93–95: `admin-cms/src/app/bug-intelligence/page.tsx` — markdown export gains call-site / severity-score / migration-resolves fields per report; `formatExportAsMarkdown()` orders bundles by `severity_score DESC` once #94 is deployed.
- #96: `supabase/functions/triage-bugs/index.ts` — `enrichFingerprint()` calls `bug_intel_find_similar_resolutions`; `EnrichedFingerprint` extended with `similar_past_fixes`; `buildUserPrompt` includes the array; `SYSTEM_PROMPT` gets a `# USING SIMILAR_PAST_FIXES` rubric telling Claude to reuse known owner/file on `match_strength=3`. CMS export adds `### 🧠 Similar past fixes (N)` block.
- #97: `admin-cms/src/app/api/admin/route.ts` — new `get_bug_intel_severity_calibration` action; `admin-cms/src/app/bug-intelligence/page.tsx` — `SeverityCalibrationPill` in header with current weights table + latest report summary on click.
- Documentation: `QUALITY_PERFORMANCE_AGENT.md` (#25q-bugintel extension + #25r/#25s), `SUPABASE_AGENT.md` (#34/#35/#36), `DATA_BACKEND_AGENT.md` (#47/#48/#49 bug-intel), `.cursor/rules/admin-cms-rules.mdc` (similar_past_fixes + SeverityCalibrationPill rules).

## Sync Triage 2026-04-27 — Index Catch-up (Phase 1 of sync_triage_2026-04-27)

> Three migrations on disk that were authored during the 2026-04-26/27 Widget Freshness Sprint + Phase 7e but never landed in the release-train ledger. Indexed here so the deploy trail is reconstructable. All three are idempotent and safe to (re-)apply on prod in the order given. The first one (`20260629`) is the **highest-priority hotfix** — every iOS challenge progress sync was failing with Postgres 42703 between when `20260626_widget_writes_kill_switch.sql` (#127) shipped and `20260629` is deployed.


| #   | File                                                            | Status                      | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | --------------------------------------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 136 | `20260628_log_community_challenge_progress_deadlock_retry.sql`  | 🆕 Ready                    | Applies the deterministic-lock-order + 40P01 retry recipe (originally from `20260618_log_challenge_progress_deadlock_retry.sql` for 1v1 + `20260524_private_challenge_deadlock_retry.sql` for private) to `log_community_challenge_progress`. Concurrent invocations for the same `(challenge_id, user_id)` — foreground `logProgress` from `Fit33/CommunityChallengeService.swift` racing the fanout from `BackgroundChallengeSyncService` after a HealthKit observer fires — were acquiring locks in opposite orders and deadlocking. Fix takes a `FOR UPDATE` row-lock on `community_challenge_participants` BEFORE touching `community_challenge_daily_progress`, then loops on `EXCEPTION WHEN deadlock_detected` for up to 3 attempts with jittered `pg_sleep(0.05 + random()*0.1)`. Resolves bug-intel fingerprints `e03ca9df` (crash) + `d29ff85a` (log). Idempotent — drops + recreates the function via `CREATE OR REPLACE`. Audit `DO $$` block at tail confirms `FOR UPDATE` + `deadlock_detected` are present in `prosrc`.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 137 | `20260629_fix_log_challenge_progress_drop_last_progress_at.sql` | 🚨 **Hotfix — deploy ASAP** | Hot-fixes `log_challenge_progress` (1v1) by removing the bogus `last_progress_at = NOW()` write that landed in #127's recreation of the function. The column doesn't exist on `challenge_participants` — the canonical "last progress timestamp" is computed dynamically from `challenge_daily_progress.updated_at` and surfaced via `my_last_progress_at` / `opponent_last_progress_at` on `get_active_challenges` (#122). Effect of the regression: every challenge progress sync on builds 1.38(57) and (58) raised `42703 column "last_progress_at" of relation "challenge_participants" does not exist`, the iOS retry loop logged it as `[CHALLENGE] logProgress failed (attempt 1/3, source: fit33)`, and the home-screen widget + dashboard challenge cards stopped getting fresh server-side numbers. Resolves fingerprints `3de7fbe4` (challenge progress sync 42703 crash), `d441ebc8` (daily quests sig mismatch — 1.37 cohort noise that surfaced through the same path), `80234a6b` (steps not resetting at midnight — paired iOS fix in `Fit33/HealthKitManager.swift`). Preserves the #127 widget kill-switch (silent-success on `p_source = 'widget'` when `internal_config.widget_writes_enabled = false`) AND the #117/`20260618` deterministic-lock-order + deadlock-retry recipe. Signature unchanged → no Swift recompile required. Trailing audit `DO $$` block fail-louds if `last_progress_at\s*[:=]` regex still matches `prosrc`. Idempotent. |
| 138 | `20260630_morning_kickstart_and_silent_push_throttle.sql`       | 🆕 Ready                    | Phase 7e widget freshness — three server-side fixes for "silent pushes are firing at 3 AM but no morning steps land" diagnostic from 2026-04-27. (A) Slows the `wake-stale-challenge-opponents` cron 30min → 60min so the silent-push budget per user (Apple's ~2-3/hr per app) isn't exceeded by the combined existing wake + Phase 7b `progress_update` pushes. (B) Adds a per-user 23:00–06:00 local-time skip filter to the cron's recipient resolution (helper RPC `is_in_local_quiet_hours(p_user_id, p_now_utc)` reads `user_notification_preferences.timezone`) so we stop wasting wake budget on locked sleeping phones. (C) New `enqueue_morning_kickstart_nudges()` SECURITY DEFINER service-role RPC + pg_cron schedule (`*/15 * * * *`) that fires a VISIBLE push at user's local 07:30 (±15 min) when they're in an active 1v1 / private / community challenge AND have not pushed `challenge_daily_progress` today (in their local TZ) AND haven't been morning-kickstarted today already AND have push notifications enabled. The push uses the new `morning_kickstart` notification type; iOS routes it in `Fit33/NotificationManager.swift` (companion code change, not in this migration) to a deep-link that opens the dashboard with the active challenge cards visible. Idempotent — uses `cron.unschedule` before re-scheduling, `INSERT ... ON CONFLICT DO NOTHING` for the per-day kickstart-stamp table.                                       |


**Run order**: 136 → 137 → 138. All standalone, all idempotent. **#137 is the priority** — apply it first if Phase 0 query (a) returns `still_broken=true`, then verify the same query returns `still_broken=false` before continuing. #136 has no overlap with #137 (different RPCs); #138 is purely additive (new RPC + cron + helper). Verify #137 post-deploy by checking that the next `[CHALLENGE] logProgress` log line on iOS reads success rather than `42703`, and that opponent step values on the home-screen widget start updating again.

**Paired code changes (NOT migrations — pure iOS, already shipped)**:

- #137 (companion): `Fit33/HealthKitManager.swift` — day-boundary step cache gate from commit `a1b10c0` (drops a stale day-of-yesterday cached step value on first read after midnight). Server-side hotfix is the cause of the visible bug; the iOS gate is the matching half of the "steps not resetting at midnight" symptom.
- #138 (companion): `Fit33/NotificationManager.swift` — adds `case "morning_kickstart"` deep-link handler. `Fit33/BackgroundChallengeSyncService.swift` — Phase 7e throttle ladder retune (writer 60s → 30s widget extension, edge function 60s → 20s `progress_update`, reader 8s → 4s coalesce window). See PE invariant 50a for the documented end-to-end ladder.

## Sync Triage 2026-04-28 — Social Sync Regression Fix (Layer C)

> One migration that closes the receiver-side gap on the realtime publication for friend requests, shared workouts, and every other social-realtime table. Companion to iOS Layers A + B (`Fit33/Fit33App.swift` foreground social re-sync + `Fit33/RealtimeService.swift` `forceReconnectIfStale()`). Resolves the user-reported regression where push notifications for friend requests / 1v1 invites / private invites fired correctly but the corresponding cards never appeared on the receiver's dashboard. Idempotent — safe to (re-)run on prod.


| #   | File                                             | Status                  | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ------------------------------------------------ | ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 139 | `20260708_realtime_social_publication_audit.sql` | ✅ Deployed (2026-04-28) | Reasserts `supabase_realtime` publication membership AND `REPLICA IDENTITY FULL` on every table the iOS `RealtimeService` subscribes to (16 tables across friend, challenge, private, community, activity-feed, privacy-events, exercises). The audit revealed that `**friendships` and `shared_workouts` were NEVER explicitly added to the publication via a migration file** — the repo migration history only adds `friend_activity_feed` (#20260307), `challenge_*` (#112 / #20260612), `community_*` (`fix_community_realtime_v2.sql`), `private_challenge_*` (`fix_private_realtime_publication.sql` + #20260426), `privacy_change_events` (#20260330), `exercises` (#20260420). Friendships + shared_workouts may have been added directly via the Supabase dashboard at some point, but if any prior maintenance recreated the publication those channels would silently go dark — exactly the symptom reported (push fires, no card). Each `ALTER PUBLICATION` is wrapped in a `DO $$ ... EXCEPTION WHEN duplicate_object` block so re-running on a healthy prod is a no-op. Trailing fail-loud audit `DO $$` block enumerates `pg_publication_tables` + `pg_class.relreplident` per table; raises `EXCEPTION` if any membership is missing or any replica identity isn't `'f'` (FULL). Resolves the same fingerprint family as `721fe5d6` (HIGH, 2026-04-27 — friend-request push without dashboard card) plus the 2026-04-28 user report. |


**Paired iOS code changes (NOT migrations — shipped in the same commit as #139)**:

- #139 (companion A): `Fit33/Fit33App.swift::scenePhase == .active` — replaces the `if !RealtimeService.shared.isConnected { ... }` Phase 3+4 conditional retry block (the original 2026-04-27 fix) with an unconditional 9-RPC social re-sync. The previous gate was a permanent no-op for returning users because `isConnected` (a Swift `@Published` Bool) stays `true` after iOS suspends + drops the underlying WebSocket — there's no socket-close callback wired to it, so neither realtime nor the fallback was rescuing the carousel. The 9 fetches mirror every `@Published` array `DashboardModels.DashboardNotificationCarousel` reads (friend requests, 1v1 invites, 1v1 sent, group invites, private invites, private myChallenges, community myChallenges, activity feed, received workouts). All 9 run in parallel via `async let`; `RequestCoalescer` + per-service `isAuthenticated` guards make redundant fetches free.
- #139 (companion B): `Fit33/RealtimeService.swift` — adds `forceReconnectIfStale()` helper + softens the 10s `connectThrottleInterval` so a stale channel (last realtime event > 30s old) bypasses the throttle. Companion A calls `forceReconnectIfStale()` instead of the previous `if !isConnected { connect() }` gate. Also expands the post-connect canonical resync from 4 surfaces to all 9, so `connect()` becomes the single canonical "everything social, refreshed" entry point.

**Deploy order**: standalone — no dependency on #136–138. Recommended sequence post-iOS-build:

1. Apply #139 in Supabase SQL Editor (production).
2. Verify trailing `DO $$` audit emits the `✅ All 16 social-realtime tables verified...` notice.
3. Ship companion iOS code via TestFlight.
4. Reproduce the original symptom: User A sends User B a friend request → User B sees the card within 5s if foregrounded, within 1s of next foreground if backgrounded.
5. Optional manual post-deploy verification queries are inlined at the bottom of `20260708_realtime_social_publication_audit.sql`.

## Sync Triage 2026-04-28 — Onboarding Cleanup (1h Window + FK Fix)

> User report (2026-04-28): "I deleted the app before completing onboarding — the temp account creation should disappear — this was working before". Root cause: the cleanup function at `supabase/cleanup_incomplete_onboarding.sql` is byte-identical to v1.36 (verified via `git show 8816661`). What changed is HOW FAR users get into onboarding before abandoning. The original gate required `username IS NULL AND birthday IS NULL AND height_cm IS NULL AND weight_kg IS NULL` — a strict "barely-started" check that excluded any profile that had progressed past the `body` step (step 5). Users who reached the `contacts` step (step 13) and bailed had all four fields populated, so the cleanup permanently skipped them. This migration loosens the gate to "any incomplete profile older than 1 hour" and fixes the `private_challenge_members.invited_by` FK that was blocking manual deletes.


| #   | File                                                | Status                  | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | --------------------------------------------------- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 140 | `20260709_loosen_incomplete_onboarding_cleanup.sql` | ✅ Deployed (2026-04-28) | Three changes in one migration: (a) `private_challenge_members.invited_by` FK becomes `ON DELETE SET NULL` (was unset, blocked manual user deletes with 23503; we hit this twice in the 2026-04-28 session); (b) `cleanup_incomplete_onboarding_profiles()` is rewritten — drops the AND-chain on partial data, drops the 30-min window down to 1h, adds per-row try/except so one stuck row doesn't abort the cron batch; (c) one-time backfill runs the new cleanup once at deploy time so existing orphans (e.g. the `joereedis@icloud.com` test row from the 2026-04-28 session) are reaped immediately rather than waiting up to 10 min for the next cron tick. The job name + cron cadence (`*/10 * * * *`) stay identical so no follow-up reschedule is needed. Trailing audit `DO $$` block raises a WARNING (not EXCEPTION — partial cleanup still useful) if any incomplete profiles >1h survive, listing up to 5 blocked ids via `string_agg(id::text, ', ' ORDER BY created_at)` so we can audit additional FKs in a follow-up. **Authoring note**: the first deploy attempt failed with `42883: function min(uuid) does not exist` — Postgres has no min/max aggregate for UUID. Cast to text or use `string_agg` instead; never `MIN(uuid)`/`MAX(uuid)` in audit blocks. Idempotent. |


**Behavior contract after this lands** (matches user requirements stated 2026-04-28 8:29 PM):

- **"Leave the app and come back is fine"** → iOS `UserDefaults` checkpoint preserves entered data through normal backgrounding; the auth-gate restoration fix at `Fit33/NewOnboardingView.swift::onAppear` (shipped earlier in the same session) ensures the checkpoint is restored even when the user has an authenticated session mid-onboarding.
- **"Don't complete in one go" → start over** → 1h cleanup window: if user signs up at 9:00 AM and doesn't finish by 10:00 AM, server reaps their `user_profiles` + `auth.users` rows on the next 10-min cron tick.
- **"Force quit or delete the app" → start over** → same 1h cleanup. iOS UserDefaults is wiped on uninstall anyway, so reinstall always starts fresh; once the cloud row is reaped, the email is re-registerable.

**Deploy order**: standalone — no dependency on prior migrations. Already deployed on 2026-04-28 via Supabase SQL Editor; audit emitted no warning so all pre-existing >1h-old incomplete profiles were reaped on the backfill pass. Manual verification queries are inlined at the bottom of `20260709_loosen_incomplete_onboarding_cleanup.sql`.

**No paired iOS changes needed** — the iOS-side checkpoint already saves on every step navigation, and the post-cleanup re-signup path already exists in `Fit33/NewOnboardingView+Auth.swift` (`signUpOrRecoverExistingAccount`). After cleanup, the user can re-register the same email cleanly.

---

## Daily Goals Coherence 2026-04-28 — Retire `respect_red_recovery`

> User report (2026-04-28): "daily goals contradict — can have a workout on a rest day". Screenshot showed the slate serving BOTH `complete_workout` "Crush a Workout" (✓ Late night Core Focus workout) AND `respect_red_recovery` "Smart Rest" (0/1 — "Chose mobility on a red recovery day"). The two quests are mutually exclusive by construction: the verifier in `verify_wearable_quests_for_today` requires `NOT EXISTS (SELECT 1 FROM workouts WHERE date = today)` for `respect_red_recovery` to complete, while `complete_workout` requires `EXISTS workouts WHERE date = today`. Once slot 1 finishes, `respect_red_recovery` is permanently locked at 0/1 with zero recoverable path (FE invariant 18: no two full workouts/day). Same anti-pattern family as 20260706 retired `exercise_sets_15` / `exercise_sets_25`, but worse: `exercise_sets_15` *could* complete on a workout that happened to hit 15 sets; `respect_red_recovery` is impossible by design. Eleventh retirement on the PE 19d watch-list — extends the rule from "no quest gated on slot-1 completion" to "no quest mutually exclusive with slot-1 completion."


| #   | File                                             | Status                  | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | ------------------------------------------------ | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 141 | `20260710_retire_respect_red_recovery_quest.sql` | ✅ Deployed (2026-04-28) | (User feedback 2026-04-28 — Daily Goals dashboard screenshot, eleventh PE 19d retirement) Soft-disables `respect_red_recovery` "Smart Rest" (Chose mobility on a red recovery day) by setting `is_active = FALSE`. The verifier (`supabase/bundles/bundle_c_smart_adaptive_quests.sql` lines 2868-2889) requires (a) red readiness band, (b) recovery-flavored cardio logged today, AND (c) `NOT EXISTS workouts WHERE date = today` — the `NOT EXISTS workouts` clause makes the quest mutually exclusive with `complete_workout` (slot 1 anchor) by construction. Same anti-pattern family as 20260706 (`exercise_sets_15` / `exercise_sets_25`) but worse: literally impossible to complete on a day the user follows slot 1's instruction. Replacement is already shipped: `active_recovery_logged` "Active Recovery: Log 15 min walk/yoga/stretch" (from 20260610) has no band gate AND no exclusion of strength workouts, so a user can complete BOTH `complete_workout` AND `active_recovery_logged` on the same day with zero contradiction. Layer 7's `v_recovery_pool` in `get_daily_quests` v4 already elevates `active_recovery_logged` on red days — this retirement does NOT reduce red-day recovery coverage. Cleanup pass: `DELETE FROM user_daily_quests WHERE quest_key = 'respect_red_recovery' AND is_completed = FALSE AND quest_date BETWEEN today-1 AND today+1` so users (e.g. Joe in the screenshot) aren't stuck with the broken quest until midnight; completed + historical rows preserved for streak/audit. iOS notes: `Fit33/DailyQuestService.swift` line 2074 keeps the key in the `wearableKeys` set for backwards-compat (PE 19d) — the verifier short-circuits cheaply via `IF v_readiness IS NOT NULL` when no in-flight rows exist; cleanup is doc-debt. No `QuestKey` enum case to remove (raw-string only). `DailyBriefEngine.matchQuests` does not reference `respect_red_recovery` in any `DebtKind` array — zero brief↔slate coordination break. Idempotent. |


**Behavior contract after this lands**:

- Red-recovery user gets the slate `[complete_workout, ?, active_recovery_logged]` instead of `[complete_workout, ?, respect_red_recovery]`. Both workout-class quests can complete in the same day.
- The "Smart Rest" copy never reappears on a slate that also says "Crush a Workout" — voice consistency restored.
- Existing in-flight `respect_red_recovery` rows get cleaned up at deploy time so users see the new slate on their next refresh.

**Deploy order**: standalone — no dependency on prior migrations. Re-running flips `is_active = FALSE` (no-op) and the cleanup `DELETE … WHERE is_completed = FALSE` is idempotent.

**No paired iOS ship change required** — the eligibility pool's `WHERE qt.is_active = TRUE` clause naturally drops the template once the migration runs. Existing iOS `wearableKeys` reference is preserved for backwards-compat with already-completed historical rows.

## Realtime Widget Server Pull 2026-04-28 — Phase 8a (Caller Profile Photo URL)

> User report (2026-04-28): "make sure the homescreen widget pulls both user profile photo" — the home-screen `ActiveChallengeWidget` was rendering gradient-initials placeholders for the caller's own avatar AND the opponent's avatar whenever the iPhone main app hadn't been foregrounded recently (e.g. user installed the widget before ever opening Fit33, OR the device had been killed for hours). The opponent URL had been on the wire since Phase 2a (`20260620_…`); the caller's was completely absent because the RPC computed every other "my_*" column from `challenge_participants` and never joined back to `user_profiles` for the caller. Closes Phase 8a of the Realtime Widget Server Pull sprint by extending the RPC and shipping a new widget-side photo downloader (`RunningActivityWidget/WidgetPhotoFetcher.swift`).


| #   | File                                                  | Status                  | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | ----------------------------------------------------- | ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 142 | `20260711_get_active_challenges_my_profile_photo.sql` | ✅ Deployed (2026-04-28) | Adds a single `my_profile_photo_url TEXT` column to the `RETURNS TABLE` of `get_active_challenges(p_timezone TEXT)`. Body now SELECTs `profile_photo_url` from `user_profiles` for `auth.uid()` into `v_my_photo_url` ONCE upfront (cheap PK lookup) and repeats it on every result row — same TEXT pointer, no per-row cost. Privacy: deliberately does NOT honor `privacy_hide_photo` for the caller's own photo (the flag is for opponents viewing this user, not for the user's own widget — mirrors the in-app `ProfileView` posture). DROP-all-overloads loop runs first per Supabase invariant 12 against the 1-arg shape from `20260620_…`. Trailing audit `DO $$` block fails loud if (a) more than one definition of `get_active_challenges` exists, (b) the new column is missing from `pg_get_function_result(oid)`, (c) `v_my_photo_url` is missing from `prosrc`, OR (d) the Phase 2a `my_last_progress_at` / `opponent_last_progress_at` columns regressed (additive-only contract). Idempotent. |


**Paired iOS code changes (NOT migrations — shipped in the same commit as #142)**:

- #142 (companion A): `RunningActivityWidget/WidgetSupabaseFetcher.swift` — adds `my_profile_photo_url` to the private `GetActiveChallengesRow` decoder (Optional `String?` for forward-compat with payloads missing the key), wraps the public return in a new `ActiveChallengesPullResult` struct that surfaces both the `[WidgetActiveChallenge]` list AND the caller's `myProfilePhotoURL` lifted from the first row. Single call site (`ActiveChallengeProvider.pullAndMergeIfPossible`) updated to consume the new shape.
- #142 (companion B): `RunningActivityWidget/WidgetPhotoFetcher.swift` (new file) — owns photo hydration. `ensureCached(userPhotoURL:opponents:perImageTimeout:)` skips the user photo when `widget_user_photo.jpg` already exists in the App Group container (bridge already wrote it), then downloads remaining opponent photos in a `withTaskGroup` (parallel, deduped by opponentId). Resize parity with `Fit33/ActiveChallengeWidgetBridge.swift`: 240pt long side, JPEG @0.8 quality, written atomically. Filename contract: `widget_user_photo.jpg` / `widget_opponent_<opponentId>.jpg` — IDENTICAL to the bridge so the widget render path's filename-based lookup picks up either writer transparently. Returns a `WrittenPhotos { didWriteUserPhoto, didWriteOpponentByID }` ledger so the caller can stamp flags.
- #142 (companion C): `RunningActivityWidget/ActiveChallengeWidget.swift` — `pullAndMergeIfPossible` now (a) consumes `ActiveChallengesPullResult`, (b) builds a `[OpponentInput]` from `fresh.opponentPhotoUrl` per challenge, (c) `await`s `WidgetPhotoFetcher.ensureCached(...)` AFTER `mergeFreshWithCache` and BEFORE the HK overlay, (d) calls a new private `applyPhotoFlagUpdates(_:written:)` helper that stamps `hasUserPhoto` / `hasOpponentPhoto = true` on each merged row when this pass wrote a fresh avatar. The render path resolves photos by filename existence, so the flags are bookkeeping only — kept honest so the bridge's next `publish()` sees a coherent state.

**Behavior contract after this lands**:

- Widget on cold-install (user added widget pre-app-open) renders the user's actual avatar within the first timeline tick (10–20 min) instead of gradient-initials forever.
- Widget after a long background period (iPhone killed for hours) re-hydrates avatars on its next pull without waiting for the main app to foreground.
- Once the bridge runs (next main-app foreground), it overwrites the same files from its in-memory caches — no race, no flicker.
- RPC payload size grows by exactly one TEXT pointer per row (~50–100 bytes).

**Deploy order**: standalone — no dependency on prior migrations except the existing `get_active_challenges` (`20260620_…`). Re-running re-creates the function with the same shape (idempotent).

## Personalized Insights ON CONFLICT Hotfix 2026-04-29 — Phase 1 (Non-Partial Unique Index)

> Bug-intel cluster `4922971b` / `8fb2f8b3`: every call to `compute-readiness-insights`, `compute-strava-insights`, and the on-device `PersonalizedInsightsService.upsertCorrelationInsight(...)` returned PG 42P10 ("there is no unique or exclusion constraint matching the ON CONFLICT specification"). The Phase-2 wearable migration (`20260507_personalized_insights_wearable.sql`) created a PARTIAL unique index `idx_personalized_insights_user_key … WHERE insight_key IS NOT NULL`. PostgREST's `onConflict` parameter generates `ON CONFLICT (user_id, insight_key)` with no `WHERE` clause, so PostgreSQL can't match the partial index. Identical class of bug as the already-fixed `cardio_workouts` constraint.


| #   | File                                                    | Status                  | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ------------------------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 143 | `20260712_personalized_insights_non_partial_unique.sql` | ✅ Deployed (2026-04-29) | Drops the legacy PARTIAL index `idx_personalized_insights_user_key`, defensively dedups any rows where `(user_id, insight_key)` is non-null and duplicated (keeps newest `id` per pair), then re-creates the index WITHOUT the `WHERE insight_key IS NOT NULL` predicate. PostgREST's `onConflict: "user_id,insight_key"` now matches directly. NULLS DISTINCT (PG default) preserves the existing append-only behavior for legacy `insight_key IS NULL` on-device insights — multiple `(user_id, NULL)` rows still coexist. Trailing audit `DO $$` block fails loud if the rebuilt index still carries a `pg_get_expr(indpred, indrelid)` predicate. Idempotent. No paired iOS / edge-function ship change required — all three call sites (`Fit33/PersonalizedInsightsService.swift:1191`, `supabase/functions/compute-readiness-insights/index.ts:581`, `supabase/functions/compute-strava-insights/index.ts:452`) already pass the correct `onConflict` string. |


**Behavior contract after this lands**:

- `compute-readiness-insights` cron (03:30 UTC) and `compute-strava-insights` writes succeed instead of returning 42P10. The 14-day insights view (`v_user_wearable_insights`) starts populating again for active users.
- On-device `PersonalizedInsightsService.upsertCorrelationInsight(...)` no longer surfaces `NetworkErrorClassifier` server-error logs in production crash reports.
- Legacy on-device insights (`insight_key IS NULL`) remain append-only — they were never on the upsert path.

**Deploy order**: standalone — supersedes the partial-index half of `20260507_personalized_insights_wearable.sql` (#69 in the v2-pre-cleanup era). Re-running flips the dedup query to a no-op and re-creates the same non-partial index shape (idempotent).

## Bug-Intel Classifier Phase 12 — Transient Network / OS Denylist Expansion (2026-04-29)

> 2026-04-29T04:13 export shows three classes still regenerating CRITICAL fingerprints despite the on-device `NetworkErrorClassifier`: `BGTaskSchedulerErrorDomain` code 1 (BGTaskSchedulerError.unavailable — Low Power Mode / Background App Refresh OFF), `NSURLErrorDomain` -1017 (cannotParseResponse — Cloudflare flap returning truncated JSON / HTML), and lingering -1001 (timedOut) noise that the 20260516 `soft` tier let through. All three are confirmed transient; retry queue (`CloudSyncRetryQueue`) recovers automatically. Phase 12 ships a paired denylist: server-side `bug_intel_noise_filter` rows + on-device classifier extensions so new occurrences never reach the rollup AND existing fingerprints auto-resolve.


| #   | File                                                | Status                  | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | --------------------------------------------------- | ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 144 | `20260713_bug_intel_classifier_denylist_expand.sql` | ✅ Deployed (2026-04-29) | Seeds two new `bug_intel_noise_filter` rows (`bgtask_scheduler_unavailable` matched by `message_pattern ~ 'BGTaskSchedulerErrorDomain.*Code=1'`, `nsurl_cannot_parse_response` matched by `nsurl_code = -1017`) on `tier='hard'`. UPDATE-flips the existing `nsurl_timeout_short` (NSURL -1001) row from `soft` → `hard` based on three months of zero real-bug signal — per-op regression sensitivity now lives in `bug_intel_severity_weights.class_amp_transient` instead. Backfill auto-resolves matching open fingerprints with `auto_resolved_reason = 'noise_filter_expanded'` (mirrors the 20260517 / 20260519 convention) and merges paired pending `bug_intelligence_reports`. Trailing audit `DO $$` block fails loud if any of the three expected post-state invariants are missing (BGTask filter present + hard, -1017 filter present + hard, -1001 row on hard). Idempotent — re-running is a no-op via `ON CONFLICT (name) DO NOTHING` plus `WHERE tier <> 'hard'` on the flip. |


**Paired iOS code change (NOT a migration — shipped in the same commit as #144)**:

- `Fit33/NetworkErrorClassifier.swift` — `classify(_:)` now returns `.transientNetwork` for (a) `NSURLErrorDomain` code `NSURLErrorCannotParseResponse` (-1017) by adding it to the existing NSURL transient `switch` and (b) any error in domain `"BGTaskSchedulerErrorDomain"` with `code == 1` via a separate guard after the NSURL block. Both routes log at `transientLevel: .warning` instead of `.error`, so even legacy `AppLogger.error`-style call sites that the classifier_lint backlog hasn't migrated yet stop creating new fingerprints once they're rerouted through the classifier.

**Behavior contract after this lands**:

- Background-task scheduler errors during Low Power Mode / Settings-disabled Background App Refresh stop generating bug-intelligence fingerprints. The `BackgroundChallengeSyncService.scheduleNext()` log line drops to `.warning`.
- Cloudflare flap-induced `cannotParseResponse` (-1017) errors stop fingerprinting — they were the long-tail companion to the existing 502/503 HTML matchers.
- Existing -1001 / -1017 / BGTask CRITICAL fingerprints auto-resolve with `auto_resolved_reason = 'noise_filter_expanded'` on the next rollup.

**Deploy order**: standalone — depends only on `bug_intel_noise_filter` table from `20260516_bug_intel_structural_fingerprint.sql`. Re-running re-flips the same idempotent state (UPDATE narrowed by `WHERE tier <> 'hard'`, INSERT guarded by `ON CONFLICT (name) DO NOTHING`).

---

## Bug-Intel Pipeline Phase 13 — Collapse + Classify (2026-04-29)

> 2026-04-29T04:13 export had 30 reports for ~17 underlying root causes. Five concrete pipeline failures observed: (1) crash↔log twin fingerprints for the same call site (5+ pairs), (2) `class=unknown` on 21/30 reports despite the message body literally saying `code: Optional("42703")`, (3) same 3 generic `match_strength=1` "Similar past fixes" repeated on every report (workout-cancel / HK-sync / password-rate-limit), (4) already-fixed bugs from 20260628 / 20260629 / 20260707 still listed as PENDING because `Resolves:` directives were either missing (20260707) or never replayed post-deploy (20260628 / 20260629), (5) pain-points + TODOs duplicated 4× across the bonus_claimed quartet. Phase 13 collapses, classifies, and replays.


| #   | File                                                   | Status                              | What it does                               |
| --- | ------------------------------------------------------ | ----------------------------------- | ------------------------------------------ |
| 145 | `20260714_bug_intel_phase13_collapse_and_classify.sql` | 🆕 Ready (2026-04-29) — deploy ASAP | Adds `root_cause_fingerprint TEXT = md5(op |


**Paired CMS code change (NOT a migration — shipped in the same commit as #145)**:

- `admin-cms/src/app/bug-intelligence/page.tsx` — adds `root_cause_fingerprint?: string | null` to BOTH `Fingerprint` (full table type) and `ExportFingerprint` (export type). `formatExportAsMarkdown()` dedup key priority changes to `root_cause_fingerprint > structural_fingerprint > raw fingerprint > orphan` — collapses crash↔log twins into one canonical report. New per-cluster `keySource` discriminator + `crossSourceTwins` counter surface in the "Collapsed: N duplicate triage rows merged" header line ("· N crash↔log twins merged via root_cause_fingerprint"). New `variantFingerprints` map renders **Collapsed siblings** (N · crash:1 · log:1) line in each canonical report body so reviewers see which hashes were merged. Root-cause-fields bullet now also surfaces `root_cause=<8-char-hash>` next to `struct=<…>` when present. New **Themes** block (after the "By agent owner" summary) dedupes `pain_point_candidate` + `suggested_todo` across canonical bundles — anything matching ≥2 distinct fingerprints (case-insensitive whitespace-collapsed match) surfaces ONCE with the list of cluster fingerprints. Cuts cross-fingerprint repetition where the bonus_claimed quartet had the same "Daily quests fail to load" pain point repeated 4×.

**Behavior contract after this lands**:

- Crash + log fingerprints of the same call site (e.g. HK sleep `14b58e6a` + `9dd27552`) collapse into ONE export report. The dropped FP is surfaced as a "Collapsed sibling" line in the canonical's body.
- `class=unknown` count drops sharply for any fingerprint whose `sample_message` contains a SQLSTATE code in any of the four supported formats. The 04-29 export's 21 unknowns drain to whatever residual count is from messages that genuinely lack a SQLSTATE.
- "Similar past fixes" sections on `class=unknown` reports either become empty (no match strength ≥ 2) or only show structural / op+class matches — no more workout-cancel / HK-sync / password-rate-limit boilerplate.
- The 8 fingerprints already fixed by 20260628 / 20260629 / 20260707 immediately flip to `status=resolved` with `auto_resolved_reason = 'migration_resolved:<id>'`. Future twin-FPs auto-drain via `bug_intel_resolve_by_root_cause()` on the 00:45 UTC tick without anyone needing to file `Resolves:` directives by hand.
- Repeated pain points / TODOs across distinct root causes surface ONCE in the "Themes" block at the top of the export instead of N× in the per-report bodies.

**Deploy order**: standalone — depends on `bug_intelligence_fingerprints` (#41 root) + `bug_intel_resolved_history` (#96, deployed) + `mark_fingerprints_resolved_by_migration` (#95, deployed) + `bug_intel_patch_callsite_columns` pattern (#92, deployed). Re-running is idempotent across all paths: `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`, `CREATE OR REPLACE FUNCTION`, `CREATE INDEX IF NOT EXISTS`, `mark_fingerprints_resolved_by_migration` (no-op on terminal FPs), `cron.unschedule` before `cron.schedule`. The G1 backfill UPDATE only touches rows where `error_class IS NULL OR 'unknown'` AND extractor returns non-NULL.

---


| #   | File                                   | Status                | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | -------------------------------------- | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 146 | `20260715_league_percentage_zones.sql` | 🆕 Ready (2026-04-29) | Sprint 1 of the Weekly League Redesign Plan (workstream A1). Adds `league_tiers.promotion_pct` + `relegation_pct` numeric columns and seeds them at the calibrated per-tier values (Bronze 17%/0%, Silver 17%/17%, Gold 17%/17%, Platinum 15%/18%, Diamond 12%/20%, Elite 10%/25%, Verified 0%/20%). Adds `calc_league_zone_count(member_count, pct)` IMMUTABLE helper that returns `GREATEST(1, LEAST(floor(N×pct), floor(N/2)-1))` — clamped to 0 when `pct=0` (Bronze relegate / Verified promote) and capped at half-1 so promotion + relegation zones never overlap. Rewrites `process_past_league_weeks()` to compute zone sizes via the helper at rollup. Rewrites `get_or_join_weekly_league(UUID)` and `get_league_leaderboard(UUID, UUID)` so the JSON's `promotion_count` / `relegation_count` are derived live from `member_count × pct` instead of the legacy fixed integer columns — fixes the broken edge case where a privacy-filtered group of 8 with the old "top 5 promote" produced 63% promotion. Swift `LeagueStanding.promotionCount` / `relegationCount` decoders are byte-compatible (output JSON shape unchanged). Old `promotion_count` / `relegation_count` integer columns kept for now; a follow-up will drop them after rollouts settle. Idempotent — `ADD COLUMN IF NOT EXISTS`, `DROP FUNCTION IF EXISTS` before each `CREATE OR REPLACE`, all percentage UPDATEs are deterministic. |


**Behavior contract after this lands**:

- A privacy-filtered Silver group of 8 now promotes 1 (was 5 — broken), relegates 1 (was 5 — broken).
- A Diamond group of 25 promotes 3 (was 3), relegates 5 (was 5) — same as before, math validates.
- Bronze never relegates; Verified never promotes — invariant preserved.
- Promotion + relegation zones never overlap (always ≥1 stable middle for groups ≥ 4).
- Existing IDOR / privacy / Bronze-reshuffle / friend-overlap / blocks / hidden-users behavior is unchanged.

| 147 | `20260716_league_sprint2_stakes.sql` | 🆕 Ready (2026-04-29) | Sprint 2 of the Weekly League Redesign Plan (workstreams A2-A4 + C3). Stakes & Drama. Adds six columns to `user_league_tier`: `top3_streak`, `shield_available`, `verified_relegation_streak`, `pending_league_points`, `next_week_starting_points`, `crown_until`. Adds four flag columns to `league_history`: `was_stand_out`, `was_crown`, `was_shielded`, `was_bounceback` so the iOS celebration overlay can dispatch `TierPromotionEvent.Variant` without re-running the rollup logic. Rewrites `process_past_league_weeks()` to layer six new mechanics ON TOP of Sprint 1's percentage zones: (1) **Stand-Out skip-tier** when `top3_streak >= 3` AND in the promotion zone (skips one tier, capped before Verified); (2) **First-Strike Shield** that suppresses the first relegation hit in any tier and burns silently — granted on every promotion; (3) **Verified Two-Strike** rule that requires two consecutive bottom-3 finishes at rank 7 to relegate (one strike doesn't drop you out of Verified); (4) **Crown of the Week** + **Head-Start Bonus** for rank-1 finishers (gold cosmetic ring for 7 days + carry-forward +20 League Points to next week's row); (5) **Bounceback** detection — last week was_relegated AND this week was_promoted; (6) per-user top3 / verified streak bookkeeping. Adds `drain_pending_into_league_member(UUID)` helper that returns `pending_league_points + next_week_starting_points` and zeroes both — both `auto_place_all_league_members()` and the lazy-placement branch of `get_or_join_weekly_league(UUID)` call it at insert time so freshly-placed rows START with the carried-forward total instead of `points = 0`. Rewrites `add_league_points(UUID, INTEGER, TEXT)` so pre-placement points (no membership for current week) are routed to `user_league_tier.pending_league_points` instead of returning `no_membership` — also recognizes the new `cardio_session` source for the `workouts_completed` counter (parity with strength workouts). Rewrites `get_or_join_weekly_league(UUID)` to surface `pending_league_points`, `shield_available`, `top3_streak`, `crown_until` in both the `not_placed` and the placed JSON; the leaderboard sub-select adds `has_crown` (true iff the row's `crown_until > now()`) so the iOS leaderboard can render the gold ring. Idempotent — `ADD COLUMN IF NOT EXISTS`, `DROP FUNCTION IF EXISTS` before each `CREATE OR REPLACE`, no destructive ALTERs. Paired iOS commit decodes the new JSON fields via additive `LeagueStanding` Codable changes — old clients keep working since all new fields are optional. |

**Behavior contract after this lands**:

- Rank 1 finisher gets a 7-day gold-ring cosmetic AND starts next week with +20 League Points already in the bucket.
- A user with three top-3 finishes in a row, when promoting on the third, climbs TWO tiers in one rollup (Stand-Out). Capped before Verified — no Stand-Out from Elite into Verified to avoid 1-week speedruns.
- The first time a user would relegate from a tier, their shield burns and they hold instead. Shield is re-granted on the next tier-up. No weekly grants to non-promoting users.
- At Verified (rank 7): one bottom-3 finish does nothing; two in a row drop you to Elite.
- Pre-placement League Points (won via workouts before the user's first Monday) accumulate in `user_league_tier.pending_league_points` and credit on Monday placement — no more "spent the week earning points that didn't count".
- Bronze (rank 1) still never relegates; Verified (rank 7) still never promotes; promotion + relegation zones still never overlap.
- All Sprint 1 IDOR / privacy / friend-overlap / blocks / Bronze-reshuffle behavior is preserved unchanged.

| 148 | `20260717_league_sprint3_caps_peak_day.sql` | 🆕 Ready (2026-04-29) | Sprint 3 of the Weekly League Redesign Plan (workstreams A5 + C2). Engagement & Polish. Adds `user_league_tier.peak_day_iso` (NULL or 1-7 ISO weekday) — League Points earned on this weekday are multiplied 3× by `add_league_points`. Re-randomized every Monday by the new `refresh_user_peak_days()` SECURITY DEFINER function; the composite cron entrypoint `auto_place_all_league_members_with_peakdays()` chains `refresh_user_peak_days()` → `auto_place_all_league_members()` so a single Monday job does both (switch the pg_cron job to this new function name to enable the rotation — Sprint 2's entrypoint keeps working without it). Adds `league_point_awards` ledger table (one row per `add_league_points` award; user_id/source/awarded_date/week_start indexed; RLS read-only-own; writes only via SECURITY DEFINER RPC). Adds `league_point_source_caps` policy table seeded from the Swift `LeaguePointSource` cap enum (mealLogged 3/day, dailyLogin 1/day, bodyWeightLogged 1/day, friendKudosGiven 5/day, workoutSharedWithFriend 3/wk, streakMilestone_{7,30,100} + newExerciseTried lifetime). Rewrites `add_league_points(UUID, INTEGER, TEXT, TEXT)` (new optional `p_attribution` param) so it: (1) checks `league_point_source_caps` for the source; (2) counts existing rows in `league_point_awards` for the user × source × bucket; (3) returns `{ success: false, cap_blocked: true, cap_reason: 'daily_cap'\|'weekly_cap'\|'lifetime_cap', cap_value: N }` when over cap; (4) applies the 3× Peak Day multiplier when `CURRENT_DATE matches user_league_tier.peak_day_iso`; (5) writes an award ledger row with the effective points + multiplier; (6) JSON response now includes `multiplier` so iOS optimistic updates can credit 3× when the server applied 3×. Pre-placement bucket (`pending_league_points`) still works — Peak Day still multiplies into the pending bucket so the carry-forward remains honest. Rewrites `get_or_join_weekly_league` to surface the new `peak_day` field in both placed and `not_placed` JSON. Idempotent — `ADD COLUMN IF NOT EXISTS`, `CREATE TABLE IF NOT EXISTS`, `INSERT … ON CONFLICT DO UPDATE` for cap policy, `DROP FUNCTION IF EXISTS` before each `CREATE OR REPLACE`. |

**Behavior contract after this lands**:

- Each user has a randomized weekday (1-7); on that weekday only, every awarded League Point counts 3×. Refreshes every Monday.
- The dashboard renders a "Peak Day:  — 3×" widget when `peak_day` is non-null.
- A user can no longer earn `friend_kudos_given` more than 5 times per day even if they bypass the iOS UI — the server returns `cap_blocked: true` on the 6th call.
- A user trying to spam `streak_milestone_7` (e.g. via repeated streak-edge bugs) gets `cap_reason: 'lifetime_cap'` after the first successful award.
- `body_weight_logged` is awarded once per day even if the user logs weight 10 times — repeat logs return `cap_blocked: true` cleanly without throwing.
- The Peak Day Bonus is OFF by default until ops switches the cron job to `auto_place_all_league_members_with_peakdays()`. The DDL ships safely — no behavior change for any user until cron flip.
- All Sprint 1+2 invariants preserved: percentage zones, Stand-Out, Shield, Crown, Bounceback, Two-Strike Verified, IDOR / privacy / friend-overlap / blocks unchanged.

**Deploy order**: standalone — depends on `weekly_leagues_migration.sql` (`league_tiers` table), `20260326_verified_league_tier.sql` (Verified tier rank 7), and `20260426_sprint7_security_hygiene.sql` (latest IDOR-guarded function bodies that this migration carries forward). Safe to re-run.

**Paired iOS code change (in same Sprint 1 PR, NOT a migration)**:

- `Fit33/WeeklyLeagueService.swift` — no model change required; `LeagueStanding.promotionCount` / `relegationCount` already decode the same JSON keys.
- `Fit33/WeeklyLeagueViews.swift` — leaderboard zone-divider rendering already keys off `standing.promotionCount` / `relegationCount`, so the new live-computed values flow through automatically.

---

## Monetization Phase 1a — Subscriptions schema + revenue rollup (2026-04-29)

> Phase 1a of the MONETIZATION_AGENT.md rollout. Creates the canonical
> data model for subscription state, the App Store Server Notifications
> v2 ingest path, the comp-grant audit log, and the nightly revenue
> rollup that feeds the CMS `/revenue` tab. Pairs with the
> `assn-webhook` edge function (Phase 1b — same PR) and the
> `PremiumManager.refreshFromServer()` defensive flip (Phase 1c — same
> PR; observability-only until paywall ships).


| #   | File                                 | Status                                                         | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | ------------------------------------ | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 147 | `20260715_monetization_phase_1a.sql` | 🆕 Ready (2026-04-29) — deploy after assn-webhook env vars set | Adds `user_profiles.subscription_tier TEXT NOT NULL DEFAULT 'free'` (`free` / `pro_monthly` / `pro_yearly` / `pro_lifetime` / `comp`) — cheap RLS-gating column other RPCs read without joining `subscriptions`. Creates **6 new tables** (`subscriptions`, `iap_receipts`, `subscription_grants`, `revenue_daily_rollup`, `paywall_experiments`, `paywall_experiment_assignments`) with RLS-pinned read-own policies on `subscriptions` / `subscription_grants` / `paywall_experiment_assignments` and service-role-only access on `iap_receipts` / `revenue_daily_rollup` / `paywall_experiments` (mirrors `silent_push_wake_log` pattern, Infra invariant 14). Creates **9 SECURITY DEFINER RPCs**: `get_my_subscription_state()` (iOS-facing, `auth.uid()`-pinned; resolves subscription > comp grant > free), `is_subscriber()` (cheap boolean for other RPCs to gate Pro features), `record_iap_event(...)` (single-write path used by `assn-webhook` — does `iap_receipts` insert + `subscriptions` upsert + `user_profiles.subscription_tier` mirror + `subscription_grants` audit row for refunds, all in one tx), `grant_premium_to_user(...)`, `revoke_premium_from_user(...)`, `extend_trial(...)`, `mark_refund_acknowledged(...)` (CMS comp-grant + refund-ack actions, all REVOKEd from PUBLIC), `get_revenue_overview(p_lookback_days)` (admin-CMS read for `/revenue` tab — reads `revenue_daily_rollup`; never aggregates live), `compute_revenue_rollup()` (nightly cron: end-of-day counts, day-deltas, MRR/ARR, refunds_count/cents, per-tier subscriber breakdown). Schedules `revenue-daily-rollup` pg_cron job at `15 0 * * *` (between daily-quest reset at 00:05 and bug-intel rollup at 01:00). Updates `delete_user_account()` to cascade-delete the 4 new user-data tables. Trailing audit `DO $$` block fails loud if (a) any of the 6 monetization tables is missing or has RLS disabled, (b) any of the 9 canonical RPCs is missing, (c) `user_profiles.subscription_tier` column is missing. Idempotent — `IF NOT EXISTS` ALTERs, `CREATE OR REPLACE FUNCTION`, `CREATE INDEX IF NOT EXISTS`, `cron.unschedule` before `cron.schedule`. |


**Paired Edge Function (NOT a migration — shipped in the same commit as #147)**:

- `supabase/functions/assn-webhook/index.ts` — App Store Server Notifications v2 webhook. Decodes the JWS envelope + nested `signedTransactionInfo` / `signedRenewalInfo` (`base64UrlDecode` helper, no Apple-cert chain validation in Phase 1b — gated by `ASSN_VERIFY_SIGNATURE` env flag, defaults to `false` audit-only mode until cert chain validator lands). Resolves transaction → user_id via `appAccountToken` (canonical) or `original_transaction_id` lookup on existing `subscriptions` row (fallback). Single canonical write through `record_iap_event(...)` RPC — never two writes for one event. Bundle-ID guard via `ASSN_BUNDLE_ID` env (defense in depth against cross-app spoof). Handles all 18 ASSN v2 notification types via the type→status map inside `record_iap_event`; `TEST` events ACKed without state mutation; `REFUND` / `REVOKE` automatically write a `subscription_grants` audit row. **Required env vars**: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `ASSN_BUNDLE_ID` (must match `aps.appAppleId` in payload), `ASSN_VERIFY_SIGNATURE` (`true`/`false` — default `false` until cert chain verified end-to-end). Deploy: `supabase functions deploy assn-webhook --no-verify-jwt` (Apple sends no Supabase JWT; signature is the only auth boundary). Register in App Store Connect → App Information → App Store Server Notifications v2 production URL field after deploying.

**Paired iOS code change (NOT a migration — shipped in the same commit as #147)**:

- `Fit33/UserManager.swift` — `PremiumManager` adds `serverEntitlement: SubscriptionEntitlement` (enum: `.unknown` / `.free` / `.premium(tier, source, expiresAt)`) + `lastServerCheckAt: Date?` + `refreshFromServer()` async (calls `get_my_subscription_state` RPC, decodes response into private `SubscriptionStateResponse` DTO). Defensive: RPC failures (function-not-found pre-deploy, auth-expired, network flap) leave `serverEntitlement = .unknown` and **never** mutate `isPremiumUser`. `updateFromStoreKit(hasSubscription:)` now kicks off a server refresh after every transaction event so `serverEntitlement` tracks the webhook's view. **Phase 1c is observability-only** — when client and server disagree, we LOG via `AppLogger.info` so the gap surfaces in `dev_session_logs`. Future commit (paywall ship — MONETIZATION roadmap Phase 2) flips `isPremiumUser` to derive from `serverEntitlement` and removes the always-true default.
- `Fit33/PerformanceSignposts.swift` — adds `iapLoadProducts` / `iapPurchase` / `iapRestore` / `iapEntitlementRefresh` / `iapServerVerify` to `Op` so StoreKit catches collapse to one fingerprint per call site (BUG_INTELLIGENCE invariants 1–2). `Fit33/StoreKitManager.swift` — `loadProducts` / `purchase` / `restorePurchases` / `Transaction.updates` catch blocks now route through `NetworkErrorClassifier.log(op:)` instead of `AppLogger.error`.
- `Fit33/AdManager.swift` — adds 60s interstitial cooldown (`lastInterstitialShownAt` stamped at present-time, not dismiss-time, per MONETIZATION invariant 15), 3-workout activation window (no ads until user has completed at least 3 workouts), under-13 / COPPA gate (`isUnder13` reads `UserManager.shared.currentUser?.age`; under-13 users skip ATT prompt entirely + skip ads + always get `npa: 1` if any ad ever loads), and `npa: 1` extras for ATT-denied / under-13 (via new `makeAdRequest()` helper threaded through `InterstitialAd.load` + `RewardedAd.load`). MONETIZATION_AGENT invariants 11, 15, 16, 18, 19.
- `Fit33/PrivacyInfo.xcprivacy` — `NSPrivacyTracking` flipped to `<true/>` and 6 AdMob tracking domains added to `NSPrivacyTrackingDomains` (`googleads.g.doubleclick.net`, `googleadservices.com`, `www.googleadservices.com`, `adservice.google.com`, `pagead2.googlesyndication.com`, `googlesyndication.com`). MONETIZATION invariant 34. Apple scans + rejects on submission if missing.

**Behavior contract after this lands**:

- `subscriptions` / `iap_receipts` / `subscription_grants` start empty (no backfill — all real subscription rows arrive via `assn-webhook` from real Apple events). `revenue_daily_rollup` populates with zeros until the first paid subscriber happens; CMS `/revenue` tab Overview cards light up automatically (today returns `schema_deployed: false`; the page's API probe flips on once this migration deploys).
- `PremiumManager.refreshFromServer()` runs after every StoreKit transaction event. Until paywall ships, no behavior change for users — `isPremiumUser` stays `true` for everyone, but `serverEntitlement` now tracks the server's view and we LOG the divergence.
- StoreKit purchase / restore failures collapse to ONE fingerprint per call site instead of one per occurrence — sandbox outages and App Store flaps stop spamming `bug_intelligence_fingerprints`.
- Interstitial ads enforce the 60s cooldown — users who finish 3 sets in 90s no longer see 2 ads back-to-back. Users <3 workouts in see ZERO ads (activation window). Under-13 users see no ads + skip the ATT prompt entirely (COPPA / GDPR-K compliance).
- Apple privacy report (Xcode → Archive → Distribute → Privacy Report) now correctly declares AdMob tracking domains. Eliminates Apple's "Privacy Report missing domains" rejection path on next submission.

**Deploy order**: this migration is standalone — only depends on `user_profiles`. Pre-deploy checklist: (1) Set `assn-webhook` env vars in Supabase Functions secrets (`ASSN_BUNDLE_ID`, `ASSN_VERIFY_SIGNATURE=false` for first cut), (2) Deploy edge function `supabase functions deploy assn-webhook --no-verify-jwt`, (3) Run migration via Supabase SQL Editor (idempotent — re-running is a no-op), (4) Register the assn-webhook URL in App Store Connect → App Information → App Store Server Notifications v2 (Sandbox URL first, Production after sandbox flow validated), (5) Verify `revenue-daily-rollup` cron job is scheduled (`SELECT * FROM cron.job WHERE jobname = 'revenue-daily-rollup';`).

---

## Bug-Intel Audit Drainage — 2026-04-29 (1946 UTC export)

> Hotfixes targeting the four highest-occurrence clusters in the
> `2026-04-29T19:46` audit export. Two are recurring schema-shape bugs
> (sleep_logs ON CONFLICT + shared_workouts CHECK) that the iOS code
> already calls correctly — the database needs to catch up. Pairs with
> matching iOS code changes (KeychainHelper return-status surfacing,
> WhoopService verified-write rotation, WHOOP / Contacts log-level
> hygiene) shipped in the same commit.


| #   | File                                              | Status                                                                                                 | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 149 | `20260718_sleep_logs_upsert_unique.sql`           | 🆕 Ready (2026-04-29) — drains 51 occ across 2 fingerprints                                            | Adds non-partial unique index on `sleep_logs(user_id, date_of_sleep, source)` so PostgREST `onConflict: "user_id,date_of_sleep,source"` (the existing `HealthDataService.swift::saveSleepFromHealthKit` upsert call site) actually matches. Backfills NULL `source` rows to `'manual'` so the index domain is well-defined; defensive dedup keeps the newest row per `(user_id, date_of_sleep, source)` triple before the index is created. Adds a companion `idx_sleep_logs_user_date` read index. Trailing audit `DO $$` block fails loud if (a) the new index is somehow still partial (predicate present means PostgREST can't use it), (b) any duplicate `(user_id, date_of_sleep, source)` group survives dedup. Idempotent — `CREATE INDEX IF NOT EXISTS` + `DROP INDEX IF EXISTS` for the legacy partial-index name; safe to re-run. Resolves bug-intel `9e02b91e` (HIGH crash, 36 occ) + `b38bcdf9` (MEDIUM log, 15 occ).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 150 | `20260719_shared_workouts_status_saved.sql`       | 🆕 Ready (2026-04-29) — drains 2 occ + the entire "Save to favorites" path on inbound shared workouts  | Widens `shared_workouts_status_check` to include `'saved'`. The iOS `FriendService.swift::saveSharedWorkout` (line 1099) already writes `status = 'saved'` when the user taps "Save to favorites" on a received workout, but the on-prod CHECK constraint allowlist was tightened to `pending/accepted/declined/started/completed` only — every save tap rejected with pg:23514 and the optimistic-removed row reappeared on next foreground sync. Migration drops the legacy CHECK (any name variant — `shared_workouts_status_check` and the older `valid_status` are both DROP-IF-EXISTSed) and re-creates with the full canonical 6-value allowlist. Trailing audit fails loud if the new constraint is missing or doesn't include `'saved'`. Idempotent. Resolves bug-intel `69b01dea` (crash) + `eb041521` (log).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 152 | `20260721_orphan_auth_user_purge_and_trigger.sql` | 🆕 Ready (2026-04-29) — unblocks any user who ever deleted an account before this trigger was deployed | Reinstalls the `BEFORE DELETE` trigger `trigger_delete_auth_user_on_profile_delete` on `user_profiles` so future "delete account" flows cascade to `auth.users` + `auth.identities`. The trigger function (defined in the historical `complete_account_deletion.sql`) was either never deployed or got dropped, leaving every "deleted" account as an `auth.users` zombie that permanently locked the email from re-signup with the cryptic "User already registered" error. Adds a one-shot purge of `auth.users` rows that have NO corresponding `user_profiles` AND `created_at < NOW() - INTERVAL '7 days'` (7-day grace excludes in-flight signups mid-onboarding where the auth row is created before the profile row). Paired with iOS fix in `Fit33/NewOnboardingView+Verification.swift::signUpOrRecoverExistingAccount` — sign-up recovery path now distinguishes "Invalid login credentials" on the recovery sign-in (zombie row OR real prior account at a different password) from a successful recovery, and surfaces actionable copy ("An account already exists for X. Tap 'Sign in' or 'Forgot password?' to reset.") instead of the misleading raw GoTrue error. Trailing audit `DO $$` block fails loud if (a) trigger missing/disabled after the migration, (b) any orphan older than 7d survives. Idempotent — DROP/CREATE on the trigger; the purge is a no-op on second run because all matching orphans are gone. Canonical incident: `joereedis@icloud.com`, 2026-04-29 — auth row created by Joe's pre-trigger-fix delete, signup re-attempt blocked, recovery sign-in failed with `Invalid login credentials` because the typed password was for a NEW account. Companion iOS sign-out hardening also lands in this commit: `Fit33/PersistenceController.swift::clearAllUserData` now sweeps the previously-leaked `fit33_*`, `private_*`, `community_*`, `whoop_*`, `oura_*`, `strava_*`, `fitbit_*` UserDefaults prefixes (root cause of the @jreedy + Manuel friend-list flash on `joe@test.com`'s first launch right after Joe's account was force-signed-out) and clears `FriendPhotoCache.shared` to nuke per-friend disk photos. Adds Data invariant #49.                                                                                                         |
| 153 | `20260722_remove_smack_rate_limit.sql`            | 🆕 Ready (2026-04-29) — unblocks unlimited smack-talk during widget shout-bubble dogfood               | Drops the 5/day per-(challenge, sender) reaction cap from `send_challenge_reaction` so users can fire trash talk / power-up cheers without hitting the wall. All other validation stays intact (sender-is-participant, recipient-is-participant, challenge-status='active'). The push-notification queue insert + JSONB shape (`type`, `challenge_id`, `from_user_name`, `reaction_emoji`, `reaction_text`, `reaction_category`) are preserved verbatim — required so the new iOS `SilentPushHandler.handleChallengeReaction` + `SmackTalkWidgetBridge` widget shout-bubble pipeline (shipped 2026-04-29) keeps working. `remaining_today` field stays in the success response (sentinel = 999) so older `iOS::SendReactionResponse.remainingToday` decoders keep deserializing without a coordinated client release; the iOS picker UI is updated in the SAME commit to drop the remaining-count badge + gating. `get_challenge_reaction_count_today()` left in place for analytics + an easy revert path. Drops every known overload before CREATE OR REPLACE per supabase-rules invariant 12. Trailing audit `DO $$` block fails loud if (a) the function is missing post-migration, (b) any `today_count >= N` rate-limit pattern survives in the rebuilt prosrc, (c) the `push_notification_queue` insert with `type='challenge_reaction'` is missing (which would silently break the widget pipeline). Idempotent — `BEGIN/COMMIT`, no data mutation. Paired iOS code change in `Fit33/ChallengeReactionsView.swift`: removes `remainingToday` `@State`, the `dailyLimitBadge` view, the `.disabled(remainingToday <= 0)` gate, and the `.task { getReactionCountToday }` call.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 151 | `20260720_progress_zero_clobber_guard.sql`        | 🆕 Ready (2026-04-29) — drains the "step progress 0-clobber" cluster (Manuel d10d5d03 1v1, 2026-04-29) | Adds a transient-HK-zero guard to all three challenge progress RPCs: `log_challenge_progress` (1v1/group), `log_private_challenge_progress`, `log_community_challenge_progress`. Single new branch in the upsert `DO UPDATE SET … progress_value = CASE` clause: `WHEN p_allow_decrease AND EXCLUDED.progress_value = 0 AND <table>.progress_value > 0 THEN <table>.progress_value` — refuses to overwrite a positive same-day `progress_value` with `0` even when `p_allow_decrease=TRUE`. Companion branch in the `target_hit` CASE preserves the existing `target_hit` so a hit-row doesn't flip to a miss-row when the 0 push is rejected. Root cause: the 2026-04-24 dawn-ghost fix (DATA invariant #47) routed `steps`/`active_minutes` through `allow_decrease=true` so stale yesterday-EoD `@Published` values could be replaced; that opened the opposite failure mode where a silent-push or BGTask wake-up runs `syncHealthKitDataToChallenges()` BEFORE the HKAnchored query has emitted today's first sample → both `HealthKitManager.shared.todaySteps` and `HealthKitService.shared.todaySteps` read 0 → iOS pushes 0 with `allowDecrease=true` → server clobbers a real value (e.g. Manuel 8174 → 0). Canonical incident: user `d10d5d03-1a0d-4b41-b61b-a4f30a56362e` 1v1 challenge `21423bcb-…` clobbered to 0 at 2026-04-29 19:28 UTC + 2026-04-27 05:57 UTC (both correlated to silent-push lite-wake batches per `dev_session_logs`). Fanout trigger (`fanout_challenge_progress`, #94) propagated the 0 to community + private, blanking ALL of Manuel's leaderboards. Trailing audit `DO $$` block fails loud if the regex `EXCLUDED.progress_value = 0` is missing from any of the three RPC `prosrc`s OR if the paired `progress_value > 0` half is missing. Drops every known overload before CREATE OR REPLACE per supabase-rules invariant 12. Idempotent — `BEGIN/COMMIT`, no data mutation (Manuel's 0-rows self-heal on next non-zero push). Paired iOS fix in `Fit33/ChallengeService.swift`: converts the stuck `isChallengeSyncing: Bool` flag to a timestamp-based stale-lock (`challengeSyncStartedAt: Date?`, auto-releases after 30s) so a hung sync body whose `defer` never fires can no longer permanently block subsequent syncs. Adds Data invariant #48. |


**Paired iOS code changes (NOT migrations — shipped in the same commit as #149 + #150)**:

- `Fit33/KeychainHelper.swift` — `save(key:value:)` and `delete(key:)` now return `OSStatus` (with `@discardableResult` so existing callers stay binary-compatible). Failures (any non-success / non-`errSecItemNotFound` from `SecItemDelete`+`SecItemAdd`) log at `.warning` so the breadcrumb survives. New `saveAndVerify(key:value:) -> Bool` performs an immediate read-back to confirm the keychain write actually persisted — used by OAuth refresh paths to detect the silent-failure mode that was the root cause of bug-intel `af583196` ("WHOOP refresh token wiped, keychain readable, accessToken present, refreshToken missing", 13 occ across 2 users). The original silent `OSStatus` drop allowed a transient `errSecInteractionNotAllowed` (BGTask wake on locked device) to lose the rotated refresh_token while leaving the access_token intact — guaranteeing the next refresh would hit the disconnect branch and auto-sign-out the user.
- `Fit33/WhoopService.swift` — adds private `writeRefreshTokenVerified(_:)` helper that wraps `KeychainHelper.saveAndVerify` for the rotated RT path with an OAuth audit-log breadcrumb. `_performTokenRefresh` now uses the verified write with one retry; if BOTH attempts fail the previous (still-valid) RT stays in keychain and the audit trail captures the keychain-write failure — no more silent disconnects. The "Refresh token wiped" log line drops from `.error` → `.warning` (recoverable user-facing state, NOT a bug-class event), and the canonical `disconnect()` log at line 801 drops from `.error` → `.warning` (every disconnect — intentional or recovery — was inflating bug-intel severity scores). Real refresh failures (HTTP 4xx token reject) are still logged at `.error` in the calling path before they reach `disconnect()`.
- `Fit33/ContactsService.swift` — three call sites (`findMatchingUsersDirect()` line 428, `syncContactsToDatabase()` line 595, `checkForContactJoinedNotifications()` line 614) now `catch is CancellationError` separately and log at `.debug`. `URLError.cancelled` (`NSURLErrorDomain` -999) likewise drops to `.debug`. Drains bug-intel `5491a773` + `3893b2b5` + `910d8141` — all three were `Swift.CancellationError` from rapid tab switching being mis-classified as crash-class events.

**Behavior contract after this lands**:

- HealthKit sleep sync stops failing on every tick. The first successful upsert per `(user_id, date_of_sleep, source)` per day persists; subsequent same-day rollups update in place via the matched constraint.
- Tapping "Save to favorites" on an inbound shared workout now persists `status='saved'` server-side; the workout disappears from the inbox AND stays gone after the next refresh (no more bounce-back).
- WHOOP rotated refresh tokens are verified before declaring success. Locked-class keychain races during BGTask wake no longer silently lose the RT; the previous (still-valid) RT stays in place and the next refresh tries again instead of disconnecting.
- WHOOP "Disconnected" log lines and "Refresh token wiped — disconnecting" log lines stop generating bug-intel error fingerprints. Real OAuth failures (HTTP 4xx token reject) still classify as errors.
- Tab-switch / view-disappear cancellations of contact sync / matching / notification check stop generating bug-intel CancellationError fingerprints.

**Deploy order**: both migrations are standalone. Run #149 + #150 via Supabase SQL Editor in any order; ship the iOS commit in the same release. Re-running either is a no-op.

---

## Quality Workout Corpus + Reversible Completion — 2026-04-29

> Pair of migrations that solve two related-but-distinct problems on the
> workout completion path. (1) The auto-gen training corpus
> (`collaborative_workout_data`) is currently polluted by 7-minute /
> 2-exercise tap-throughs because `CollaborativeLearningEngine.swift`
> records every completed workout with `was_successful = (exerciseCount >= 3)` —
> a flimsy filter. (2) The completion path eagerly writes to seven
> server-side surfaces BEFORE the user sees the completion screen, so
> there's no way for the user to "undo" a workout they shouldn't have
> logged (test taps, accidental quick-completes, etc.). #154 defines a
> server-side quality rubric that gates the corpus; #155 adds the atomic
> reversal RPC that powers the new "Delete" button on the completion
> screen.


| #   | File                                           | Status                                    | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | ---------------------------------------------- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 164 | `20260802_exercise_performance_id_match.sql`   | ✅ Deployed (2026-04-29) — paired iOS writer in `ExerciseHistoryService.saveExercisePerformance` was already shipping in build 1.38(64) before the migration ran, causing 3× `PGRST204` ("column not found in schema cache") on every workout finish until the column was added to prod. Lesson: paired iOS+SQL changes must apply the SQL migration to prod BEFORE / WITH the iOS build that writes the new column, never after. | Adds `exercise_id UUID` to `exercise_performance_history` and `exercise_set_history` so the active-workout "Previous Max" tile row (`RecentSessionsTilesRow`, fed by `ExerciseHistoryService.fetchRecentSessions(...)`) can filter by EXACT exercise identity instead of by `exercise_name` alone. **Why**: until now the tile row queried `(user_id, exercise_name)`. When the user's catalog contained two distinct `Exercise` rows that happened to share a `.name` (custom + catalog row, equipment variants merged under one canonical name during a CMS sync, a renamed exercise that collided with a sibling), the tile row pulled history from a DIFFERENT exercise and rendered it as `"Previous Max: 185 lb"` on a card the user had never trained. User-visible bug filed 2026-04-29. **Schema**: nullable `exercise_id UUID REFERENCES exercises(id) ON DELETE SET NULL` on both tables (nullable so the migration adds without a long-running backfill blocking it; FK SET NULL so a future catalog deletion doesn't cascade-wipe the user's history). **Indexes**: partial `(user_id, exercise_id, workout_date DESC) WHERE exercise_id IS NOT NULL` on `exercise_performance_history` (the tile-row read pattern), `(user_id, exercise_id) WHERE exercise_id IS NOT NULL` on `exercise_set_history` for future per-set lookups. **Best-effort backfill**: for each `(user_id, exercise_name)` tuple with `exercise_id IS NULL`, resolve `exercises.id` ONLY when the name maps unambiguously to a single catalog row (`HAVING COUNT(*) = 1` over `exercises` grouped by name). Ambiguous names — the exact bug class above — intentionally stay NULL: the tile row will then hide for them, prompting the user to log a fresh session against the specific exercise they're now looking at. Mirror the resolution into `exercise_set_history` via `performance_id` join so per-set rows track the same canonical exercise_id. **No RLS changes** — both tables already filter by `auth.uid() = user_id`. **iOS coordination (paired commits, NOT migrations)**: `ExerciseHistoryService.saveExercisePerformance(...)` accepts a new `exerciseId: UUID?` and writes it; `fetchRecentSessions(forExerciseId:limit:)` is the new strict-id read entrypoint (no name fallback when `exerciseId` is present); `ExerciseCard.loadRecentSessionsIfNeeded()` always passes `exercise.id`. Trailing fail-loud `DO $$` audit verifies both columns exist and counts (`total / resolved / ambiguous-NULL`) for ops visibility. **Idempotent** — `ADD COLUMN IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`, the backfill UPDATE is filtered to `exercise_id IS NULL` so re-runs only touch unresolved rows. **Reversible** — drop columns + indexes; iOS keeps a name-keyed fallback path for safety when the column is absent. |
| 163 | `20260801_canonical_muscle_taxonomy.sql`       | 🆕 Ready (2026-04-29) — depends on #157, #162 | Fixes a real taxonomy drift in `exercises.primary_muscles` where the same anatomical muscle is stored under both a long anatomical name AND a canonical short name across different rows (4 long-form names found in 27 prod rows). **Why discovered**: #157's sister-corroboration gate fired on a `remove` proposal for "Latissimus Dorsi" on Bent Over Row (Smith Machine) variants — Claude correctly noticed 13 other rows in the `bent_over_row` family use `["Back","Lats"]` while 3 Smith Machine rows use `["Latissimus Dorsi"]`, and proposed removing the outlier. The proposal was *technically* sister-corroborated but factually wrong: clicking "Override" would have left `primary_muscles=[]` (no back tag at all). The right answer is a RENAME, not a DELETE. **Drift map (verified 2026-04-29 against prod)**: `Latissimus Dorsi`→`Lats` (3 rows), `Pectorals`→`Chest` (10 rows), `Quadriceps`→`Quads` (12 rows), `Trapezius`→`Traps` (2 rows). Total 27 row updates, all in `primary_muscles`, none in `secondary_muscles`. **Three changes**: (1) NEW `_normalize_muscle_array(TEXT[]) RETURNS TEXT[]` IMMUTABLE helper — maps long-form to canonical short-form via static CASE, then DISTINCT-collapses so an array containing both forms (e.g. `["Lats","Latissimus Dorsi"]`) merges cleanly without dupes. (2) DATA CLEANUP — single `UPDATE exercises SET primary_muscles = _normalize_muscle_array(primary_muscles) WHERE primary_muscles && ARRAY[...]` per column, idempotent (re-running is a no-op). (3) AUTO-REJECT the 2 stuck `Bent Over Row%` × `secondary_muscles`/`primary_muscles` × `remove ['Latissimus Dorsi']` proposals with `rejected_reason='taxonomy_drift_fixed_by_migration_163'`. The Pull-Up Wide Grip proposal is unrelated (legacy v1 #156 append-only bug, removing Triceps+Front Delts) and stays pending for admin override. (4) GUARDRAIL — `apply_exercise_correction` is rewritten (same signature, same gate semantics as #162) to normalize muscle inputs through `_normalize_muscle_array()` BEFORE union/diff. Future Claude proposals using long-form names land as their canonical short form; the catalog can never drift back. (5) Edge function `analyze-quality-workout` system prompt updated with explicit "use these short forms only" list — paired redeploy after this migration. **Trailing fail-loud `DO $$` audit** verifies 0 rows still contain long-form names AND counts the auto-rejected proposals. Idempotent — `CREATE OR REPLACE` on the helper, the data UPDATEs are no-ops once converged, and the proposal-rejection UPDATE is filtered to `status IN ('pending','blocked_core_exercise')` so re-running won't disturb anything already decided. **Behavior contract**: catalog has 1 canonical name per anatomical muscle; sister-corroboration gates fire on real disagreements (not naming inconsistencies); the proposals queue is clean of taxonomy-induced false positives; the Pull-Up legacy mistake is the only blocked-core proposal awaiting admin decision.
| 162 | `20260731_correction_audit_fix.sql`            | 🆕 Ready (2026-04-29) — depends on #157 | Two fixes for the catalog-correction audit trail introduced by #157. **(1) Audit `new_value` bug**: for `add` and `remove` operations on `primary_muscles` / `secondary_muscles`, the old `apply_exercise_correction` body wrote `p_new_value` (the INPUT — items being added or removed) into `exercise_corrections.new_value` instead of the resulting array state. The Workout Intelligence report page displayed it as `["Chest","Shoulders"] → ["Chest","Shoulders"]` on a removal — confusingly looking like a no-op even though the underlying catalog row was correctly updated. Migration rewrites the function body to compute `v_post_value = to_jsonb(v_new_text_array)` and inserts that. Existing audit rows stay (append-only history); only future corrections render correctly. Same signature, same RLS, same corroboration gate (still requires `p_corroboration_kind ∈ {sister,name,multi_report,admin_override}`). **(2) Admin-override path for blocked proposals**: pull-up case from #156 where Triceps + Front Delts incorrectly tagged as `secondary_muscles` and the sister-corroborated `remove` proposal sits in `blocked_core_exercise` because pull-ups are on `catalog_core_exercises`. New `admin_apply_correction_proposal(p_proposal_id UUID) RETURNS JSONB` SECURITY DEFINER, service-role only — looks up the proposal, calls `apply_exercise_correction` with `p_corroboration_kind = 'admin_override'`, then UPDATEs the proposal status to `applied`. The `'admin_override'` kind is the ONLY one that bypasses the core-exercise lockout. New `admin_reject_correction_proposal(p_proposal_id UUID, p_reason TEXT)` companion RPC marks pending or blocked proposals as `rejected` with a reason string. Both are wired into the CMS Catalog Proposals page (per-card Approve/Override and Reject buttons). Idempotent — `DROP FUNCTION IF EXISTS` before each `CREATE OR REPLACE`. **Behavior contract**: nothing changes about which proposals auto-apply (the corroboration ladder is unchanged); audit display becomes accurate on new corrections; admins can finally clear the pull-up backlog and any future blocked-on-core-list proposals from the CMS without writing SQL.
| 161 | `20260730_rls_league_point_source_caps.sql`    | ✅ Deployed (2026-04-29) — Supabase linter hotfix; depends on #148 | RLS hotfix for `public.league_point_source_caps`, the static cap-policy table seeded in #148. The Supabase platform linter (2026-04-29 export, attached screenshot) flagged it CRITICAL because the table was created without `ENABLE ROW LEVEL SECURITY` — public-schema tables are PostgREST-exposed, so `anon` and `authenticated` could SELECT every row. Migration: (1) `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` (no-op if already on). (2) Single `lpsc_select_authenticated` SELECT policy `FOR SELECT TO authenticated USING (TRUE)` — cap policy is the same data the iOS `LeaguePointSource` enum already ships, so exposing it to authed clients is harmless and lets future UI render "5/day" hints. (3) NO write policies — cap edits go through migrations only, exactly per the table comment ("Edit cap values via new migrations — never UPDATE rows directly in production."). **Why this won't break `add_league_points`**: the only reader is the `add_league_points(UUID, INTEGER, TEXT, TEXT)` SECURITY DEFINER RPC at line 180 of #148, which executes as the function owner (`postgres`) and `postgres` has `BYPASSRLS` by default in Supabase. `service_role` also bypasses RLS via the same attribute, so any future edge-function read works too. **Trailing fail-loud audit** verifies `pg_class.relrowsecurity = TRUE`, the policy exists with the canonical name, and the table still has the 14 seeded cap rows (catches a regression where someone TRUNCATEs in the same PR — empty table = silent no-op cap enforcement). Idempotent — `ENABLE ROW LEVEL SECURITY` is a no-op when on, `DROP POLICY IF EXISTS` precedes `CREATE POLICY`. |
| 160 | `20260729_user_training_profile.sql`           | 🆕 Ready (2026-04-29) — depends on #156 | Wires up the rolling per-user training-style profile (table created in #156, never populated until now). New `refresh_user_training_profile(p_user_id UUID) RETURNS JSONB` SECURITY DEFINER RPC. Aggregates the user's last 8 completed quality workouts into a single `user_training_profile` row. **Fields populated**: (1) `inferred_intent` from rep distribution: ≥50% of working sets in 1-5 reps → `strength`; ≥50% in 6-12 → `hypertrophy`; ≥30% at 13+ reps → `endurance`; otherwise `mixed`. Warmup sets excluded. (2) `median_rest_sec` from `exercise_set_history.completed_at` deltas, capped at 600s to exclude exercise-transition gaps. NULL when not enough timestamped sets exist. (3) `preferred_equipment` top 5 `equipment_category` values by frequency. (4) `strong_movement_patterns` / `weak_movement_patterns` per primary muscle: appears in ≥3 workouts AND completion rate ≥0.90 (strong) / <0.70 (weak). (5) `avg_session_duration_min`, `avg_working_sets_per_session`. (6) `profile_jsonb` extensible bag (window_size, total counts, rep_distribution, workout_ids in window) for future fields. **Refresh cadence**: edge function `analyze-quality-workout` calls this fire-and-forget at end of every successful Claude report write (step 8 in `processWorkout`). RPC no-ops when user has <4 completed reports — first 3 quality workouts contribute data but don't trigger a profile yet. After that, every quality workout refreshes in-place. Failures in the profile refresh are logged but never fail the parent report — profile is auxiliary intelligence. **IDOR-pinned**: raises `42501` if `auth.uid()` is set AND ≠ `p_user_id` (service role passes through). **One-shot backfill**: trailing `DO $$` block computes the profile for every user with ≥4 completed reports already in `ai_workout_reports`. Idempotent — `INSERT ... ON CONFLICT (user_id) DO UPDATE` so re-running the migration just re-aggregates. **Behavior contract**: unblocks the last gap in the auto-pipeline — `WorkoutSuggestionEngine` and `SmartExerciseSelectionEngine` can now read `user_training_profile` to bias auto-gen toward this user's inferred intent + preferred equipment, and to deprioritize their weak movement patterns. |
| 159 | `20260728_workout_intel_cron.sql`              | ✅ Deployed (2026-04-29) — depends on #157 | pg_cron schedules for the workout-intelligence pipeline. **Two schedules**: (1) `analyze-quality-workout-run` at `7,17,27,37,47,57 * * * *` (every 10 min, off-cycle from `triage-shake-reports-run` :3-:53 and `triage-bugs` :17-every-4h) — wraps `analyze-quality-workout` edge function in canonical fast-path-skip pattern (returns immediately when `ai_workout_reports.status='pending'` count is 0). (2) `promote-corroborated-proposals-nightly` at `45 3 * * *` (03:45 UTC, off-cycle from quest-personalization 03:50 and bug-intel-autoresolve 04:30) — wraps `promote_corroborated_proposals(500)` SQL function (no edge call, purely DB-local; re-evaluates every pending confidence=1.0 proposal against the corroboration ladder and auto-promotes any that newly pass). **New wrapper RPCs** (canonical pattern, mirrors `trigger_triage_shake_reports`): `trigger_analyze_quality_workout()` (reads internal_config supabase_url/service_role_key/anon_key, fires `net.http_post` with `x-cron-key` header, fast-path skips on empty queue) and `trigger_promote_corroborated_proposals()` (purely DB-local; counts pending confidence=1.0 proposals first to skip empty sweeps, calls bounded `promote_corroborated_proposals(500)`). Both `SECURITY DEFINER service_role`-only. Idempotent: unschedules existing jobs before scheduling, `CREATE OR REPLACE` on wrappers, single-shot `BEGIN; … COMMIT;` block. |
| 158 | `20260727_widen_name_gate.sql`                 | ✅ Deployed (2026-04-29) — depends on #157 | Widens the name-keyword regexes in `_correction_name_gate` so obvious cases auto-promote on a single quality workout instead of waiting for a 2nd corroborating report. **Why**: #157's regex was conservative — it covered canonical lifts (deadlift/squat/bench/pull-up) but missed obvious isolation cuts (skullcrusher/leg curl/pec deck), single-leg patterns (lunge/step-up), hip-hinge variants beyond deadlift (hip thrust/glute bridge), and rep-based keywords that aren't primary movements (kickback/pushdown/preacher/etc.). The first 10-workout run produced 3 proposals that should have auto-applied but sat as `pending`: Walking Lunge × workout_type=Strength + duration_based=false; Skullcrusher × is_compound=false. **What changed**: (1) Strength regex: + lunge, step-up, skullcrusher, hyperextension, leg press, calf raise, shrug, cable crossover, pec deck, hip abduction/adduction, leg curl, leg extension, good morning, RDL, rack pull, kettlebell swing, hip thrust, face pull, crossover. (2) `is_compound=true` regex: + lunge, step-up, hip thrust, glute bridge, RDL, good morning, leg press, calf raise, hyperextension, cable crossover, pec deck, kettlebell swing, push-up, burpee, BUT now ALSO requires NOT matching the isolation regex (so "Single-arm Cable Crossover" never accidentally auto-applies as compound). (3) `is_compound=false` regex: NEW — was previously never matched. Matches isolation patterns (curl, fly, leg curl/extension, kickback, pushdown, lateral/front/rear raise, pec deck, etc.) AND requires NO compound disambiguator (so "Bulgarian split squat" never slips through). (4) `duration_based=false` regex: + lunge, thrust, skullcrush, kickback, pushdown, shrug, crunch, sit-up, leg raise, crossover, RDL, glute bridge, good morning, step-up. (5) Plyometrics + Cardio regexes: small additions (jump squat, broad jump, mountain climber, sled push, prowler, ski erg, assault bike, airdyne). **Trailing sweep**: `DO $$ ... PERFORM promote_corroborated_proposals(500)` runs at the end of the migration so all pending proposals re-evaluate against the new regex in the same SQL run — the 3 stuck cases auto-promoted on this exact path. **Same defense-in-depth as #157**: TRUE-direction-only positive assertions, FALSE direction for is_compound only after the compound-disambiguator filter, core-exercise removal lockout still applies. Existing `exercise_corrections` audit rows unchanged — only pending proposals re-evaluate. **Verified outcomes**: Walking Lunge `workout_type` corrected `Cardio → Strength` (catalog had it wrong as "Cardio"!), Walking Lunge `duration_based false`, Skullcrusher `is_compound false` all auto-applied via NAME gate. |
| 157 | `20260726_correction_corroboration.sql`        | ✅ Deployed (2026-04-29) — depends on #156 | Tightens the catalog-correction policy from "Claude says 1.0 → apply" to "Claude says 1.0 AND a deterministic gate agrees → apply". **Why**: #156 trusted Claude's self-reported confidence as the only gate. The first real run produced an "addition" correction that was factually right but exposed the structural flaw — append-only union prevents data LOSS but creates a one-way ratchet (every wrong tag accumulates forever). This migration adds three deterministic corroboration gates and a separate stricter path for REMOVALS. **The three gates (auto-apply requires confidence=1.0 AND ≥1 of these)**: (a) **sister-exercise corroboration** — for muscle additions: a sister exercise (same `exercise_family`) already has the proposed muscle in the same field. (Pull-up adding "Biceps" → chin-ups already have it.) For muscle REMOVALS: ≥2 sisters do NOT have the value being removed. (b) **name-based determinism** — exercise name contains an unambiguous keyword for the proposed value (TRUE-direction only). E.g. `is_compound=TRUE` for any name matching `(deadlift|back squat|bench press|pull-up|...)`. Conservative — never matches FALSE direction. (c) **multi-report agreement** — same correction (exercise_id, field_name, operation, proposed_value) proposed by ≥2 distinct source reports in last 30 days (≥3 for REMOVE). **Removal path (stricter)**: `operation='remove'` requires confidence=1.0 AND multi-report ≥3 (or sister-disagreement) AND not-a-core-canonical-exercise. New `catalog_core_exercises` table is a hard-list of canonical name patterns (bench, squat, deadlift, pull-up, OHP, row, clean, snatch, jerk) — removals against these are NEVER auto-applied regardless of corroboration count. **New tables**: (1) `exercise_correction_proposals` — corroboration queue. Every Claude-proposed correction lands here. Status `pending`/`applied`/`rejected`/`superseded`/`blocked_core_exercise`. Tracks `sister_corroborated`, `name_corroborated`, `multi_report_count`. (2) `catalog_core_exercises` — read-only blocklist of canonical name patterns. **New RPCs**: (1) `propose_exercise_correction(...)` — the new public-facing RPC the edge function calls. Always inserts a proposal row, then auto-applies if a gate passes. Returns `{success, proposal_id, auto_applied, corroboration_kind?, reason?}`. (2) `apply_exercise_correction(...)` — REWRITTEN with new signature: adds `p_operation TEXT` and `p_corroboration_kind TEXT` (must be sister/name/multi_report — REFUSES to apply without one). Now an internal-only primitive. Old 6-arg signature dropped. (3) `promote_corroborated_proposals(p_max INTEGER DEFAULT 100)` — cron entry point. Re-evaluates pending proposals; promotes any that newly pass a gate. Bounded per-run. **Helpers** (SECURITY DEFINER, service-role only): `_correction_sister_gate`, `_correction_name_gate`, `_correction_multi_report_count`, `_correction_is_core_exercise`. **Edge function changes** (paired redeploy of `analyze-quality-workout`): (a) System prompt teaches Claude ADD/SET/REMOVE operation semantics, with the pull-up case as an explicit example. (b) `applyCorrections` calls `propose_exercise_correction` instead of the direct-apply RPC. (c) Stops client-side filtering by confidence — proposes ALL valid-shape corrections regardless of confidence; SQL handles all gating. **Trailing fail-loud audit**: verifies all 5 new tables/functions exist and the OLD 6-arg `apply_exercise_correction` signature is gone. Idempotent — `IF NOT EXISTS`, `DROP IF EXISTS` before `CREATE OR REPLACE`. **Behavior contract**: existing exercise_corrections audit rows are unchanged. The pull-up case from #156 (Triceps + Front Delts retained incorrectly via append-only) will now be REMOVED automatically once Claude proposes the removal in 3+ reports — until then it sits in the proposals queue for one-click admin approval.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 156 | `20260725_workout_intelligence.sql`            | 🆕 Ready (2026-04-29) — depends on #154   | Workout Intelligence — every quality workout (score >= 70) gets a Claude-generated structured analysis; only 100%-confident exercise corrections are auto-applied to the canonical `exercises` catalog. **Schema additions**: `workout_history.workout_type TEXT` (origin: `auto_gen`/`custom`/`program`/`friend_workout`/`cardio`); `exercise_set_history.completed_at TIMESTAMPTZ` (per-set wall-clock; NULL for legacy rows — pacing analysis falls back to a `duration / completed_sets` proxy). **New tables**: (1) `workout_swap_events` — per-swap audit with `workout_id`, `swap_index` (FE invariant 25 tier), `picked_rank` (0 = top algorithmic suggestion), `swap_source` (`quick_swap`/`smart_swap`/`search`/`random`), `completed_replacement BOOLEAN` populated post-workout. Owner-read RLS; service-role inserts via post-finishWorkout flush since FK requires a stable workout_id. (2) `ai_workout_reports` — one row per quality workout, `report_jsonb` carries the full structured analysis (sections A-H from FE spec: splitFamily/volumeBalance/pressDistribution/orderingScore/pairingQuality/pairingFindings/pacingProfile/progressionEvidence/swapInsights/redFlags/exerciseCorrections/programmedVsExecuted/recommenderSignals/summaryMd), `summary_md` is the human rolling-log entry, `is_suspicious BOOLEAN` (pre-flight detector caught a 1-rep-100-rep-style anomaly → excluded from corpus regardless of quality_score), `is_lost_session BOOLEAN` (workout scored 60-69 — surfaced for manual promote/discard). Pipeline: `pending` → `analyzing` → `complete`/`failed`/`skipped`. Owner-read + owner-can-enqueue RLS. UNIQUE(workout_id) for idempotency. (3) `exercise_corrections` — append-only audit of every auto-applied correction to the canonical exercises catalog. Strict whitelist: `primary_muscles`/`secondary_muscles`/`workout_type`/`equipment_category`/`is_compound`/`duration_based`. `**confidence` column has a CHECK constraint enforcing exactly `1.00`** — anything below MUST be left alone (no human-review queue, no partial-confidence storage). (4) `pairing_signals` — auto-discovered synergistic + negative pairings, `co_occurrence_count` increments on insert conflict, `avg_pairing_quality` is rolling weighted, `reason_codes TEXT[]` aggregates `pairingFindings[].code`. Lexicographic UUID order in (a,b) collapses (A,B) and (B,A) into a single row. Drives `SmartExercisePairingEngine` bias. (5) `user_training_profile` — rolling per-user style profile (`inferred_intent`, `median_rest_sec`, `preferred_equipment[]`, `weak_movement_patterns[]`, `strong_movement_patterns[]`, full extensible `profile_jsonb`). Refreshed every 4 quality workouts. **RPCs**: (1) `apply_exercise_correction(p_exercise_id UUID, p_field_name TEXT, p_new_value JSONB, p_confidence NUMERIC, p_evidence TEXT, p_source_report_id UUID DEFAULT NULL) RETURNS JSONB` SECURITY DEFINER. **Service-role only** (raises `42501` if `auth.uid() IS NOT NULL`). **Strict 100%-confidence gate**: raises `23514` if `p_confidence < 1.00`. **Whitelist gate**: raises `23514` if `p_field_name NOT IN (...)`. Per-field validation (workout_type CHECK whitelist; muscle additions are append-only union — never remove or reorder; equipment/is_compound/duration_based direct write). Append-only audit row to `exercise_corrections` on every successful correction. (2) `enqueue_quality_workout_for_analysis(p_workout_id UUID) RETURNS JSONB` SECURITY DEFINER, `auth.uid()`-pinned. iOS calls fire-and-forget after `score_workout_quality` settles. Inserts a `pending` row into `ai_workout_reports`; idempotent via `ON CONFLICT (workout_id) DO NOTHING`. Score 60-69 rows enqueue with `is_lost_session = TRUE`. **Cheat-code features baked into the schema (V1)**: programmed-vs-executed diff (in `report_jsonb.programmedVsExecuted`); pre-flight suspicious-pattern detector (`is_suspicious`); AMRAP-as-form-rating proxy (per-set in `progressionEvidence`); per-exercise quality rollup (consumed via `pairing_signals` + the report log); synergistic-pairing auto-discovery (`pairing_signals`); lost-session detection (`is_lost_session`); per-user training-style profile (`user_training_profile`). **Companion edge function** (NOT in migration): `analyze-quality-workout` — picks up `pending` rows from cron + ad-hoc invocation. Mirrors `triage-shake-reports` pattern (Anthropic API + service-role JWT auth + structured JSON parsing). Trailing fail-loud `DO $$` audit verifies all 5 tables, both column additions, both RPCs, AND the strict `confidence = 1.00` CHECK constraint format on `exercise_corrections`. Idempotent — `IF NOT EXISTS` everywhere, DROP all overloads before CREATE OR REPLACE. |
| 154 | `20260723_quality_workout_corpus.sql`          | 🆕 Ready (2026-04-29)                     | Defines the canonical "quality workout" rubric and gates the auto-gen corpus on it. **Schema**: adds `workout_history.quality_score INT` + `quality_band TEXT CHECK ('high'/'medium'/'low')` + `quality_reasons JSONB DEFAULT '{}'` + `qualifies_for_corpus BOOLEAN GENERATED ALWAYS AS (quality_score >= 70) STORED` (defense-in-depth — corpus reads filter on the GENERATED column, cannot drift from the score). Adds `collaborative_workout_data.workout_history_id UUID REFERENCES workout_history(id) ON DELETE CASCADE` (so #155's delete RPC auto-purges the corpus row), `workout_quality_score INT` (snapshot at insert time, never recomputed), `is_quality_workout BOOLEAN GENERATED ALWAYS AS (workout_quality_score >= 70) STORED`, `total_sets INT`, `total_volume_lbs NUMERIC`. Hot-path partial index `idx_collab_workout_quality_corpus ON (user_id, completed_at DESC) WHERE is_quality_workout = TRUE` for the auto-gen corpus read pattern. **RPC**: `score_workout_quality(p_workout_id UUID) RETURNS JSONB` SECURITY DEFINER, `auth.uid()`-pinned (Supabase invariant 9). Computes the rubric, UPDATEs `workout_history`, returns structured JSONB `{success, score, band, reasons, qualifies_for_corpus, total_volume_lbs, total_sets}`. Fail-soft: rubric errors return `{success:false, error:...}` rather than raising — the workout stays unscored (safe default — excluded from corpus). **Canonical rubric (Fitness Expert authority — 100 pts total)**: Duration ≥ 25 min (20pts) + completion_rate ≥ 0.80 (25pts) + ≥ 3 distinct canonical exercises (15pts) + ≥ 12 working sets, warmups excluded (15pts) + ≥ 50% strength sets weighted (10pts) + avg ≥ 20s/set proxy (10pts) + FE balance heuristic, push:pull ≤ 2:1 + ≤ 2 horizontal presses (5pts). 70 threshold = `qualifies_for_corpus`; the first four checks sum to 75, so a workout MUST clear Duration + Completion + Exercises + Sets to qualify. Excludes 7-min / 2-exercise tap-throughs by construction. **Backfill**: one-shot `DO $$` block scores every existing un-scored row with `duration > 0`, capped at 10K candidates for safety (ops can run a bigger backfill manually). Trailing fail-loud audit `DO $$` block raises EXCEPTION if (a) `quality_score` column missing, (b) `qualifies_for_corpus` not GENERATED, (c) `score_workout_quality` missing or not RETURNS jsonb, (d) `idx_collab_workout_quality_corpus` missing, (e) backfill ran with scoreable workouts but 0 were scored (catches a silent rubric regression). Idempotent — `ADD COLUMN IF NOT EXISTS`, GENERATED columns dropped+added defensively, `CREATE INDEX IF NOT EXISTS`, `DROP FUNCTION IF EXISTS` before `CREATE OR REPLACE`. `**delete_user_account()` impact**: NONE. Existing CASCADEs (auth.users → user_profiles → workout_history → collaborative_workout_data) propagate through the new columns + new FK automatically.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 155 | `20260724_delete_workout_and_revert_stats.sql` | 🆕 Ready (2026-04-29) — depends on #154   | Atomic "delete workout AND reverse all stat side-effects" RPC powering the new Delete button on the workout completion screen. Single function: `delete_workout_and_revert_stats(p_workout_id UUID) RETURNS JSONB` SECURITY DEFINER, IDOR-pinned (Supabase invariant 9 — `auth.uid() IS NULL` lets service role through; cross-user calls raise `42501 Forbidden`). Function body IS the transaction. Reversal order (matters — children before parents): (a) lookup workout → (b) auth gate → (c) DELETE `exercise_set_history` via subquery on `performance_id` (FIRST, before performance rows are deleted) → (d) DELETE `exercise_performance_history WHERE workout_id` → (e) DELETE `collaborative_workout_data WHERE workout_history_id` (defense-in-depth alongside #154's CASCADE FK) → (f) reverse league points: SUM `awarded_points` (already multiplier-baked) for matching `league_point_awards` row, DELETE it, decrement `league_members.points` for the placed week (Monday computed via canonical `(d - EXTRACT(ISODOW FROM d)::int + 1)::date`) OR fall back to `user_league_tier.pending_league_points` for pre-placement users. Limits ledger DELETE to ONE row (most recent by `awarded_at` for that date) so multiple-workouts-per-day doesn't over-revert. → (g) reverse `user_daily_quests` for `complete_workout` / `complete_program_day` / `do_friend_workout` / `workout_30_min` slots on the workout's date, clamping `current_value` at 0 and only flipping `is_completed` back to FALSE if new value < target → (h) reverse `user_progress.total_xp` and `total_workouts` (both clamped at 0) → (i) DELETE `workout_history` last (#154's FK CASCADE picks up any straggler corpus rows). Returns structured JSONB with every counter (`xp_reverted`, `league_points_reverted`, `league_award_rows_deleted`, `league_members_updated`, `pending_updated`, `perf_rows_deleted`, `set_rows_deleted`, `collab_rows_deleted`, `quest_rows_updated`, `workout_rows_deleted`, plus `workout_date` so iOS can run conditional streak revert). **Streak revert is NOT done server-side**: streak is conditional ("only if THIS was the only workout today") and the server can't know without joining `cardio_workouts` and any sibling `workout_history` rows for the same date — iOS owns conditional streak revert in `WorkoutManager.deleteCompletedWorkout`. `REVOKE EXECUTE … FROM PUBLIC; GRANT EXECUTE … TO authenticated`. Trailing fail-loud audit `DO $$` block raises EXCEPTION if (a) function missing, (b) more than one overload exists, (c) RETURNS clause is not jsonb, (d) `prosrc` lacks `auth.uid() IS NOT NULL` guard or `<> auth.uid()` IDOR comparison or `42501` ERRCODE, (e) `prosrc` doesn't reference any of the six reversal targets (catches a future refactor that accidentally drops a side-effect reversal). Idempotent — DROP all overloads before CREATE OR REPLACE per supabase-rules invariant 12.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |


**Behavior contract after this lands**:

- The auto-gen recommender no longer learns from 7-min / 2-exercise tap-throughs. Reads against `collaborative_workout_data` filtered on `is_quality_workout = TRUE` only see real workouts (≥25min, ≥80% completion, ≥3 exercises, ≥12 sets, ≥50% weighted).
- Existing rows backfilled at deploy time (capped at 10K — anything above runs as a manual chunked backfill). Newly completed workouts are scored fire-and-forget on the iOS completion path (paired iOS code calls `score_workout_quality(workout.id)` after the existing eager writes).
- Tapping "Delete" on the workout completion screen reverses every server-side stat increment atomically: XP comes back out of `user_progress`, `total_workouts` ticks down, the daily quest current_value drops back, league points come out of the placed-week `league_members.points` (or `pending_league_points` for pre-placement), and every history table row is purged. The transaction is all-or-nothing — partial failures roll back.
- iOS handles conditional streak revert based on the returned `workout_date` (only rolls the streak back if no other workout/cardio is logged that day in Core Data).

**Paired iOS code changes (NOT migrations — coordinated commits, Wave 2)**:

- `Fit33/CollaborativeLearningEngine.swift::recordWorkoutCompletion` — wires `workout_history_id` + `workout_quality_score` + `total_sets` + `total_volume_lbs` into the upsert payload. The `was_successful >= 3` flimsy filter is replaced by reading `workout_history.qualifies_for_corpus` after the score has been computed (or simply skipping the `collaborative_workout_data` insert entirely until `qualifies_for_corpus = TRUE`).
- `Fit33/WorkoutManager.swift::completeWorkout` (and `Fit33/ActiveWorkoutView+Actions.swift::finishWorkout`) — adds `Task.detached { await SupabaseManager.shared.client.rpc("score_workout_quality", params: ["p_workout_id": workoutId]) }` post-eager-write fire-and-forget. Never `await` from the completion return path.
- `Fit33/WorkoutManager.swift::deleteCompletedWorkout` (NEW method) — calls `delete_workout_and_revert_stats(workoutId)`, parses the structured response, and rolls back the matching Core Data rows. Owns the conditional streak revert: queries Core Data for any other workout/cardio on `response.workout_date`; if none, decrements `currentStreak` and updates `lastWorkoutDate` to the previous workout. Decoder struct: `DeleteWorkoutResult { success, reason?, xp_reverted, league_points_reverted, perf_rows_deleted, set_rows_deleted, collab_rows_deleted, quest_rows_updated, workout_rows_deleted, workout_date }`.
- New "Delete" button on the workout completion screen wired to `WorkoutManager.deleteCompletedWorkout`.

**Deploy order**: **#154 → #155** (strict order). #155 references `collaborative_workout_data.workout_history_id`, which is added by #154 — running #155 first will fail with a column-not-found error. Both are idempotent on re-run (additive schema + DROP-then-CREATE function). Pre-deploy checklist: (1) Apply #154 in Supabase SQL Editor, verify trailing audit emits the `✅ QUALITY WORKOUT CORPUS MIGRATION COMPLETE` notice with the per-band counts. (2) Apply #155, verify trailing audit emits `✅ delete_workout_and_revert_stats DEPLOYED`. (3) Ship paired iOS commit. Both migrations are standalone in the sense that NO companion edge function / cron change is required — score-on-completion is fire-and-forget from iOS; the delete RPC is request/response from iOS.

**Open questions / gotchas for the iOS side (Wave 2)**:

- The league reversal in #155 limits the `league_point_awards` DELETE to ONE row (most recent by `awarded_at` matching `(user_id, source='workout', awarded_date)`). If a user logs 2 workouts in a day and deletes the OLDER one, the newer workout's award is the one reversed — semantically wrong but rare (and self-corrects on the next workout). Long-term fix: store the workout's UUID in `league_point_awards.attribution` for `source='workout'` rows so the reversal can target the exact award. That's a follow-up migration once iOS stamps the attribution on `add_league_points` calls.
- `awarded_points` on `league_point_awards` is already multiplier-baked (Peak Day 3× is folded into the stored value). The reversal sums `awarded_points` directly (not `awarded_points * multiplier`) — the spec snippet that said "SUM(awarded_points * multiplier)" was incorrect; doing that would double-revert.
- `score_workout_quality` is fail-soft — bad rubric inputs return `{success:false}` rather than raising. The iOS fire-and-forget call should log the failure but never block the completion path on it.
- The FE balance heuristic (5 pts) is best-effort token matching against `exercise_name`. False positives / negatives are acceptable since it caps at 5 of 100 — never carries a poor workout over the 70 threshold by itself.

---

## Legacy / Bulk Ledger (Q2-85, Sprint 8 — 2026-04-27)

This section documents `supabase/*.sql` files that are **intentionally not in
the numbered release-train above**. They are either pre-`YYYYMMDD`_ legacy
migrations, one-shot bulk data loads, or read-only auditors. They live on
disk for audit / history only — do NOT re-run on prod without checking
§Process below first.

### Pre-`YYYYMMDD`_ era (historic)

Ship order is lost; effects have been absorbed / superseded by the numbered
release-train migrations above. Kept for historical audit.


| File                                     | Category            | Notes                                                                                                                     |
| ---------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `challenge_rpc_functions.sql`            | Challenges (legacy) | Canonical owners for most of these RPCs are now the `fix_`* files listed in §Duplicated Function Definitions above.       |
| `challenge_type_migration.sql`           | Challenges (legacy) | Superseded by `fix_challenge_cascade_delete.sql` + `fix_challenge_participants.sql`.                                      |
| `challenge_reactions.sql`                | Challenges (legacy) | Schema live; no further changes expected.                                                                                 |
| `community_challenges_migration.sql`     | Challenges (legacy) | Schema live; later hotfixes consolidated.                                                                                 |
| `community_friends_gating.sql`           | Friends (legacy)    | Gating now enforced by RPCs in `create_friend_rpc_functions.sql` + `fix_friend_safety.sql`.                               |
| `create_friend_rpc_functions.sql`        | Friends (legacy)    | Canonical RPC shapes live; later hotfixes add IDOR guards.                                                                |
| `fix_friend_safety.sql`                  | Friends (legacy)    | Layered on by `fix_comprehensive_audit.sql`.                                                                              |
| `friend_request_system.sql`              | Friends (legacy)    | Superseded by `friend_request_notifications.sql`.                                                                         |
| `friend_request_notifications.sql`       | Friends (legacy)    | Canonical owner of `send_friend_request`.                                                                                 |
| `cascade_delete_incomplete_profiles.sql` | Auth / profile      | Paired with `cleanup_incomplete_onboarding.sql`.                                                                          |
| `cleanup_incomplete_onboarding.sql`      | Auth / profile      | pg_cron-driven cleanup job; still live.                                                                                   |
| `cleanup_test_accounts.sql`              | Ops                 | Manual cleanup helper. Run ad hoc only.                                                                                   |
| `complete_account_deletion.sql`          | Auth / profile      | Canonical owner of `delete_user_account`.                                                                                 |
| `daily_quests_migration.sql`             | Daily Quests        | Superseded by 2026-04-xx quest migrations (#54, #57–#59).                                                                 |
| `daily_quests_v2_migration.sql`          | Daily Quests        | Superseded by 2026-04-xx quest migrations.                                                                                |
| `fix_account_deletion.sql`               | Auth / profile      | DELETED 2026-04-17 (see §Duplicated Function Definitions).                                                                |
| `fix_ambiguous_columns.sql`              | Challenges (legacy) | Canonical owner of `log_challenge_progress`.                                                                              |
| `fix_cardio_workouts_constraint.sql`     | Cardio              | Canonical; absorbed into release-train.                                                                                   |
| `fix_challenge_cascade_delete.sql`       | Challenges (legacy) | Canonical owner of cascade policies.                                                                                      |
| `fix_challenge_participants.sql`         | Challenges (legacy) | Canonical owner of participant constraints.                                                                               |
| `fix_comprehensive_audit.sql`            | Multi               | Canonical owner of several friend RPCs.                                                                                   |
| `fix_contact_matching_rls.sql`           | Contacts            | Canonical RLS for contact discovery.                                                                                      |
| `fix_create_group_challenge.sql`         | Challenges (legacy) | Canonical owner of `create_group_challenge`.                                                                              |
| `fix_data_relationships.sql`             | Multi               | FK + orphan cleanup.                                                                                                      |
| `fix_group_challenge_timezone.sql`       | Challenges (legacy) | Canonical owner of `get_active_group_challenges` + `log_group_challenge_progress`.                                        |
| `fix_leave_group_challenge.sql`          | Challenges (legacy) | Canonical owner of `leave_group_challenge`.                                                                               |
| `fix_materialized_view.sql`              | Multi               | One-shot MV refresh.                                                                                                      |
| `fix_private_realtime_publication.sql`   | Realtime            | Private challenge realtime bootstrap (missed `private_challenge_chat` — closed by 20260426_sprint7_security_hygiene.sql). |
| `fix_trigger_conflicts.sql`              | Auth / profile      | Canonical owner of `cleanup_auth_on_profile_delete`.                                                                      |
| `global_food_popularity.sql`             | Nutrition           | Canonical schema live.                                                                                                    |
| `private_challenges_migration.sql`       | Private Challenges  | Canonical schema; realtime bootstrap above.                                                                               |
| `program_templates_migration.sql`        | Programs            | Canonical schema live.                                                                                                    |
| `refresh_exercise_view.sql`              | Exercises           | Manual MV refresh helper.                                                                                                 |
| `verify_critical_functions.sql`          | Audit               | Read-only verifier.                                                                                                       |
| `weekly_leagues_migration.sql`           | Weekly Leagues      | Canonical schema; later hotfixes layered on.                                                                              |


### Bulk exercise data loads (one-shot)

These files ship thousands of `INSERT … ON CONFLICT` statements for the
exercise library. Already applied on prod; do not re-run unless re-seeding
a fresh Supabase project.


| Pattern                                                | Count | Purpose                                                                                   |
| ------------------------------------------------------ | ----- | ----------------------------------------------------------------------------------------- |
| `exercise_replace_{01..15}.sql`                        | 15    | Generational CSV re-import (15 batches).                                                  |
| `update_exercises_{csv,batch2,batch3,final_fixed}.sql` | 4     | Incremental CSV corrections. Canonical final batch is `update_exercises_final_fixed.sql`. |
| `exercises_update_*.sql`, `update_2_exercises_*.sql`   | misc  | Targeted string fixes (lever names, spaces, etc.).                                        |


### Auditors / verifiers (read-only)


| File                           | Purpose                                                                                                   |
| ------------------------------ | --------------------------------------------------------------------------------------------------------- |
| `audit_before.sql`             | Baseline snapshot; runs before `20260320_`* audit remediation.                                            |
| `audit_after.sql`              | 7-test verification suite; runs after.                                                                    |
| `verify_query_performance.sql` | Indexes-present audit; extend when adding hot-path indexes. See DB-5 entry above.                         |
| `sim_test_helpers.sql`         | Fixture / simulation helpers for automated QA only. Never ship to prod.                                   |
| `migrations/*.sql`             | Legacy CLI-style migrations (pre-SQL Editor workflow); canonical files are all in `supabase/*.sql` today. |


---

## Process for New Migrations

1. Create a new file: `YYYYMMDD_description.sql`
2. Wrap DDL in `BEGIN; ... COMMIT;` for transactional safety
3. DROP all overloads before `CREATE OR REPLACE FUNCTION` (see supabase-rules §12)
4. Add `IF NOT EXISTS` to every `CREATE INDEX` / `CREATE TABLE` for idempotency
5. Add the file to the numbered release-train above with status 🆕
6. Test in a staging project first
7. Deploy via Supabase SQL Editor
8. Update status to ✅ Deployed