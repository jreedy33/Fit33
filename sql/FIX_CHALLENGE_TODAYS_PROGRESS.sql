-- =====================================================
-- STEP 1: First run this to drop the old function
-- =====================================================
DROP FUNCTION IF EXISTS get_active_challenges();

-- If successful, you should see "Success. No rows returned"
-- Then proceed to run STEP 2 below
